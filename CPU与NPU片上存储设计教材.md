# CPU 与 NPU 片上存储设计
## —— 从 Cache 到 Scratchpad 的系统自学教程

> 定位读者：具备基础计算机组成原理知识（了解 CPU、内存、总线、流水线），希望系统学习 CPU 与 NPU 中 cache 及其他片上存储设计的工程师与学生。
> 目标：从零建立完整知识体系，理解真实芯片中的设计权衡，最终具备独立进行存储子系统设计与分析的能力。

---

## 前言：本书怎么用

本书沿着一条主线展开：**处理器与存储器之间的速度鸿沟（存储墙）是一切问题的根源，而存储层次是对这个问题的系统性回答**。CPU 用"硬件管理的 cache"回答它，NPU 用"软件管理的 scratchpad"回答它，异构系统则要回答"两种回答如何共存"。

建议按顺序阅读，因为后面的章节会频繁引用前面的结论：

- **第一部分（第 1～2 章）**：建立全局图景——为什么需要存储层次，存储器件的物理基础是什么。这是全书的"公理"。
- **第二部分（第 3～7 章）**：CPU cache 的完整设计空间——映射、替换、写策略、多级层次、一致性、PPA 权衡。这是全书的"主干"。
- **第三部分（第 8～12 章）**：NPU 的片上存储——SPM、数据流架构、混合存储与优化实例。这是与第二部分平行的另一条技术路线。
- **第四部分（第 13～15 章）**：当 CPU 与 NPU 出现在同一颗芯片上，存储如何协同——共享内存、一致性扩展、数据搬移调度。
- **第五部分（第 16～18 章）**：从"理解设计"到"做出设计"——设计流程、工具链、误区与综合案例实践。

每章末尾的练习请务必动手做，尤其是计算题——存储设计的核心能力是**用数字做权衡**，而不是背概念。

### 编写体例说明

全书每章均包含以下九个固定板块，按序排列：

1. **本章导读**——本章在知识体系中的位置；
2. **学习目标**——3～5 条可衡量的目标；
3. **核心概念**——本章关键术语中英文对照表；
4. **详细讲解**——按小节展开，遵循"直观类比 → 结构图示化描述 → 参数/公式/实例计算"三段式；
5. **案例/示例**——真实芯片或典型应用分析；
6. **常见误区与注意事项**——新手易踩的坑；
7. **本章小结**——要点回顾；
8. **思考题与练习**——概念题、计算分析题、开放设计题；
9. **拓展阅读**——经典论文与书籍章节。

---

# 全书目录

## 第一部分：存储层次与背景基础

**第 1 章 为什么需要存储层次**
- 1.1 存储墙：处理器与内存速度的剪刀差
- 1.2 局部性原理：程序行为的天然规律
- 1.3 存储层次的基本结构：一座金字塔
- 1.4 命中率、缺失率与平均访存时间（AMAT）
- 1.5 缺失的三种类型：3C 分类法
- 1.6 案例研究：一台真实处理器的存储层次

**第 2 章 存储技术基础与术语**
- 2.1 位的世界：寄存器、SRAM、DRAM 的存储原理
- 2.2 SRAM 与 DRAM 全方位对比：速度、面积、功耗、成本
- 2.3 两种管理哲学：Cache（硬件管理）与 Scratchpad Memory（软件管理）
- 2.4 关键术语精读：块/行、标记、索引、偏移、相联度、命中延迟、缺失代价
- 2.5 存储层次设计的基本权衡：容量 × 延迟 × 带宽 × 能耗 × 面积

## 第二部分：CPU Cache 设计

**第 3 章 Cache 基本结构与地址映射**
- 3.1 一次查找的完整旅程：拆分地址 → 定位组 → 比对标记
- 3.2 直接映射（Direct-Mapped）：一个地址只有一个家
- 3.3 全相联（Fully Associative）：随处可住
- 3.4 组相联（Set-Associative）：折中的智慧
- 3.5 地址划分实战：tag / index / offset 位数计算
- 3.6 真实 CPU 的 cache 参数解读（Intel / AMD / ARM 实例）

**第 4 章 替换策略与写策略**
- 4.1 替换问题：新块进门，谁该搬走
- 4.2 替换策略：LRU、伪 LRU（PLRU）、随机、FIFO 的硬件实现与对比
- 4.3 写策略（一）：写直达（Write-Through）
- 4.4 写策略（二）：写回（Write-Back）与脏位管理
- 4.5 写分配（Write-Allocate）与非写分配：缺失时的写怎么办
- 4.6 写缓冲区（Write Buffer）：让写不再阻塞
- 4.7 策略组合的工程选择与真实案例

**第 5 章 多级 Cache 与高级优化技术**
- 5.1 为什么需要多级 cache：延迟与容量的分工
- 5.2 包含性策略：包含（Inclusive）、非包含（Non-Inclusive）、互斥（Exclusive）
- 5.3 降低冲突与延迟的武器：Victim Cache、Skewed-Associative、Way Prediction
- 5.4 硬件预取：Next-Line、步长（Stride）、流（Stream）、全局历史缓冲（GHB）
- 5.5 软件预取：x86 PREFETCH 等指令的使用方法与陷阱
- 5.6 案例分析：Intel Skylake 的多级预取器

**第 6 章 缓存一致性（Cache Coherence）**
- 6.1 多核时代的难题：两份副本，谁说了算
- 6.2 写无效（Write-Invalidate）与写更新（Write-Update）
- 6.3 总线侦听与 MSI 协议：状态机的舞蹈
- 6.4 MESI 与 MOESI：从四状态到五状态
- 6.5 目录协议：全位目录、有限指针目录、链式目录
- 6.6 真实处理器的一致性实现：Intel 的 MESIF 与 AMD 的 MOESI

**第 7 章 Cache 设计中的性能、功耗与面积权衡**
- 7.1 参数敏感性分析：容量、块大小、相联度对缺失率、命中延迟、功耗的影响
- 7.2 低功耗设计：Banked Cache、Drowsy Cache、动态容量/相联度调节
- 7.3 关键路径分析与时序优化：Tag 阵列、数据阵列与比较器
- 7.4 CACTI 入门：面积、延迟、能耗估算实操
- 7.5 案例研究：嵌入式 CPU 与高性能 CPU 的 cache 设计决策对比

## 第三部分：NPU 片上存储设计

**第 8 章 NPU 计算模式与存储需求分析**
- 8.1 神经网络算子的数据流特征：卷积、全连接、池化、激活函数
- 8.2 卷积中的三类数据：输入特征图、权重、输出特征图及其复用机会
- 8.3 为什么 NPU 偏爱 Scratchpad：可预测性、能耗、实时性
- 8.4 典型 NPU 的存储层次：寄存器文件 → 片上 SRAM → 片外 DRAM

**第 9 章 Scratchpad Memory（SPM）设计**
- 9.1 SPM 的地址空间管理：静态分配、动态分配、编译器自动管理
- 9.2 SPM 与 DMA 的共舞：搬运粒度与双缓冲/多缓冲
- 9.3 多 Bank 设计：提升带宽、检测与规避 bank 冲突
- 9.4 容量与带宽的权衡：不同网络层的需求差异

**第 10 章 数据流架构与存储适配**
- 10.1 四种经典数据流：Weight Stationary、Output Stationary、Row Stationary、No Local Reuse
- 10.2 数据流决定存储调度：什么数据留片上、DMA 何时搬
- 10.3 循环变换：分块（Tiling）、循环展开、循环交换、im2col 与直接卷积的存储开销对比
- 10.4 案例：Google TPU v1（统一缓冲区与脉动阵列）与 NVIDIA DLA 的存储层次

**第 11 章 NPU 中的片上缓存与混合存储**
- 11.1 NPU 何时仍需要 cache：不规则数据、稀疏计算、动态控制流
- 11.2 混合存储架构：cache 与 SPM 各自负责什么数据
- 11.3 权重压缩与稀疏存储：剪枝、量化后的 CSR/CSC 格式对存储层次的影响
- 11.4 片上网络（NoC）与存储的交互：多核 NPU 的数据路由

**第 12 章 NPU 存储层次优化实例**
- 12.1 为 ResNet 与 MobileNet 做存储优化：tiling 尺寸、SPM 容量与带宽选择
- 12.2 编译器与硬件协同：TVM / MLIR 的存储规划与自动 tiling
- 12.3 性能建模：Roofline 模型在 NPU 存储分析中的应用
- 12.4 能耗视角：数据搬运能耗 vs 计算能耗，最小化数据移动的设计原则

## 第四部分：异构系统存储协同设计

**第 13 章 CPU 与 NPU 的存储交互方式**
- 13.1 共享内存 vs 分离内存 vs 统一内存：三种架构的利弊
- 13.2 数据共享与同步机制：中断、信号量、原子操作、事件
- 13.3 任务下发时的数据传递：地址传递 vs 数据拷贝
- 13.4 异构缓存一致性：ARM DSU 与 CCI/CCN 一致性互连

**第 14 章 异构缓存一致性与内存模型**
- 14.1 非一致内存访问（NUMA）在异构系统中的表现
- 14.2 互连协议：AMBA CHI、Intel UPI 与 PCIe/CXL.cache
- 14.3 内存模型（弱内存模型）对异构编程的影响
- 14.4 案例：手机 SoC（如高通骁龙）中 CPU/GPU/NPU 的共享内存与一致性

**第 15 章 数据搬移与调度优化**
- 15.1 零拷贝技术：避免 CPU 与 NPU 之间的无谓拷贝
- 15.2 异步数据搬运：DMA 引擎与计算的重叠执行
- 15.3 多任务并发下的存储带宽分配与 QoS 保障
- 15.4 系统级案例：自动驾驶芯片中多传感器数据的存储协同

## 第五部分：设计方法、工具与实践

**第 16 章 存储子系统设计流程**
- 16.1 需求分析：从应用负载提取存储访问模式（profiler 实操）
- 16.2 设计空间探索：容量、块大小、相联度、bank 数的确定方法
- 16.3 性能评估：gem5、Sniper 与自研 trace-driven 模拟器
- 16.4 物理评估：CACTI 与 McPAT 的面积、延迟、功耗估算
- 16.5 迭代优化：从初始设计到满足性能/功耗/面积目标的闭环

**第 17 章 常见设计误区与工程经验**
- 17.1 六大典型误区：相联度滥用、写策略忽视、预取污染、SPM 尺寸失当、bank 冲突、一致性盲区
- 17.2 对应的工程解决方案与真实教训

**第 18 章 综合案例研究与实践练习**
- 18.1 案例 1：嵌入式 CPU 的 L1 数据 cache 设计（含完整权衡过程）
- 18.2 案例 2：面向 CNN 的 NPU 存储层次设计（SPM 容量、tiling、DMA 调度）
- 18.3 动手实践：用 Verilog/SystemC 实现组相联 cache，或用 Python 编写 cache 模拟器
- 18.4 综合设计报告的撰写规范

## 附录

- 附录 A：术语表（中英文对照）
- 附录 B：推荐阅读与参考资料
- 附录 C：常用设计参数速查表

---
---

# 第 1 章 为什么需要存储层次

## 本章导读

本书要讨论的一切——CPU 里的 cache、NPU 里的 scratchpad、多核之间的一致性协议——都源于同一个矛盾：**我们造得出极快的处理器，却造不出又快、又大、又便宜的主存**。本章先讲清楚这个矛盾（存储墙），再说明为什么它没有拖垮整个计算机产业（局部性原理），然后给出工程师的系统性回答（存储层次），最后介绍量化分析存储层次的基本工具（命中率、AMAT 公式、3C 缺失分类）。

本章是全书的"第一因"。第 3～7 章的每一个 cache 设计决策，几乎都能追溯到本章的某个公式或某条原理；第 8 章之后的 NPU 设计师之所以选择另一条路线，也正是从本章的矛盾出发得出了不同的结论。请务必把本章的两个计算工具（AMAT、3C）练到熟练。

## 学习目标

读完本章，你应当能够：

1. 用具体数字解释存储墙问题（说出 CPU 时钟周期与 DRAM 延迟的数量级差距及其历史趋势）；
2. 在给定代码片段中指出时间局部性与空间局部性，并判断哪些访问模式会破坏局部性；
3. 默画典型存储层次结构，说出每一级的典型容量、延迟量级与管理者；
4. 熟练使用 AMAT 公式计算平均访存时间，并定量分析缺失率对性能的敏感性；
5. 将给定的 cache 缺失归入强制/容量/冲突三类，并说出每类缺失对应的设计对策。

## 核心概念

| 术语（中文） | 英文 | 一句话定义 |
|---|---|---|
| 存储墙 | memory wall | 处理器速度与内存速度的差距持续扩大的现象 |
| 时间局部性 | temporal locality | 刚被访问的信息，不久后很可能再次被访问 |
| 空间局部性 | spatial locality | 刚被访问的地址附近，很可能马上被访问 |
| 存储层次 | memory hierarchy | 由多级速度/容量/成本递变的存储器组成的系统 |
| 命中 / 缺失 | hit / miss | 要访问的块在/不在当前层级 |
| 命中率 / 缺失率 | hit rate / miss rate | 命中（缺失）次数占访问总数的比例 |
| 命中时间 | hit time | 访问本层级并判断命中的总时间 |
| 缺失代价 | miss penalty | 缺失后从下一级取回数据所需的额外时间 |
| 平均访存时间 | AMAT (Average Memory Access Time) | 综合命中与缺失的平均每次访存时间 |
| 块 / 行 | block / line | cache 与主存之间数据搬运的基本单位 |
| 工作集 | working set | 程序在一段时间内活跃访问的数据集合 |
| 强制缺失 | compulsory (cold) miss | 块第一次被访问导致的缺失 |
| 容量缺失 | capacity miss | 容量不足、块被逐出后再次访问导致的缺失 |
| 冲突缺失 | conflict miss | 组/行冲突导致的逐出与再访问（容量足够时本可避免） |

## 1.1 存储墙：处理器与内存速度的剪刀差

先看一组数字。一颗现代高性能 CPU 的主频约为 3～5 GHz，一个时钟周期只有 0.2～0.33 ns；而一次 DRAM 访问，从地址发出到数据返回，典型需要 60～100 ns。也就是说，**CPU 等一次内存的时间，足够它执行两百到五百条指令**。

这还不是最糟的——最糟的是这个差距曾经长期持续扩大。在 1980 年到 2000 年间，CPU 性能大约每年提升 50% 以上（主频提升与每周期指令数提升双轮驱动），而 DRAM 的速度每年只改善约 7%。两条曲线像一把逐渐张开的剪刀（见图 1-1），“存储墙”（memory wall）因此得名。2005 年之后，受功耗限制，单线程性能增速放缓，但绝对差距已经拉大到几个数量级，再也无法靠“等内存追上来”解决。

**图 1-1　存储墙：CPU 与 DRAM 的速度剪刀差（对数坐标示意）**

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 680 320" width="100%" role="img">
  <title>存储墙：CPU 与 DRAM 的速度剪刀差</title>
  <desc>1980 至 2020 年 CPU 整数性能与 DRAM 访问速度的对比曲线（对数坐标示意），两者差距扩大至数万倍。</desc>
  <text x="340" y="24" text-anchor="middle" font-size="14" font-weight="500" fill="#333333">存储墙：CPU 与 DRAM 的速度剪刀差（对数坐标示意）</text>
  <g stroke="#cccccc" stroke-width="0.5">
    <line x1="80" y1="260" x2="640" y2="260"/><line x1="80" y1="230" x2="640" y2="230"/>
    <line x1="80" y1="200" x2="640" y2="200"/><line x1="80" y1="170" x2="640" y2="170"/>
    <line x1="80" y1="140" x2="640" y2="140"/><line x1="80" y1="110" x2="640" y2="110"/>
    <line x1="80" y1="80" x2="640" y2="80"/><line x1="80" y1="50" x2="640" y2="50"/>
  </g>
  <g font-size="11" fill="#888888" text-anchor="end">
    <text x="72" y="264">1×</text><text x="72" y="234">10×</text><text x="72" y="204">10²×</text>
    <text x="72" y="174">10³×</text><text x="72" y="144">10⁴×</text><text x="72" y="114">10⁵×</text>
    <text x="72" y="84">10⁶×</text><text x="72" y="54">10⁷×</text>
  </g>
  <g font-size="11" fill="#888888" text-anchor="middle">
    <text x="80" y="276">1980</text><text x="220" y="276">1990</text><text x="360" y="276">2000</text>
    <text x="500" y="276">2010</text><text x="640" y="276">2020</text>
  </g>
  <polygon points="80,260 220,207 360,154 430,128 500,116 570,104 640,92 640,225 570,231 500,235 430,239 360,243 220,251 80,260" fill="#E24B4A" fill-opacity="0.12"/>
  <polyline points="80,260 220,207 360,154 430,128 500,116 570,104 640,92" fill="none" stroke="#E24B4A" stroke-width="2"/>
  <polyline points="80,260 220,251 360,243 430,239 500,235 570,231 640,225" fill="none" stroke="#378ADD" stroke-width="2"/>
  <line x1="430" y1="50" x2="430" y2="260" stroke="#999999" stroke-width="1" stroke-dasharray="3,3"/>
  <text x="436" y="64" font-size="11" fill="#777777">2005：单线程增速放缓</text>
  <line x1="100" y1="56" x2="130" y2="56" stroke="#E24B4A" stroke-width="2"/>
  <text x="138" y="60" font-size="12" fill="#444444">CPU 整数性能（1980–2004 年均约 +50%）</text>
  <line x1="100" y1="76" x2="130" y2="76" stroke="#378ADD" stroke-width="2"/>
  <text x="138" y="80" font-size="12" fill="#444444">DRAM 访问速度（年均约 +7%）</text>
  <text x="455" y="165" font-size="13" font-weight="500" fill="#A32D2D">存储墙（memory wall）</text>
  <text x="455" y="182" font-size="11" fill="#888888">速度差距已达数万倍</text>
  <text x="632" y="84" text-anchor="end" font-size="11" fill="#A32D2D">≈4×10⁵ 倍</text>
  <text x="632" y="217" text-anchor="end" font-size="11" fill="#185FA5">≈15 倍</text>
  <text x="80" y="300" font-size="11" fill="#999999">纵轴：相对 1980 年的性能倍数（对数刻度）；曲线为量级示意，非精确数据</text>
</svg>

为什么造不出"又快又大又便宜"的内存？因为物理规律不允许三个目标同时成立：

- **快**要求存储单元结构简单、连线短、信号路径短；
- **大**要求极高的位密度，而高密度单元（如 DRAM 的"1 晶体管 + 1 电容"结构）天然比 SRAM 的"6 晶体管"结构慢一个数量级；
- **便宜**要求每位成本极低，这几乎等价于要求高密度。

三个目标两两冲突，必须取舍。于是工程师面临选择：要么接受一台"极慢"的计算机，要么换一种思路。

**直观类比：厨师与仓库。** 想象一位刀工极快的厨师，切一刀只要 0.3 秒；但食材仓库在 100 米外的另一栋楼，取一次食材来回要 100 秒。如果每切一刀都跑一趟仓库，这位米其林大厨的实际出菜速度还不如街边小摊。解决思路显然不是"把整栋仓库搬到灶台边"（太贵、也放不下），而是：**在灶台边放一个小小的备菜台，把接下来大概率要用的食材提前备好**。这个小备菜台，就是 cache；"提前备好"的机制，就是本书第 3～5 章的主角。

但"提前备好"需要一个前提：我们得能猜到厨师接下来要用什么食材。幸运的是，程序的行为恰好提供了这个前提——这就是局部性原理。

## 1.2 局部性原理：程序行为的天然规律

存储层次之所以成立，靠的不是运气，而是程序的两个统计学规律：

- **时间局部性（temporal locality）**：刚被访问过的信息，很可能在不久后再次被访问。
- **空间局部性（spatial locality）**：刚被访问过的地址附近，很可能马上被访问。

用代码说话。设 `a` 为 N×N 的 int 二维数组（C 语言按行存储），N = 4096，cache 块大小 64 B：

```c
// 代码 A：行优先顺序遍历 —— 空间局部性极佳
for (int i = 0; i < N; i++)
    for (int j = 0; j < N; j++)
        sum += a[i][j];        // 沿 j 推进，地址连续

// 代码 B：列优先遍历同一个数组 —— 空间局部性极差
for (int j = 0; j < N; j++)
    for (int i = 0; i < N; i++)
        sum += a[i][j];        // 每步跳跃 N×4 = 16 KB
```

- **代码 A**：一行有 N×4 = 16 KB，一个 64 B 的块装 16 个 int；沿 j 推进时地址连续，**每 16 次访问才发生一次缺失**——缺失率约 6.25%，其余全部命中。
- **代码 B**：每一步跳 16 KB，前后两次访问几乎总落在不同块上；整个矩阵 64 MB，远超任何 cache，**几乎每次访问都缺失**。同样的计算量，实测性能可以相差 10 倍以上。

时间局部性的典型来源：循环中反复读写的循环变量与累加变量、热点函数的反复执行（指令 cache 命中）、内层循环中保持不变的数组元素、栈上反复访问的局部变量。

也要认识局部性的边界——以下模式会**破坏**它：

- **指针追逐（pointer chasing）**：`p = p->next`，下一个节点的地址要等当前节点从内存读出来才知道，且堆上节点随机散布——时间、空间局部性都很差；
- **大工作集随机访问**：数据总量远大于 cache 且访问近乎随机（如巨大的哈希表）；
- **大步长访问**：按列访问行优先矩阵、stride 很大的抽样。

一个关键认知：**局部性是概率规律，不是保证**。cache 是一场"赌大多数程序、在大多数时间有局部性"的赌博——赌赢时收益巨大，赌输时性能退化为 DRAM 速度。第 3～5 章的所有设计技巧，本质上都在提高赌赢的概率；而第 8 章之后的 NPU 设计师换了一种思路——**干脆不赌，让软件显式保证数据搬运的确定性**，这就是 scratchpad memory。

## 1.3 存储层次的基本结构：一座金字塔

既然"又快又大又便宜"的单级内存不存在，工程师的答案是：**用多级存储器堆一座金字塔**——越往上越快、越小、越贵；越往下越慢、越大、越便宜。

**直观类比：从书桌到国家图书馆。** 你写作业时，常用的笔和书摊在**书桌**上（寄存器/L1，伸手即得）；桌上放不下的放在**身后书架**（L2/L3，起身可取）；书架没有的去**市图书馆**借（主存 DRAM，走路半小时）；图书馆也没有的，走**馆际调档**（磁盘/外存，等几天）。

典型现代处理器的存储层次如下（数值为量级典型值，周期按 3 GHz 折算）：

| 层级 | 典型容量 | 典型延迟 | 带宽量级 | 谁来管理 |
|---|---|---|---|---|
| 寄存器 | 数百 B ~ 数 KB | ~1 周期（<0.5 ns） | 每周期多读多写 | 编译器 |
| L1 cache（指令/数据各一份） | 32～64 KB × 2 | ~4 周期（≈1 ns） | ~100 GB/s 级 | 硬件（对软件透明） |
| L2 cache（每核私有） | 256 KB～2 MB | ~12 周期（≈4 ns） | ~50 GB/s 级 | 硬件 |
| L3 cache（多核共享） | 8～96 MB | ~40 周期（≈15 ns） | ~20 GB/s 级 | 硬件 |
| 主存 DRAM | 8～64 GB | 200～300 周期（≈70–100 ns） | ~30–60 GB/s | OS + 内存控制器 |
| SSD / 机械硬盘 | 数百 GB～TB 级 | 10⁵～10⁷ 周期（µs–ms 级） | GB/s 级 | OS + 文件系统 |

**图 1-2　现代处理器的存储层次金字塔**

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 680 478" width="100%" role="img">
  <title>现代处理器的存储层次金字塔</title>
  <desc>从寄存器、L1、L2、L3、DRAM 到外存的六级存储层次，自上而下更快更贵更小，附各级典型容量与延迟。</desc>
  <text x="340" y="28" text-anchor="middle" font-size="14" font-weight="500" fill="#333333">现代处理器的存储层次金字塔</text>
  <text x="340" y="46" text-anchor="middle" font-size="12" fill="#888888">自上而下：更快 · 更贵 · 更小　｜　自下而上：更慢 · 更便宜 · 更大（周期按 3 GHz 折算）</text>
  <g>
    <rect x="225" y="60" width="230" height="54" rx="8" fill="#E6F1FB" stroke="#185FA5" stroke-width="0.5"/>
    <text x="241" y="83" font-size="13" font-weight="500" fill="#0C447C">寄存器 Registers</text>
    <text x="241" y="102" font-size="12" fill="#0C447C">数百 B ~ 数 KB ｜ 约 1 周期</text>
    <rect x="195" y="122" width="290" height="54" rx="8" fill="#B5D4F4" stroke="#185FA5" stroke-width="0.5"/>
    <text x="211" y="145" font-size="13" font-weight="500" fill="#0C447C">L1 Cache（指令 / 数据各一份）</text>
    <text x="211" y="164" font-size="12" fill="#0C447C">32 – 64 KB × 2 ｜ 约 4 周期（≈1 ns）</text>
    <rect x="165" y="184" width="350" height="54" rx="8" fill="#85B7EB" stroke="#185FA5" stroke-width="0.5"/>
    <text x="181" y="207" font-size="13" font-weight="500" fill="#0C447C">L2 Cache（每核私有）</text>
    <text x="181" y="226" font-size="12" fill="#0C447C">256 KB – 2 MB ｜ 约 12 周期（≈4 ns）</text>
    <rect x="135" y="246" width="410" height="54" rx="8" fill="#378ADD" stroke="#185FA5" stroke-width="0.5"/>
    <text x="151" y="269" font-size="13" font-weight="500" fill="#F2F8FD">L3 Cache（多核共享）</text>
    <text x="151" y="288" font-size="12" fill="#E6F1FB">8 – 96 MB ｜ 约 40 周期（≈15 ns）</text>
    <rect x="105" y="308" width="470" height="54" rx="8" fill="#B4B2A9" stroke="#5F5E5A" stroke-width="0.5"/>
    <text x="121" y="331" font-size="13" font-weight="500" fill="#444441">主存 DRAM（DDR4 / DDR5）</text>
    <text x="121" y="350" font-size="12" fill="#444441">8 – 64 GB ｜ 200 – 300 周期（≈70 – 100 ns）</text>
    <rect x="75" y="370" width="530" height="54" rx="8" fill="#D3D1C7" stroke="#5F5E5A" stroke-width="0.5"/>
    <text x="91" y="393" font-size="13" font-weight="500" fill="#444441">外存：SSD / 机械硬盘</text>
    <text x="91" y="412" font-size="12" fill="#444441">数百 GB – TB 级 ｜ 约 10⁵ – 10⁷ 周期（µs – ms 级）</text>
  </g>
  <line x1="625" y1="80" x2="625" y2="410" stroke="#888888" stroke-width="1.5"/>
  <polygon points="625,422 619,408 631,408" fill="#888888"/>
  <text x="625" y="72" text-anchor="middle" font-size="11" fill="#888888">快 · 贵 · 小</text>
  <text x="625" y="440" text-anchor="middle" font-size="11" fill="#888888">慢 · 便宜 · 大</text>
  <text x="75" y="462" font-size="11" fill="#999999">注：容量与延迟为现代典型值；L1 分指令/数据两份，L3 为多核共享</text>
</svg>

对这座金字塔，要建立三个核心洞察：

1. **每一级（大概率）是下一级的子集**：L2 里的数据基本都在 L3 里，L3 里的数据都在 DRAM 里。层次越往下，内容越全。
2. **层次逼近两个不可能同时达到的极限**：如果命中率足够高，整体速度逼近最快一级，而每位成本逼近最便宜一级——这正是局部性原理"变现"的地方。
3. **数据在层间以"块"为单位移动**，一次搬运一整块（现代典型 64 B）而不是一个字。这样做的目的：一次搬大块可以摊薄每次搬运的固定开销，同时把空间局部性"打包"利用——访问 a[10] 时顺手把 a[11]～a[25] 都带上来。

注意金字塔顶端的两级（寄存器、L1）与底部的管理方式分野：CPU 一侧全靠**硬件自动管理**，程序员写代码时甚至感觉不到 cache 的存在；而本书第三部分会看到，NPU 一侧常用**软件显式管理**的 scratchpad。同一座金字塔，两种管理哲学，这是贯穿全书的一条暗线。

## 1.4 命中率、缺失率与平均访存时间（AMAT）

有了层次，第一个必须回答的量化问题就是：**它到底有多快？** 这需要三个基本量：

- **命中率（hit rate）**：访问在本层级找到数据的比例；**缺失率（miss rate）= 1 − 命中率**。
- **命中时间（hit time）**：访问本层级并完成命中判断的总时间（含地址解码、阵列读取、标记比较）。
- **缺失代价（miss penalty）**：缺失后从下一级取回数据所需的额外时间。

三者合成全书最重要的公式之一——**平均访存时间**：

> **AMAT = 命中时间 + 缺失率 × 缺失代价**

**例 1（单级计算）**：L1 命中时间 1 ns（3 GHz 下约 3 个周期），缺失率 5%，缺失代价 90 ns：

```
AMAT = 1 + 0.05 × 90 = 5.5 ns ≈ 16.5 个周期
```

平均每次访存时间是命中时间的 **5.5 倍**——尽管命中率高达 95%。

**例 2（敏感性分析）**：沿用例 1，只改缺失率：

| 缺失率 | AMAT 计算 | 结果 | 相对命中时间 |
|---|---|---|---|
| 1% | 1 + 0.01 × 90 | 1.9 ns | 1.9× |
| 5% | 1 + 0.05 × 90 | 5.5 ns | 5.5× |
| 10% | 1 + 0.10 × 90 | 10.0 ns | 10× |

结论：**AMAT 对缺失率高度敏感且非线性**。缺失率从 5% 降到 4%，AMAT 从 5.5 ns 降到 4.6 ns（改善 16%）。这就解释了为什么 CPU 设计师愿意付出巨大的面积和功耗代价，只为把缺失率降低一两个百分点——也解释了为什么"命中率 95% 看起来很高"完全不够用。

**例 3（多级层次，为第 5 章预热）**：两级层次可递归套用公式。设 L1 命中时间 4 周期、缺失率 4%；L2 命中时间 12 周期、局部缺失率 25%（即 L1 缺失下来的访问中又有 25% 在 L2 缺失）；DRAM 缺失代价 270 周期：

```
AMAT(L2 及以下) = 12 + 0.25 × 270 = 79.5 周期
AMAT(总)        = 4 + 0.04 × 79.5 ≈ 7.2 周期 ≈ 2.4 ns（3 GHz）
```

注意这里"局部缺失率"（该级缺失数 ÷ 到达该级的访问数）与"全局缺失率"（该级缺失数 ÷ CPU 全部访存数）的区别——看文献和工具输出时务必分清，L2 的全局缺失率只有 0.04 × 0.25 = 1%。

**AMAT 的局限**：公式假设每次访存串行、独占地使用下一级，忽略了带宽竞争、排队、命中路径与缺失路径的重叠以及预取带来的提前搬运。它适合做第一数量级判断；更精细的分析（如排队模型、IPC 级模拟）见第 5 章与第 16 章。

## 1.5 缺失的三种类型：3C 分类法

同样是"缺失"，成因可能完全不同，而**成因决定对策**。经典的 3C 分类把缺失分为三类：

1. **强制缺失（compulsory miss，又称冷缺失 cold miss）**：块第一次被访问时的缺失。哪怕 cache 无限大，第一次也必须从下级搬上来——"强制"。
   - *类比*：新学期第一节课要用这本书，你总得第一次去图书馆把它借回来。
2. **容量缺失（capacity miss）**：cache 装不下工作集，块被逐出后又被访问。
   - *类比*：书桌太小，做完数学把数学书放回书架；晚上复习数学又得去取。书桌（容量）是瓶颈。
3. **冲突缺失（conflict miss）**：多个块被映射到同一位置互相踢逐出，尽管 cache 其他地方还空着。
   - *类比*：一个"固定车位"停车场——你的车位被占，你就得把自己的车挪走，哪怕整个停车场 90% 是空的。这就是第 3 章"相联度"要解决的问题。

**如何判定（理想实验法）**，对同一访问序列分别问三个问题：

1. 换成**无限大**的 cache 还缺吗？还缺 → 强制缺失；
2. 无限大不缺、换成**同容量全相联**的 cache 还缺吗？还缺 → 容量缺失；
3. 全相联不缺、实际结构才缺 → 冲突缺失。

**一个对比例子**：cache 容量 2 块。访问块地址序列 `0, 2, 0, 2, 0, 2`：

- 若为**直接映射**（块号 mod 2 决定位置，0 和 2 都映射到位置 0）：0 缺失（装入）→ 2 缺失（踢掉 0）→ 0 缺失（踢掉 2）→……**六次访问六次缺失**，这种现象叫**抖动（thrashing）**。
- 若为**全相联**（两个位置都能放）：0 缺失、2 缺失，之后四次全部命中。六次访问只有两次缺失。
- 差出来的 4 次缺失，就是纯粹的**冲突缺失**——容量明明够，输在了"安排座位"的方式上。

**3C 与设计对策的对应关系**（这张表是通往后续各章的地图）：

| 缺失类型 | 根因 | 对症下药 | 详见 |
|---|---|---|---|
| 强制缺失 | 第一次访问 | 更大的块、预取（硬件/软件） | 第 3 章、第 5 章 |
| 容量缺失 | 工作集 > 容量 | 增大容量、优化替换策略、软件 tiling | 第 4 章、第 7 章 |
| 冲突缺失 | 映射冲突 | 提高相联度、victim cache、地址变换 | 第 3 章、第 5 章 |

（多核系统中还有第四类"一致性缺失"，留到第 6 章再谈。）

## 1.6 案例研究：一台真实处理器的存储层次

以 Intel Skylake（第 6/7 代酷睿的微架构，参数公开且经典）的单核为例：

| 层级 | 容量 | 相联度 | 块大小 | 典型命中延迟 | 备注 |
|---|---|---|---|---|---|
| L1 数据 cache | 32 KB | 8 路 | 64 B | 4 周期 | 32 KB ÷ 64 B = 512 行 |
| L1 指令 cache | 32 KB | — | 64 B | ~4 周期 | 另有 µop cache 兜底 |
| L2（每核私有） | 256 KB | 4 路 | 64 B | ~12 周期 | |
| L3（多核共享） | 2 MB/核 | — | 64 B | ~44–70 周期 | 从 Haswell 的"包含式"改为"非包含式"，第 5.2 节详述原因 |
| 主存 DDR4-2666 | 8～64 GB | — | — | ~70–90 ns | 约 210–270 周期 |

读这张表时请带着本章的工具：

- **容量上有多小**：L1 数据 cache 32 KB，只相当于一张 512×512 RGB uint8 图片（0.75 MB）的 1/24；一张 1 兆像素的 RGB 图片（3 MB）已经装不进 L2。直觉上就能理解为什么 AI 负载必须依赖 tiling（第 10 章）。
- **延迟上有多陡**：把延迟换算到人类尺度（设 1 个 CPU 周期 = 1 秒）：

| 访问对象 | 实际延迟（3 GHz） | 人类尺度（周期=1 秒） | 体感 |
|---|---|---|---|
| L1 命中 | ≈1.3 ns | 4 秒 | 从书桌上拿一本书 |
| L2 命中 | ≈4 ns | 12 秒 | 转身从书架取 |
| L3 命中 | ≈15 ns | 45 秒 | 走到隔壁房间取 |
| DRAM | ≈90 ns | 4.5 分钟 | 下楼取快递 |
| SSD | ≈100 µs | 约 3～4 天 | 网购一本调不到的书 |
| 机械硬盘 | ≈10 ms | 接近 1 年 | 馆际调档，遥遥无期 |

这张"人类尺度表"值得背下来——它让你在写每一行代码、做每一个设计决策时，对"访问哪一级存储"有肌肉记忆般的直觉。

## 常见误区与注意事项

1. **"命中率 95% 已经很高了"**——错。例 1 显示 5% 的缺失率让 AMAT 变成命中时间的 5.5 倍。缺失率必须结合缺失代价一起评估。
2. **只盯缺失率、不看命中时间**——把 cache 加大能降缺失率，但更大的阵列通常意味着更长的命中时间（走更长的字线/位线）；AMAT 是两项之和，两边都要算。
3. **认为局部性是必然**——链表、哈希、随机访问下局部性几乎为零，cache 帮不上忙；分析负载时先问"局部性有多少"。
4. **混淆缺失率与缺失代价**——一个是频率，一个是时长。DRAM 频率翻倍（DDR4→DDR5）不等于延迟减半，接口带宽涨得快、随机访问延迟（数十 ns）几乎原地踏步，这是存储墙至今没有消失的原因。
5. **混淆"块"与"字"**——cache 与主存之间搬运的基本单位是块（如 64 B），不是单个字；程序一次只读 4 B，硬件却搬来 64 B，赌的就是空间局部性。
6. **忽略指令访存**——取指同样走存储层次，循环体反复执行靠的是 L1 指令 cache 命中；icache 缺失会直接卡住流水线前端。
7. **把 AMAT 当万能指标**——它忽略带宽、排队与重叠，多核并发场景下会明显失真；作为第一近似很好，作为最终结论危险。

## 本章小结

- **存储墙**：CPU 周期约 0.2～0.33 ns，DRAM 访问约 80～100 ns，差距 2～3 个数量级且曾长期扩大；"又快又大又便宜"的单级内存在物理上不可能。
- **局部性原理**（时间 + 空间）是存储层次有效的统计学基础；指针追逐等模式会破坏它。
- **存储层次** = 多级"越上越快越小越贵"的金字塔，每级是下级子集，层间以块为单位搬运数据。
- **AMAT = 命中时间 + 缺失率 × 缺失代价**；AMAT 对缺失率高度敏感非线性，这是 cache 设计一切取舍的定量出发点。
- **3C 分类**：强制缺失→预取/大块；容量缺失→扩容/替换策略；冲突缺失→相联度。缺失分类是选择设计对策的诊断流程。
- CPU 走**硬件管理**（cache，第 3～7 章），NPU 常走**软件管理**（scratchpad，第 8～12 章）——同一矛盾，两种回答。

## 思考题与练习

**1.（概念题）** 判断下列场景各利用了哪种局部性，或破坏了局部性：(a) 循环体内反复累加的变量；(b) 顺序解码视频的每一帧；(c) 对有序数组的二分查找；(d) 沿链表逐节点遍历。
*（要点：(a) 时间；(b) 空间为主、缓冲区复用带来时间；(c) 访问位置跨度大且随范围缩小跳跃，空间局部性弱；(d) 两者都弱，典型指针追逐。）*

**2.（概念题）** 解释为什么"又快又大又便宜"的单级内存在物理上不可行，以及存储层次如何借助局部性原理逼近这一不可能的目标（同时逼近最快级的速度与最便宜级的每比特成本）。

**3.（计算题）** 某 L1 cache 命中时间 4 周期，缺失率 3%，缺失代价 200 周期，主频 2 GHz。求：(a) AMAT（周期数）；(b) AMAT（纳秒）；(c) 若要把 AMAT 压到 6 周期以内，缺失率必须降到多少？
*（参考答案：(a) 4 + 0.03×200 = 10 周期；(b) 10 × 0.5 ns = 5 ns；(c) 4 + m×200 ≤ 6 ⟹ m ≤ 1%。）*

**4.（分析题）** 容量 2 块、全相联、LRU 替换的 cache，访问块地址序列为 `1, 2, 3, 1, 2, 3`。请列出每次访问是命中还是缺失，并把每次缺失归入 3C 中的某一类；再回答：若容量改为 3 块，哪些缺失会消失，为什么？
*（参考答案：前三次均为强制缺失（装入 3 时逐出 1）；后三次均为容量缺失，且序列整体抖动。容量改为 3 后，后三次全部命中，只剩三次强制缺失——因为工作集恰好是 3 块。）*

**5.（开放设计题）** 某程序要遍历一个 8 GB 的链表，节点在堆上随机分布，机器 L3 为 32 MB。请估计缺失率的量级，说明为什么继续加大 cache 帮助有限，并给出至少两种可行的改进思路（可从数据结构、软件预取、专用硬件等角度考虑）。

## 拓展阅读

1. John L. Hennessy, David A. Patterson, *Computer Architecture: A Quantitative Approach*, 6th ed., 第 2 章 "Memory Hierarchy Design"——本章 AMAT 公式、3C 分类、性能差距数据的权威出处，也是全书第 3～7 章的母本。
2. Randal E. Bryant, David R. O'Hallaron, *深入理解计算机系统*（CSAPP），第 6 章"存储器层次结构"——从程序员视角讲局部性与层次，程序侧案例（如矩阵转置的 blocking 实验）极佳。
3. Ulrich Drepper, *What Every Programmer Should Know About Memory*, 2007（免费长文）——从 DRAM 电容结构一路讲到缓存优化，工程细节丰富。
4. Jeff Dean, "Latency Numbers Every Programmer Should Know"——本章"人类尺度表"的思想源头，工程师必备的延迟数量级速记。

---
---

# 第 2 章 存储技术基础与术语

## 本章导读

第 1 章说明了"为什么要分层"，本章回答"用什么材料造每一层"。金字塔的每一级——寄存器、SRAM、DRAM——都不是抽象概念，而是由晶体管和电容搭成的物理电路，它们的物理实现直接决定了速度、密度、功耗与成本的数量级差异，也就决定了金字塔每层"该有多厚"。本章还要精读全书使用频率最高的一组术语（块、标记、索引、偏移、相联度等）——从第 3 章起，这些词每天都在用，含糊不得。最后，本章正式引出 cache 与 scratchpad 两种管理哲学的分野，为第三部分（NPU）埋下主线。

## 学习目标

读完本章，你应当能够：

1. 画出 SRAM 6T 单元与 DRAM 1T1C 单元的结构，并解释各自的读写原理与刷新需求；
2. 用具体数字对比寄存器、SRAM、DRAM 在速度、面积、功耗、成本上的数量级差异；
3. 准确定义块/行、标记、索引、偏移、相联度、命中延迟、缺失代价，并说明各自在硬件中的角色；
4. 说清 cache（硬件管理）与 SPM（软件管理）的本质区别，以及各自适合什么负载；
5. 用"容量 × 延迟 × 带宽 × 能耗 × 面积"五维权衡框架分析任一存储设计问题。

## 核心概念

| 术语（中文） | 英文 | 一句话定义 |
|---|---|---|
| 寄存器文件 | register file | 触发器阵列，多端口、极快、极小，流水线内使用 |
| 静态随机存储器 | SRAM (Static RAM) | 6 晶体管交叉耦合锁存，不需刷新的片上存储 |
| 动态随机存储器 | DRAM (Dynamic RAM) | 1 晶体管 + 1 电容，需周期刷新的高密度片外存储 |
| 6T 单元 | 6T cell | SRAM 基本单元：两个反相器 + 两个访问管 |
| 1T1C 单元 | 1T1C cell | DRAM 基本单元：一个访问晶体管 + 一个存储电容 |
| 刷新 | refresh | 周期性重写 DRAM 以对抗电容漏电 |
| 行缓冲 | row buffer | DRAM 芯片内一整行的缓存，命中可大幅加速 |
| 片上/片外存储 | on-chip / off-chip memory | 在 die 内（SRAM）还是在封装外（DIMM 上的 DRAM） |
| 暂存存储器 | Scratchpad Memory (SPM) | 地址空间中由软件显式管理的片上 SRAM |
| 标记 | tag | 地址高位，存于行内用于身份比对 |
| 索引 | index | 地址中段，选择 cache 内的组 |
| 块内偏移 | block offset | 地址低位，块内选字节 |
| 相联度 | associativity | 一个组内的槽位（路）数 |
| 有效位 / 脏位 | valid bit / dirty bit | 行内容是否有效 / 是否被改写未回存 |
| 性能功耗面积 | PPA (Performance, Power, Area) | 芯片设计的三大目标与权衡坐标 |

## 2.1 位的世界：寄存器、SRAM、DRAM 的存储原理

**寄存器文件**：每个位是一个触发器——两个反相器首尾相接的锁存环，配时钟控制的写入口。它的特点是为速度不惜代价：单 bit 面积可达 SRAM 的 5～10 倍，但支持真正的多端口（一个周期内 2 读 1 写甚至更多），能在流水线的半个周期内完成读写，且读取不破坏内容。典型规模：32 个 64 位通用寄存器，加上重命名后物理寄存器堆可达数百个条目——总量仍只有几 KB。

**SRAM 6T 单元**：把触发器"瘦身"。结构描述（请脑内作图）：两个反相器交叉耦合构成锁存核心（4 管），左右各引出一个访问晶体管（共 6 管，故称 6T）；两个访问管的栅极接同一根**字线（word line）**，源漏分别接**位线对（bit line / bit-line-bar）**。平时字线低电平，单元安静地锁住 0 或 1。读：先把两条位线都预充到高电平，再拉高字线——存储 0 的那侧会把位线拉低，两侧产生微弱压差，**灵敏放大器（sense amplifier）**检测并放大这个压差得到输出。写：把位线对强行驱动成目标值，强攻进锁存环。整个读出过程不破坏存储内容，**不需要刷新**。

**DRAM 1T1C 单元**：极致精简——一个晶体管当开关，一个电容存电荷，电容有电=1、没电=0。结构描述：字线控制晶体管通断，晶体管另一端接位线，电容接在晶体管与地之间。读时位线预充到中间电平，接通晶体管后电容与位线发生电荷分享，产生几十毫伏的微弱摆动，靠灵敏放大器判断原值——注意这个读出是**破坏性的**（电荷被分享掉了），所以读完必须立刻回写。更要命的是电容会漏电，典型情况下几十毫秒内电荷就衰减到不可分辨，因此 DRAM 控制器必须**周期性刷新**（JEDEC 标准要求 64 ms 内把所有行读出并回写一遍），刷新期间的读请求只能排队。

**直观类比**：SRAM 像白板上用磁扣贴的便签——抬手就能看，一块板就那么大，贵在快；DRAM 像标准化的仓库货架——便宜、容量巨大，但取货要走完整套流程（先找到排（行激活），再从排里取托盘（列访问），取完还得归位）。

## 2.2 SRAM 与 DRAM 全方位对比

同工艺节点下的数量级对比（以 28 nm 为量级参考）：

| 维度 | SRAM（6T） | DRAM（1T1C） | 寄存器（触发器） |
|---|---|---|---|
| 单元面积 | ~0.1–0.2 μm²（~100+ F²） | ~0.02–0.04 μm²（制造在专用工艺上） | ~0.5+ μm² |
| 访问延迟 | 片上 0.5–5 ns（取决于容量与组织） | 50–100 ns（含行激活） | <0.3 ns（1 周期内） |
| 密度基准 | 1× | ~8–16×（且 DRAM 用独立优化工艺） | ~0.1× |
| 待机功耗 | 有泄漏，但无刷新 | 必须持续刷新，待机也耗电 | 泄漏最大 |
| 是否破坏性读 | 否 | 是（读后回写） | 否 |
| 典型容量 | KB ~ 几十 MB（片上） | GB 级（片外 DIMM） | <10 KB |
| 每位成本 | 高（片上先进工艺） | 低 1～2 个数量级 | 最高 |

三点解读：

1. **SRAM 与 DRAM 的差距不是"工艺精益求精"的差距，而是电路结构的差距**：6T 锁存天然稳定快速，1T1C 天然致密但迟缓。这就是金字塔"上层用 SRAM、下层用 DRAM"的物理根据。
2. **DRAM 其实是一个三层系统**：芯片内部的行缓冲（row buffer，通常几 KB 一行）→ DRAM bank → DIMM。同一行内的连续访问命中行缓冲，可以快一倍以上——这是第 5 章"块大小"与第 15 章"访存调度"要利用的特性。
3. **嵌入式 DRAM（eDRAM）**：把 1T1C 做进逻辑工艺，密度介于 SRAM 与外置 DRAM 之间。IBM 在大型机 L4、Intel 在 Haswell 的 Crystal Well（128 MB eDRAM 当 L4）上用过——说明"金字塔每层用什么材料"本身就是可权衡的设计变量。
4. **HBM（High Bandwidth Memory，高带宽内存）**：把多颗 DRAM die 用硅通孔（TSV）垂直堆叠，通过超宽接口（1024 位级/堆栈）经硅中介层贴近处理器封装——本质上仍是 DRAM，但带宽比 DIMM 高一个数量级、位能耗更低，是 GPU 与 AI 加速器的标配外存。它提醒我们：金字塔的"片外一层"内部也在分层（第 14 章 CXL 内存池会把这个故事讲得更远）。

## 2.3 两种管理哲学：Cache 与 Scratchpad Memory

同样一块片上 SRAM，有两种截然不同的用法：

- **Cache（硬件管理）**：SRAM 内容由硬件全权决定。CPU 照常发出 load/store，硬件自动判断数据在不在、不在就搬进来、满了就按替换算法挤一个出去。**对软件完全透明**——同一段程序在有 cache 和没 cache 的机器上都能跑，只是快慢不同。代价：每行都要存 tag 并做比较（判断"是不是它"），要维护替换状态（LRU 位），访存时间不确定（命中 1 ns、缺失 100 ns），而 tag 阵列读写与比较会消耗可观的动态功耗（经验上占 cache 总动态功耗的 20%～30%）。
- **SPM（软件管理）**：这片 SRAM 直接出现在处理器的**物理地址空间**里（例如 0x40000000～0x400FFFFF），软件用普通 load/store 显式读写。放什么、什么时候放、什么时候换，全部由程序员或编译器用 DMA 指令安排。**没有 tag、没有缺失、没有"赌局部性"**——延迟固定且编译期可知（典型 1 至数个周期，随容量增大），也没有 tag 开销。代价：所有数据搬运要显式编程，写错了程序就错，且通用代码（操作系统、不规则负载）很难自动管理。

**类比**：cache 像一个全自动秘书——你只管说"我要某某文件"，她大概率已经帮你放在手边，但你不知道也不可控，偶尔要等她去档案室；SPM 像一个自己上锁的抽屉——每样东西放在哪、什么时候拿进拿出，全按你的清单执行，绝不意外，但清单得你自己写。

两者没有绝对优劣，只有负载适配：访问模式不可预测、代码要通用的场景选 cache（CPU 的宿命）；访问模式在编译期完全可知、对能耗和实时性敏感的场景选 SPM（NPU 的机会，第 8 章展开）。

## 2.4 关键术语精读

这些术语在第 3 章会被反复使用，此处一次讲透：

- **块 / 行（block / line）**：主存与 cache 之间数据搬运的基本单位（典型 64 B）。"块"指主存侧的一段数据，"行"指 cache 里容纳一个块的槽位及其管理信息。访问一个字节，硬件也要搬一整块——赌的是空间局部性。
- **标记（tag）**：地址高位部分。数据搬进行里时，把 tag 一起存起来；之后查找时用地址的 tag 部分与行内 tag 比对，回答"这个槽位装的是不是我想要的块"。
- **索引（index）**：地址中段，用来在 cache 内"选组"——决定这次访问去哪一片区域找。
- **块内偏移（offset）**：地址低位，块内选字节。64 B 的块用 6 位。
- **相联度（associativity）**：一个组里有多少个互相等价的槽位（"路"）。停车场类比：组=停车分区，路=分区内可停的车位数。车位越多，越不容易"分区满而车场空"。
- **命中延迟（hit latency）**：从地址发出到数据可用的总时间，包含译码、阵列访问、tag 比较与选择。
- **缺失代价（miss penalty）**：缺失后从下一级搬运的额外时间（含总线、下级延迟、回填）。
- **有效位 / 脏位（valid / dirty）**：行管理位。上电后内容随机，valid=0 表示"别信这行"；dirty=1 表示"这行被改过、逐出前必须写回"（第 4 章）。

## 2.5 存储设计的基本权衡：五维坐标

任何存储设计问题都可以放进同一个坐标系：**容量、延迟、带宽、能耗、面积**。它们两两牵制：

- 容量↑ → 延迟↑（字线/位线变长）、面积与泄漏功耗↑；
- 延迟↓ → 限制容量与相联度、限制端口数；
- 带宽↑ → 更多 bank、更多端口、更宽的总线——全是面积与功耗；
- 能耗↓ → 降活动性（少翻动）、降压、关闲置块——往往牺牲性能。

**没有免费的午餐：任何一维的改善都在另外几维收费。** 一个合格的设计文档，每个参数决策都应该写清"买了什么、花了什么"。第 7 章将把这个框架落到 CACTI 的具体数字上，第 16～18 章将用它完成真实设计。

## 2.6 速览：ROB、Store Buffer 与 TLB——流水线里的另外三块存储

金字塔之外，处理器核内还有三块常被忽视、但本书后续会用到的片上存储，此处一次登记：

- **重排序缓冲（ROB，Reorder Buffer）**：乱序核的"在途指令账本"，几十到几百项，按程序顺序记录每条已发射、未提交的指令。它让流水线可以大胆乱序执行、又能按序提交（精确异常的前提）。ROB 容量就是乱序窗口的上限——窗口越大越能掩盖缺失延迟，但每项要携带目的寄存器号、状态位等几十位信息，是核内单位面积最贵的存储之一。
- **存储缓冲（Store Buffer）**：已提交 store 的"出站队列"——store 从 ROB 退休后先进 store buffer，再择机写入 cache。它吸收"提交要快、写 cache 可以慢"的速度差，并引出 load 必须先查 store buffer 的转发逻辑（store-to-load forwarding）。与第 4.6 节的分工：store buffer 属核内流水线，write buffer 属存储层次，两者前后接力。
- **地址翻译后备缓冲（TLB，Translation Lookaside Buffer）**：虚拟地址→物理地址翻译的 cache，典型两级（L1 TLB 几十项、全相联；L2 TLB 数百到上千项、组相联——回想第 3 章"小容量才敢全相联"）。TLB 缺失触发页表遍历（page walk），代价几十到几百周期，因此页表层级本身还有专用的页漫步缓存（page walk cache）。TLB 是"虚拟内存"抽象在存储层次里的投影：第 7 章 VIPT 的页内偏移约束、第 13 章 SMMU 的设备侧翻译，都是它的续集。

三者的共同点：**都是用昂贵的快存储，为更大的正确性或灵活性目标服务**——与 cache 用快存储"赌局部性"是同一哲学的不同下注方向。

## 案例/示例：Crystal Well 的 eDRAM L4

Intel Haswell 的 Crystal Well 版本（如 i7-4770R）在封装内额外堆了 128 MB eDRAM，作为 CPU 与集显共享的 L4 cache。选材逻辑值得玩味：容量要求大（128 MB 用 SRAM 在 22 nm 上要几十 mm²，成本不可接受），又要求带宽高（>50 GB/s，外置 DRAM 做不到），于是选择逻辑工艺内的 eDRAM 折中——密度约为 SRAM 的 3～4 倍、延迟 30～50 周期，介于 L3 与 DRAM 之间。这说明"金字塔每层用什么器件"是开放的设计题，不是教条。

## 常见误区与注意事项

1. **"SRAM 快是因为它小"——因果颠倒**。SRAM 快是因为结构简单（锁存 + 差分读）且连线短；小只是密度低的副产品。128 MB 的 SRAM 阵列照样比 64 KB 的慢。
2. **"DRAM 断电丢数据，SRAM 不丢"**——两者都易失（volatile），断电都丢；非易失的是闪存/磁盘。
3. **混淆"行"与"块"**：块在主存侧，行在 cache 侧；"这行的 tag"不能说成"这块的 tag"。
4. **把 SPM 当"简化的 cache"**：SPM 不是删掉 tag 的 cache，而是地址空间的一部分，管理责任从硬件完全转移到软件——编程模型根本不同。
5. **忽略刷新开销**：高带宽读写下，DRAM 每隔约 7.8 µs（tREFI）就要插入一次刷新事务，峰值带宽会打折；算带宽预算时要留余量。
6. **用"访问延迟"概括 DRAM 性能**：DRAM 的行缓冲命中与否、bank 并行度、读写转向（tWTR）都会大幅改变有效延迟与带宽。

## 本章小结

- 寄存器（触发器，多端口，<1 周期）、SRAM（6T 锁存，不需刷新，0.5–5 ns）、DRAM（1T1C，需 64 ms 刷新，破坏性读，50–100 ns）构成金字塔的三种主要"建筑材料"。
- SRAM/DRAM 的速度密度差是**电路结构**决定的：锁存 vs 电容。
- DRAM 内部还有行缓冲一级，连续访问可显著加速——后续章节优化的重要抓手。
- Cache（硬件管理、透明、不确定延迟、tag 开销）与 SPM（软件管理、显式 DMA、确定延迟、零 tag）是同一块 SRAM 的两种哲学——CPU 与 NPU 分道扬镳的起点。
- 五维权衡（容量/延迟/带宽/能耗/面积）是全书分析一切存储设计的统一坐标系。

## 思考题与练习

**1.（概念题）** 解释 DRAM 为什么必须刷新而 SRAM 不需要；并说明"破坏性读出"给 DRAM 的一次读访问额外增加了什么步骤。
*（要点：电容漏电 vs 双稳态锁存；读后需灵敏放大并回写原值。）*

**2.（计算题）** 某 28 nm 工艺 6T SRAM 单元面积约 0.15 μm²，估算 512 KB（含 4 Mbit）SRAM 的裸阵列面积，按外围电路（译码、灵敏放大、tag）再乘 1.4 的系数；若同样容量用密度高 10 倍的 eDRAM 实现，面积是多少？
*（参考：4 Mbit × 0.15 = 0.6 mm² → ×1.4 ≈ 0.86 mm²；eDRAM ≈ 0.086 mm² 阵列级。）*

**3.（概念题）** 用一句话分别说清 tag、index、offset 在一次 cache 查找中的分工，并回答：为什么 offset 位在查找的最早阶段就可用，而 tag 比对结果要到最后才出现？
*（要点：index 选组、offset 选块内字节、tag 判身份；offset/index 来自地址本身无需等待，tag 需阵列读出后比较。）*

**4.（设计题）** 一个嵌入式实时系统（硬截止时间任务）要选片上数据存储方案：cache 还是 SPM？从延迟确定性、能耗、软件成本三方面论证，并说明什么情况下你会改变选择。

**5.（分析题）** 为什么多端口寄存器文件不能简单地做得很大（比如 1 MB）？从面积（端口数 × 单元面积的增长）与时序（端口位线负载）两方面分析。

## 拓展阅读

1. Hennessy & Patterson, *Computer Architecture: A Quantitative Approach*, 6th ed., 附录 B "Review of Memory Hierarchy"——本章器件与术语的教科书级出处。
2. Ulrich Drepper, *What Every Programmer Should Know About Memory*——第 2、3 节对 DRAM 单元、行缓冲、刷新时序的工程级讲解，无可替代。
3. Weste & Harris, *CMOS VLSI Design*, 4th ed., 存储器章节——6T 单元读写的电路细节（位线预充、灵敏放大、写驱动）。

---
---

# 第 3 章 Cache 基本结构与地址映射

## 本章导读

从本章起进入第二部分的正文。任何一次 cache 访问，硬件都要在几个纳秒内回答三个问题：**去哪里找（映射）、是不是它（命中判断）、装不下怎么办（替换与写策略）**。本章回答前两个问题——地址如何切分、直接映射/全相联/组相联三种映射方式的硬件结构与优劣，并给出 tag/index/offset 位数计算这一"必考动作"的完整套路。第 4 章接着回答第三个问题。学完本章，你看到任何一颗 CPU 的 cache 参数（容量/相联度/块大小），都应能反推出它的内部结构。

## 学习目标

1. 按顺序描述一次组相联 cache 查找的完整流程（地址切分 → 选组 → 并行比对 → 选路 → 偏移选数据）；
2. 给定容量、块大小、相联度与地址位数，计算 index/tag/offset 位数及 tag 阵列总位数；
3. 画出（用文字精确描述）直接映射与组相联 cache 的阵列结构；
4. 论证三种映射方式在冲突率、命中延迟、硬件复杂度上的取舍，解释为什么主流 L1 是 4～8 路组相联；
5. 解读真实 CPU 公开的 cache 参数表并反推其结构。

## 核心概念

| 术语（中文） | 英文 | 一句话定义 |
|---|---|---|
| 直接映射 | direct-mapped | 每个块只能进唯一一个行（index 唯一决定位置） |
| 全相联 | fully associative | 块可进任意行，所有 tag 并行比较 |
| 组相联 | set-associative | 折中：先按 index 选组，组内任意行 |
| 组 | set | cache 内被同一 index 选中的一组行 |
| 路 | way | 组内等价的槽位，相联度=路数 |
| 标记阵列 / 数据阵列 | tag array / data array | 存行管理信息（tag+valid+dirty）/ 存块内容 |
| 侦听比较 | tag comparison | 组内各路 tag 与地址 tag 的并行比对 |
| 内容寻址存储器 | CAM | 按内容并行匹配的存储器，全相联的硬件基础 |
| 地址划分 | address partition | 地址切成 tag / index / offset 三段 |
| 虚拟索引物理标记 | VIPT | 用虚拟地址低位做 index、物理地址 tag 的折中方案 |

## 3.1 一次查找的完整旅程

**图示化描述（请脑内搭建这台机器）**：一块 32 KB、4 路、64 B 块的 cache 内部并排放着两个阵列——**tag 阵列**（每行存 19 位 tag + 1 位 valid + 1 位 dirty）与**数据阵列**（每行存 64 B）。阵列被组织成 128 组 × 4 路：想象一个 128 排、每排 4 个格子的货架，每格放一个 64 B 的"抽屉"，抽屉门上贴着标签（tag）。

**图 3-1　组相联 cache：地址划分与查找结构**

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 680 400" width="100%" role="img">
  <title>组相联 cache 的地址划分与查找结构</title>
  <defs>
    <marker id="arr31" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
      <path d="M2 1L8 5L2 9" fill="none" stroke="#444441" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
    </marker>
  </defs>
  <text x="340" y="24" text-anchor="middle" font-size="14" font-weight="500" fill="#333333">组相联 cache：地址划分与查找结构（32 KB / 4 路 / 64 B，32 位地址）</text>
  <g font-size="11" fill="#888888">
    <text x="62" y="46">31</text><text x="386" y="46" text-anchor="end">13</text>
    <text x="394" y="46">12</text><text x="516" y="46" text-anchor="end">6</text>
    <text x="524" y="46">5</text><text x="616" y="46" text-anchor="end">0</text>
  </g>
  <rect x="60" y="52" width="330" height="30" fill="#B5D4F4" stroke="#185FA5" stroke-width="0.5"/>
  <text x="225" y="71" text-anchor="middle" font-size="12" fill="#0C447C">Tag（19 位）</text>
  <rect x="390" y="52" width="130" height="30" fill="#FAC775" stroke="#854F0B" stroke-width="0.5"/>
  <text x="455" y="71" text-anchor="middle" font-size="12" fill="#412402">Index（7 位）</text>
  <rect x="520" y="52" width="100" height="30" fill="#C0DD97" stroke="#3B6D11" stroke-width="0.5"/>
  <text x="570" y="71" text-anchor="middle" font-size="12" fill="#173404">Offset（6 位）</text>
  <line x1="225" y1="82" x2="225" y2="112" stroke="#444441" stroke-width="1.5" marker-end="url(#arr31)"/>
  <text x="225" y="128" text-anchor="middle" font-size="11" fill="#777777">并行比对</text>
  <line x1="455" y1="82" x2="455" y2="112" stroke="#444441" stroke-width="1.5" marker-end="url(#arr31)"/>
  <text x="455" y="128" text-anchor="middle" font-size="11" fill="#777777">选组（128 选 1）</text>
  <line x1="570" y1="82" x2="570" y2="112" stroke="#444441" stroke-width="1.5" marker-end="url(#arr31)"/>
  <text x="570" y="128" text-anchor="middle" font-size="11" fill="#777777">块内选字节</text>
  <rect x="60" y="146" width="450" height="224" rx="12" fill="#E6F1FB" stroke="#185FA5" stroke-width="0.5"/>
  <text x="285" y="168" text-anchor="middle" font-size="13" font-weight="500" fill="#0C447C">Cache：128 组 × 4 路（每行 64 B + tag / V / D）</text>
  <text x="76" y="196" font-size="11" fill="#999999">组 41 …</text>
  <text x="76" y="316" font-size="11" fill="#999999">组 43 …</text>
  <text x="76" y="336" font-size="11" fill="#999999">⋮（共 128 组）</text>
  <text x="76" y="240" font-size="12" font-weight="500" fill="#0C447C">组 42</text>
  <g>
    <rect x="118" y="208" width="88" height="58" fill="#FFFFFF" stroke="#5F5E5A" stroke-width="0.5"/>
    <rect x="118" y="208" width="88" height="20" fill="#B5D4F4" stroke="#5F5E5A" stroke-width="0.5"/>
    <text x="162" y="222" text-anchor="middle" font-size="11" fill="#0C447C">tag V D</text>
    <text x="162" y="248" text-anchor="middle" font-size="11" fill="#444441">64 B 数据</text>
    <rect x="214" y="208" width="88" height="58" fill="#FFFFFF" stroke="#5F5E5A" stroke-width="0.5"/>
    <rect x="214" y="208" width="88" height="20" fill="#B5D4F4" stroke="#5F5E5A" stroke-width="0.5"/>
    <text x="258" y="222" text-anchor="middle" font-size="11" fill="#0C447C">tag V D</text>
    <text x="258" y="248" text-anchor="middle" font-size="11" fill="#444441">64 B 数据</text>
    <rect x="310" y="208" width="88" height="58" fill="#FFFFFF" stroke="#5F5E5A" stroke-width="0.5"/>
    <rect x="310" y="208" width="88" height="20" fill="#B5D4F4" stroke="#5F5E5A" stroke-width="0.5"/>
    <text x="354" y="222" text-anchor="middle" font-size="11" fill="#0C447C">tag V D</text>
    <text x="354" y="248" text-anchor="middle" font-size="11" fill="#444441">64 B 数据</text>
    <rect x="406" y="208" width="88" height="58" fill="#FFFFFF" stroke="#5F5E5A" stroke-width="0.5"/>
    <rect x="406" y="208" width="88" height="20" fill="#B5D4F4" stroke="#5F5E5A" stroke-width="0.5"/>
    <text x="450" y="222" text-anchor="middle" font-size="11" fill="#0C447C">tag V D</text>
    <text x="450" y="248" text-anchor="middle" font-size="11" fill="#444441">64 B 数据</text>
  </g>
  <text x="118" y="286" font-size="11" fill="#888888">路 0 ｜ 路 1 ｜ 路 2 ｜ 路 3（组内并行比较）</text>
  <line x1="510" y1="237" x2="540" y2="208" stroke="#444441" stroke-width="1.5" marker-end="url(#arr31)"/>
  <rect x="544" y="186" width="96" height="40" rx="6" fill="#F5C4B3" stroke="#993C1D" stroke-width="0.5"/>
  <text x="592" y="210" text-anchor="middle" font-size="11" fill="#712B13">并行比较 4 路 tag</text>
  <line x1="592" y1="226" x2="592" y2="252" stroke="#444441" stroke-width="1.5" marker-end="url(#arr31)"/>
  <rect x="544" y="256" width="96" height="40" rx="6" fill="#C0DD97" stroke="#3B6D11" stroke-width="0.5"/>
  <text x="592" y="280" text-anchor="middle" font-size="11" fill="#173404">4:1 路选择</text>
  <text x="592" y="316" text-anchor="middle" font-size="11" fill="#444441">命中路的 64 B</text>
  <text x="592" y="332" text-anchor="middle" font-size="11" fill="#444441">→ offset 选字节</text>
</svg>

一次 load 0x0000A1B2 的旅程：

1. **切分地址**：把 32 位地址砍成三段——高 19 位 tag、中 7 位 index、低 6 位 offset（位数怎么来的，见 3.5 节）；
2. **选组**：7 位 index 译码，选中 128 组中的一组（货架的某一排）；
3. **并行比对**：该组 4 个抽屉门上的 tag 同时与地址 tag 比对（4 个并行比较器），valid=1 且 tag 相等的那一路"举手"；
4. **选路选数据**：举手的那路从数据阵列读出 64 B，再用 6 位 offset 从中挑出要的字节/字；
5. **无人举手**：缺失，向下一级发起请求（第 4～5 章的主角）。

注意 tag 阵列与数据阵列的读取是**并行**的（同一 index 同时驱动两个阵列），tag 比对与数据读出在时间上重叠——这是命中延迟优化的关键（第 7 章关键路径再细讲）。

## 3.2 直接映射：一个地址只有一个家

**结构描述**：只有 1 路。512 行排成一列，index 直接选中唯一的一行，只比一次 tag。像一栋只有一个房号的公寓——地址 (块号 mod 512) 是几号，就只能住几号房。

- **优点**：硬件最简（一个比较器）、命中延迟最短、功耗最低、不需要替换决策（要进来只能赶走唯一的住户）。
- **缺点**：冲突惨烈。两个交替访问的块若 index 相同，会互相反复踢逐出——第 1 章 `0,2,0,2` 的抖动就是直接映射的日常。实际程序里最阴险的场景：两个数组恰好按 cache 容量对齐，`a[i]` 与 `b[i]` 永远映射同一行（见本章案例）。

## 3.3 全相联：随处可住

**结构描述**：没有 index。任何块可放在任意一行，查找时**所有**行的 tag 必须同时与地址比对——这需要内容寻址存储器（CAM）或等价的并行比较网络。像一个不设房号的青旅：床位随便挑，但前台要同时问遍所有床位。

- **优点**：零冲突缺失（只要容量没满，谁都不用走），空间利用率最高。
- **缺点**：N 行就要 N 个并行比较器，延迟与功耗随容量线性恶化——只适合小容量。
- **典型用途**：TLB（几十项）、victim cache（8～16 项，第 5 章）、目录项查找。

## 3.4 组相联：折中的智慧

**结构描述**：直接映射与全相联之间连续取点。组数 = 行数/路数。停车场类比：把车场分成 128 个分区（组），每个分区 4 个车位（4 路）；你的车必须停在指定分区，但分区内车位随便挑。分区满了才需要踢车（替换，第 4 章），而"整场 90% 空、你的分区满"的概率随路数增加迅速下降。

**为什么主流 L1 是 4～8 路？** 经验数据（源自 Smith 1982 等经典研究，被无数次复现）：缺失率随相联度提升的收益集中在 1→2→4→8 路这一段，8 路之后与全相联的差异通常只剩 1～2 个百分点；而路数每翻倍，比较器数量、路选择多路器的宽度、替换状态位都翻倍，命中延迟开始吃紧——4 周期命中预算（第 1 章 Skylake 数据）容不下无限加宽。于是 8 路成为"收益/成本比"的甜点。

## 3.5 地址划分实战：位数计算

**三步套路**（务必形成条件反射）：

1. **offset 位数 = log₂(块大小)**；
2. **index 位数 = log₂(组数) = log₂(容量 ÷ 块大小 ÷ 相联度)**；
3. **tag 位数 = 地址位数 − index 位数 − offset 位数**。

**例 1（经典配置）**：32 KB、4 路、64 B 块、32 位物理地址：

```
行数   = 32 KB ÷ 64 B = 512 行
组数   = 512 ÷ 4      = 128 组
offset = log₂(64)     = 6 位
index  = log₂(128)    = 7 位
tag    = 32 − 7 − 6   = 19 位
```

**验证直觉**：512 行 × 64 B = 32 KB ✓；128 组 × 4 路 ✓。

**例 2（16 KB 直接映射、32 B 块、40 位物理地址）**：行数 512，组数 512；offset 5 位、index 9 位、tag 26 位。

**tag 阵列开销计算**：例 1 的每行 tag 需 19（tag）+1（valid）+1（dirty）= 21 位，共 512 行 → 512 × 21 bit ≈ **1.3 KB**——只为"知道里面装的是谁"，就要烧掉 4% 的容量，还附加比较功耗。这就是第 2 章说的"cache 的税"，也是 NPU 投奔 SPM 的动机之一。

**tag 的语义再强调一次**：块号（地址 ÷ 块大小）本身才是"身份证"，tag 是块号的高位部分——因为低位（index）已经隐含在"存放在哪个组"里，不必重复存储。同一组内不可能出现 index 相同而 tag 相同的两个行，这正是查找正确性的根基。

## 3.6 真实 CPU 的 cache 参数解读

| 处理器 | 层级 | 容量 | 相联度 | 块大小 | 反推结构 |
|---|---|---|---|---|---|
| Intel Skylake | L1D | 32 KB | 8 路 | 64 B | 512 行/64 组，offset 6 + index 6 + tag 20（按 32 位地址示意；Skylake 物理地址 46 位时 tag 为 34 位） |
| AMD Zen 4 | L1D | 32 KB | 8 路 | 64 B | 同上 |
| Apple Firestorm (M1) | L1D | 128 KB | 未披露（推测 8 路） | 128 B | 1024 行/128 组（8 路解读）——配 16 KB 页恰好满足 VIPT，见第 7 章 |
| Arm Cortex-A55 | L1D | 8–64 KB | 4 路 | 64 B | 小核省电取向：容量可配、结构从简 |
| Arm Cortex-M7 | L1D | 16 KB | 4 路 | 32 B | 嵌入式：1 周期命中的紧约束 |

读表方法：看到"容量 + 相联度 + 块大小"三件套，立刻心算行数、组数与三段位数；再结合目标市场（高性能/能效/实时）理解它为什么偏离主流——例如 Apple 的 128 KB L1 被普遍解读为"用大 L1 尽量截留工作集、省掉 L2 访问能耗"的激进选择；其组织细节官方未披露，"8 路 + 16 KB 页"的解读与 VIPT 约束恰好自洽（见第 7 章），其余网流传言需谨慎对待。

## 案例/示例：直接映射的"魔鬼对齐"冲突

```c
double a[8192], b[8192];   // 各 64 KB，编译器往往把它们按 64 KB 对齐
for (i = 0; i < 8192; i++)
    a[i] += b[i];
```

假设 L1 为 32 KB 直接映射、64 B 块：`a[i]` 与 `b[i]` 的地址差恰是 64 KB 的整数倍，index 完全相同——**每次访问 a 都把 b 刚装入的行踢出去，反之亦然，100% 冲突缺失**。三个修法：把 `b` 声明后强制错开对齐（加 padding）；改用 2 路以上组相联（硬件修法）；循环交错处理（`a[i], b[i], a[i+1], b[i+1]` 改变时序没用，得靠错位）。这个案例说明：**直接映射 cache 上，数据布局即性能**；而组相联度的存在，正是硬件对这种"程序员的坏运气"的保险。

## 常见误区与注意事项

1. **"组数 = 行数"**——只有直接映射才如此；组相联时组数 = 行数 ÷ 路数。
2. **以为 tag 比对是逐路串行的**——必须是并行（所有路同时比），串行比对延迟不可接受。
3. **忽略 valid 位**：上电后 tag 阵列内容是随机噪声，没有 valid=0 的初始化，冷启动会"命中"出脏数据。
4. **把虚拟地址当物理地址算位数**：tag 存的是物理地址（通常），虚拟地址要先经 TLB 翻译；L1 的 VIPT 组织让 index 取自虚拟地址低位——这带来"index+offset 不得超出页内偏移"的约束（第 7 章展开）。
5. **认为全相联"总是更准"**：它只是冲突更少；命中延迟和功耗的代价在稍大容量下就无法承受。
6. **混淆"块对齐"与"缓存行对齐"**：块就是缓存行大小，64 B 对齐的数据结构（如 `alignas(64)`）能避免跨行访问的两次填充。

## 本章小结

- 查找流程五步：切地址 → 选组 → 并行比对 → 选路 → 偏移选数据；tag 阵列与数据阵列并行读取。
- 三种映射是"唯一家 → 随处住 → 指定分区随便住"的谱系；直接映射快而脆弱，全相联准而昂贵，组相联（4～8 路）是工程甜点。
- 位数三步曲：offset=log₂(块)、index=log₂(组数)、tag=地址位数−index−offset；tag 阵列开销 ≈ 行数 × (tag+valid+dirty) 位。
- 真实参数表可反推结构；偏离主流的配置（Apple 128 KB L1）背后都有明确的设计宣言。
- 直接映射下数据布局决定冲突命运——这是软件能"白嫖"性能的地方。

## 思考题与练习

**1.（计算题）** L2 cache 为 1 MB、8 路、64 B 块，处理器物理地址 48 位。求行数、组数、三段位数、tag 阵列总开销（含 valid/dirty）。
*（参考：16384 行/2048 组；offset 6、index 11、tag 31；开销 16384×33 bit ≈ 66 KB。）*

**2.（计算题）** 把上题改为直接映射（同容量同块大小），三段位数如何变化？tag 总开销变大还是变小？
*（参考：index 14、tag 28；每行 tag 变小但行数不变——16384×30 bit ≈ 60 KB，略省。体会：相联度并不显著改变 tag 开销，改变的是比较器与多路器。）*

**3.（分析题）** 对访问序列（块号）：`3, 7, 11, 3, 7, 11`，容量 3 行。分别给出直接映射（组数=3）与全相联（LRU）下的命中/缺失序列，并计算各自缺失率。
*（参考：直接映射 3、7、11 同映射 0 组互相踢——6 缺 6；全相联 3 缺后全中——3 缺。冲突缺失的教科书案例。）*

**4.（概念题）** 为什么 TLB 几乎总是全相联或高度组相联，而 L1 数据 cache 极少用全相联？
*（要点：条目数悬殊（几十 vs 数百行）决定并行比较的代价；TLB 的容量缺失直接意味着页表遍历，代价比 cache 缺失高得多，值得用贵的方式保命中率。）*

**5.（设计题）** 你在设计一颗 4 GHz 核的 L1D，命中预算 4 周期。候选：32 KB/4 路、32 KB/8 路、64 KB/8 路（同 64 B 块）。从冲突缺失、命中延迟（路数与容量对延迟的影响）、VIPT 页约束（4 KB 页）三个维度论证你的选择。

## 拓展阅读

1. Hennessy & Patterson, *Computer Architecture: A Quantitative Approach*, 6th ed., §2.2–2.3——映射、划分与缺失分类的原始论述。
2. Alan J. Smith, "Cache Memories," *ACM Computing Surveys*, 1982——cache 研究的开山综述，相联度收益递减的经典数据来源。
3. Intel® 64 and IA-32 Architectures Optimization Reference Manual, 第 2 章——真实 CPU 的 cache 组织与对齐调优建议。

---
---

# 第 4 章 替换策略与写策略

## 本章导读

第 3 章解决了"去哪找、是不是它"，本章补上 cache 硬件的另外两块拼图：**新块进门时踢谁走（替换策略）**，以及**写数据时改哪一级、什么时候改（写策略）**。这两组决策看似细节，实则深刻影响性能（缺失率、写带宽）、正确性（一致性、脏数据回写）与功耗。读完本章你会发现：写路径远比读路径复杂——正因为如此，第 6 章多核一致性的大部分难题都出在"写"上。

## 学习目标

1. 解释 LRU 的理想目标（Bélády 最优）与 LRU 的差距来源，实现 LRU/树形 PLRU/随机替换所需的硬件状态位数；
2. 逐步描述写直达与写回的完整流程，计算各自的下级写流量；
3. 说明写分配/非写分配的语义，以及"WT+no-WA、WB+WA"成为主流组合的原因；
4. 解释写缓冲区的作用、合并机制与设计风险（读缺失撞缓冲区）；
5. 说清脏位的生命周期：置位、查询、清除。

## 核心概念

| 术语（中文） | 英文 | 一句话定义 |
|---|---|---|
| 替换策略 | replacement policy | 组满时选择牺牲者（victim）的规则 |
| 最优替换 | Bélády's OPT / MIN | 踢掉"最远的将来不再使用"者，不可实现的理论标尺 |
| 最近最少使用 | LRU (Least Recently Used) | 踢掉最久未被访问者，硬件用使用序状态位实现 |
| 伪 LRU | PLRU (tree-based) | 用二叉树近似 LRU，N 路只需 N−1 位 |
| 写直达 | write-through | 写同时更新本级行与下一级 |
| 写回 | write-back | 只写本级行，逐出时若脏才写回 |
| 写分配 | write-allocate | 写缺失时先把块调入再写 |
| 非写分配 | no-write-allocate | 写缺失直接写下级，不调入 |
| 写缓冲区 | write buffer | 暂存待写下级行的队列，解耦写延迟 |
| 写合并 | write merging | 缓冲区内同块多次部分写合并成一次下级写 |
| 脏位 | dirty bit | 行被改写而未回存的标志 |
| 排空 | drain / flush | 强制写缓冲区全部落地 |

## 4.1 替换问题：新块进门，谁该搬走

组相联 cache 的一个组满了，新块要进来，必须逐出一个牺牲者（victim）。理想的替换策略是 **Bélády 最优（OPT）**：踢掉"最远的将来才会再被访问（或不再访问）"的那一行——它需要预知未来，只能作为离线评估的理论上限。所有可实现策略都是在"过去的行为"里猜未来：

- **LRU（最近最少使用）**：假设"最近用过的将来还用"，那么踢最久没用的。对有良好时间局部性的负载接近最优；
- **随机**：不看历史，随机踢一位。

**LRU 的阿喀琉斯之踵是循环访问**：工作集比组容量大 1 的循环（如 9 个块轮着访问 8 路组），LRU 每次都恰好踢掉"下一个要用"的——100% 缺失；随机替换反而能稳定拿到 8/9≈89% 命中率。这就是著名的 LRU 反常（LRU anomaly）。安全侧信道也偏爱 LRU：攻击者可用驱逐策略探测受害者 cache 内容，随机替换因此被一些安全敏感设计采用。

## 4.2 替换策略的硬件实现

- **真 LRU 的代价**：维护 N 路的完整使用顺序需要 ⌈log₂(N!)⌉ 位状态（4 路 5 位、8 路 16 位），每次命中还要更新序——面积与关键路径都不友好。2 路 LRU 只需 1 位（记住谁最近用过），4 路勉强可行，8 路开始变贵。
- **树形 PLRU（tree-PLRU）**：N 路组配一棵 N−1 个节点的二叉树，每节点 1 位"指向"（上次未用的一侧）。访问某路时，沿路径把所有指向位改到背离它的方向；替换时顺着指向位走到的叶子就是牺牲者。8 路 PLRU 只用 7 位，更新只动路径上的 log₂N 位——**状态位少、更新局部**，成为最常用的工程实现。代价：它只是 LRU 的近似，某些序列下会作出 LRU 不会做的驱逐，但实测命中率损失通常 <1%。
- **随机**：硬件就是一个伪随机数发生器（如线性反馈移位寄存器 LFSR），零状态、零更新逻辑；对抗 LRU 反常与侧信道有奇效。
- **FIFO**：按进入顺序踢最老的，完全无视访问——命中率通常最差，只在极简硬件（某些 MCU）中出现。

## 4.3 写策略（一）：写直达（Write-Through）

**流程描述**：store 命中 → 同时写 cache 行与下一级；store 缺失 → 视写分配策略决定是否调入（4.5 节）。每一次写都产生向下一级的数据流动。

- **优点**：下一级永远与本级一致（"干净"），一致性、掉电恢复、调试都简单；脏位可以不要。
- **缺点**：写流量大。算一笔账：程序平均约 10%～15% 的访存是 store，若每秒 2 G 次访存、每次写 8 B，写直达要向下级持续输送 2×10⁹×0.1×8 = **1.6 GB/s**——对 L2 带宽是实打实的压力，多核场景会汇成带宽风暴。所以纯写直达必须配**写缓冲区**（4.6 节）才能撑住。

## 4.4 写策略（二）：写回（Write-Back）与脏位管理

**流程描述**：store 命中 → 只写本级行，置 dirty=1；store 缺失（写分配时）→ 调块、写、置脏。行被逐出时检查脏位：干净行直接扔掉（下一级本来就有），脏行必须先写回下一级再腾位置。

- **优点**：对同一行的多次写只沉淀为最终一次下级写（写合并的时间维度版本），写流量可低一个数量级；配合写分配利用写的空间局部性（写完还要读）。
- **代价**：①缺失路径变重——脏行腾位置要先写回，"一次缺失"可能引发"先写旧块、再读新块"两次下级事务；②一致性复杂化——某个核手里可能攥着别人看不见的脏数据，第 6 章的协议要专门伺候；③调试变难（最后写入主存的时间不确定）。

**脏位生命周期**：行填充时清零 → 本级 store 命中置 1 → 逐出时若为 1 则写回并清零。注意子块/部分写场景：若下级带 ECC，部分写需要"读-改-写"（先读整行、改局部、重算 ECC），这是某些设计里隐藏的延迟坑。

**图 4-1　写直达 vs 写回：一次 store 命中后的数据流**

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 680 330" width="100%" role="img">
  <title>写直达与写回策略的数据流对比</title>
  <defs>
    <marker id="arr41" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
      <path d="M2 1L8 5L2 9" fill="none" stroke="#444441" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
    </marker>
  </defs>
  <text x="340" y="24" text-anchor="middle" font-size="14" font-weight="500" fill="#333333">写直达 vs 写回：一次 store 命中后的数据流</text>
  <rect x="40" y="44" width="290" height="266" rx="12" fill="#F1EFE8" stroke="#5F5E5A" stroke-width="0.5"/>
  <text x="185" y="68" text-anchor="middle" font-size="13" font-weight="500" fill="#444441">写直达（Write-Through）</text>
  <rect x="60" y="88" width="72" height="34" rx="6" fill="#B5D4F4" stroke="#185FA5" stroke-width="0.5"/>
  <text x="96" y="109" text-anchor="middle" font-size="12" fill="#0C447C">CPU</text>
  <rect x="200" y="88" width="104" height="34" rx="6" fill="#B5D4F4" stroke="#185FA5" stroke-width="0.5"/>
  <text x="252" y="109" text-anchor="middle" font-size="12" fill="#0C447C">Cache 行</text>
  <line x1="132" y1="98" x2="200" y2="98" stroke="#444441" stroke-width="1.5" marker-end="url(#arr41)"/>
  <text x="166" y="86" text-anchor="middle" font-size="11" fill="#777777">写（同时更新）</text>
  <rect x="200" y="150" width="104" height="30" rx="6" fill="#C0DD97" stroke="#3B6D11" stroke-width="0.5"/>
  <text x="252" y="169" text-anchor="middle" font-size="12" fill="#173404">写缓冲区</text>
  <line x1="132" y1="116" x2="200" y2="158" stroke="#444441" stroke-width="1.5" marker-end="url(#arr41)"/>
  <rect x="200" y="220" width="104" height="34" rx="6" fill="#B4B2A9" stroke="#5F5E5A" stroke-width="0.5"/>
  <text x="252" y="241" text-anchor="middle" font-size="12" fill="#444441">下一级 L2/DRAM</text>
  <line x1="252" y1="180" x2="252" y2="220" stroke="#444441" stroke-width="1.5" marker-end="url(#arr41)"/>
  <text x="322" y="204" font-size="11" fill="#777777">异步落地</text>
  <text x="185" y="278" text-anchor="middle" font-size="11" fill="#888888">每次写都流向下一级；行永远干净（dirty 恒 0）</text>
  <rect x="350" y="44" width="290" height="266" rx="12" fill="#F1EFE8" stroke="#5F5E5A" stroke-width="0.5"/>
  <text x="495" y="68" text-anchor="middle" font-size="13" font-weight="500" fill="#444441">写回（Write-Back）</text>
  <rect x="370" y="88" width="72" height="34" rx="6" fill="#B5D4F4" stroke="#185FA5" stroke-width="0.5"/>
  <text x="406" y="109" text-anchor="middle" font-size="12" fill="#0C447C">CPU</text>
  <rect x="510" y="88" width="104" height="34" rx="6" fill="#B5D4F4" stroke="#185FA5" stroke-width="0.5"/>
  <text x="562" y="103" text-anchor="middle" font-size="12" fill="#0C447C">Cache 行</text>
  <text x="562" y="117" text-anchor="middle" font-size="11" fill="#A32D2D">置 dirty = 1</text>
  <line x1="442" y1="105" x2="510" y2="105" stroke="#444441" stroke-width="1.5" marker-end="url(#arr41)"/>
  <text x="476" y="93" text-anchor="middle" font-size="11" fill="#777777">只写本级</text>
  <rect x="510" y="220" width="104" height="34" rx="6" fill="#B4B2A9" stroke="#5F5E5A" stroke-width="0.5"/>
  <text x="562" y="241" text-anchor="middle" font-size="12" fill="#444441">下一级 L2/DRAM</text>
  <line x1="562" y1="122" x2="562" y2="220" stroke="#444441" stroke-width="1.5" stroke-dasharray="4,3" marker-end="url(#arr41)"/>
  <text x="632" y="176" font-size="11" fill="#777777" text-anchor="middle">逐出且</text>
  <text x="632" y="190" font-size="11" fill="#777777" text-anchor="middle">脏时才写回</text>
  <text x="495" y="278" text-anchor="middle" font-size="11" fill="#888888">对同一行的多次写沉淀为一次下级写</text>
</svg>

## 4.5 写分配：写缺失时调不调块

- **写分配（write-allocate）**：写缺失先把整块调入，再在行内改——赌"写了附近还会读写"。适合写局部性好的负载。
- **非写分配（no-write-allocate）**：写缺失绕过本级，直接写下级，行不调入。适合"写了就走、不再回看"的流式写。

**主流组合矩阵**：

| 组合 | 语义 | 使用场景 |
|---|---|---|
| **写回 + 写分配** | 缺失调入、行内改、脏回写 | 几乎所有现代 CPU 的 L1/L2——对局部性双向下注 |
| **写直达 + 非写分配** | 写直达下级、不污染本级 | 部分嵌入式/实时系统、一些 GPU L1 |
| 写直达 + 写分配 | 调入又同时写下级，重复劳动 | 少见 |
| 写回 + 非写分配 | 行内改需要行存在，逻辑别扭 | 基本不出现 |

**一个小算例**：向一个新 64 B 块写 8 B。（WB+WA）：一次读填充（64 B 入）+ 一次未来逐出写回（64 B 出）= 128 B 下级流量；（WT+no-WA）：8 B 直达 + 若之后不再访问，共 8 B 流量。若之后同块再写 7 次 8 B：WB+WA 仍 128 B，WT+no-WA 变 64 B——**策略优劣取决于后续访问模式**，这正是"负载驱动设计"的微观例证。

## 4.6 写缓冲区：让写不再阻塞

**结构描述**：一个深度为 8～32 项的 FIFO 队列，每项存（目标地址、整行/部分数据、字节使能）。store 在写直达架构下把数据丢进缓冲区就宣告完成，由缓冲区按下一级节奏慢慢落地。

**设计要点**：

1. **深度**：要能吸收"写突发 × 下级延迟"。太浅则满速写下会 backpressure 反压流水线；太深则面积与一致性问题激化。
2. **写合并（write merging）**：新写与缓冲区内既有项若同块，就地合并（部分写叠加、字节使能相或），大幅减少下级事务。
3. **读缺失撞缓冲区**：读缺失要去下一级取块，而这块的最新数据可能还躺在写缓冲区里没落地——**必须让读缺失请求与缓冲区内容做关联检查**（按地址检索），命中则取缓冲区的版本。漏掉这一步是学生设计的经典 bug。
4. **与 store buffer 的区别**：store buffer 属于处理器核（保存推测执行的 store，提交后才进 cache/写缓冲），处理乱序与精确异常；写缓冲区属于存储层次。两者前后接力。

## 案例/示例：策略选择如何被负载塑造

**案例 A（数据库 OLTP 类负载）**：大量随机 8 B 写、写局部性差。写直达会把每个小写都打到下级——缓冲区合并机会少、带宽吃不消；写回 + 写分配让多次对同一行的写（B 树节点）在 L1 内沉淀，代价是脏行逐出的两段式缺失。数据库引擎普遍按缓存行对齐记录字段（如 64 B 对齐），正是为了让"一次行填充尽量装下热字段"。

**案例 B（GPU 显存清零）**：`cudaMemset` 类"写了就走"的大块流式写。理想路径是非写分配/绕过 cache 直接整行写下级——现代 GPU 与一些 CPU 的"非临时写"（x86 `movnt*`）提供了这种提示，避免用无用数据污染 cache。

## 常见误区与注意事项

1. **"LRU 一定最好"**——循环访问下 LRU 全缺失、随机反而优秀；大容量 LLC 上近似 LRU（如 DIP 系自适应策略）常常更稳。
2. **PLRU ≠ LRU**：树形近似在特定序列会误驱逐，验证替换逻辑时要用对抗序列。
3. **以为写直达"更慢所以没人用"**：它简化一致性（下一级永远新鲜），很多实时/高可靠系统（以及 IBM z 系列的部分层级）乐于付出带宽代价。
4. **忘了读缺失对写缓冲区的关联检查**——正确性 bug，不是性能问题。
5. **混淆脏位与有效位**：valid 说"内容可信"，dirty 说"内容比下一级新"。逐出时 valid=0 直接扔、valid=1&dirty=0 直接扔、dirty=1 必须写回。
6. **忽略写流量预算**：多核 × 写直达 = 带宽灾难；估算系统带宽时先算写这一笔账。

## 本章小结

- 替换的理想是 Bélády OPT；LRU 是实用近似但有循环访问反常；工程主流是树形 PLRU（N−1 位状态）与随机（抗抖动、抗侧信道）。
- 写直达：流量大但下级永远干净；写回：流量小但脏位、两段式缺失与一致性复杂度全要买单。
- 写分配与写策略的搭配由负载写局部性决定：WB+WA 通用主流，WT+no-WA 服务流式写与实时系统。
- 写缓冲区是写直达的救命稻草：深度、合并、读缺失关联检查三大要点。
- 写路径的复杂性是多核一致性（第 6 章）问题重灾区——本章是它的直接铺垫。

## 思考题与练习

**1.（概念题）** 解释写回与写直达的区别，并各举一个最适合的场景，说明理由。
*（要点：WB 适合写局部性好的通用负载（流量小）；WT 适合要求下级始终最新、调试/一致性简单或写稀疏的实时系统。）*

**2.（计算题）** 某核每秒 3 G 次访存，其中 12% 是 store，平均每次写 8 B。分别计算写直达与写回（设写回下行流量为写的 25%，因合并与不回写干净行）向 L2 的持续写带宽需求。
*（参考：WT：3G×0.12×8 ≈ 2.9 GB/s；WB：≈0.72 GB/s。）*

**3.（分析题）** 4 路组相联、访问路序列（组内）：`0,1,2,3,4,0`。给出真 LRU 与树形 PLRU（初始指向任设）各自的驱逐对象，并说明 PLRU 与 LRU 何处分歧。

**4.（计算题）** 为 8 路组实现真 LRU 与树形 PLRU 各需要多少状态位/组？2 路组呢？
*（参考：8 路真 LRU ⌈log₂(8!)⌉=16 位、PLRU 7 位；2 路 LRU 1 位、PLRU 1 位——2 路时两者等价。）*

**5.（设计题）** 你在为 MCU 设计 L1D，约束：面积小、行为可预测。候选：(a) 写直达+非写分配、无脏位；(b) 写回+写分配。从硬件复杂度、写带宽（MCU 的 L2 是慢速 SRAM）、确定性三方面论证，并给出你的选择与风险清单。

## 拓展阅读

1. H&P, *A Quantitative Approach*, 6th ed., §2.2（写策略与写缓冲区）与替换策略小节。
2. Aamer Jaleel et al., "High Performance Cache Replacement Using Re-Reference Interval Prediction (RRIP)," ISCA 2010——现代 LLC 替换策略演进的代表作，展示"打败 LRU"的路线。
3. Norman Jouppi, "Improving Direct-Mapped Cache Performance by the Addition of a Small Fully-Associative Cache and Prefetch Buffers," ISCA 1990——虽是 victim cache 论文，其对写缓冲与缺失路径的分析同样经典。

---
---

# 第 5 章 多级 Cache 与高级优化技术

## 本章导读

前两章把单级 cache 讲透了，但现代处理器从不只有一级。本章先解决多级 cache 的两个结构性问题：**各级怎么分工（延迟与容量的梯队）**、**各级内容什么关系（包含/非包含/互斥）**——并回收第 1 章埋下的"Skylake L3 非包含式"伏笔。然后进入降低缺失与延迟的武器库：victim cache、skewed cache、路预测，以及最重要的**预取**（硬件与软件）。学完本章，你面对"命中率 97% 但 IPC 上不去"这类问题，就有了成套的分析与改进手段。

## 学习目标

1. 解释 L1/L2/L3 的分工逻辑，用局部/全局缺失率做层级流量预算；
2. 说清包含、互斥、非包含三种策略的实现机制、优缺点与真实采用者；
3. 描述 victim cache、skewed-associative、way prediction 各自解决什么问题、怎么解决；
4. 描述 next-line/步长/流/GHB 四类硬件预取器的工作方式，并用覆盖率/及时性/精度三指标评估预取；
5. 正确使用软件预取指令（选距离、防越界、防污染）。

## 核心概念

| 术语（中文） | 英文 | 一句话定义 |
|---|---|---|
| 局部/全局缺失率 | local / global miss rate | 本级缺失÷到达本级的访问 / ÷CPU 全部访存 |
| 包含式 | inclusive | 上级行必存在于下级（L3 ⊇ L2 ⊇ L1） |
| 互斥式 | exclusive | 同一数据至多存在于一级（L2+L3 容量相加） |
| 非包含非互斥 | NINE | 不维护上下级包含关系（Skylake L3） |
| 反向无效化 | back-invalidate | 包含式下级逐出行时向上发无效化 |
| 受害者缓存 | victim cache | 收容被逐出行的全相联小 cache |
| 倾斜相联 | skewed-associative | 各路用不同散列函数定位，错开冲突 |
| 路预测 | way prediction | 猜中路先取数，错则重来，缩短命中延迟 |
| 预取 | prefetching | 在需求发生前把数据搬近 |
| 覆盖率/精度/及时性 | coverage / accuracy / timeliness | 预取替代了多少缺失 / 预取的有用比例 / 到得是否正好 |
| 全局历史缓冲 | GHB (Global History Buffer) | 记录缺失历史的环形缓冲，做模式相关预取 |
| 缓存污染 | cache pollution | 无用预取占据行，挤掉有用数据 |

## 5.1 多级 Cache 的必要性：延迟与容量的分工

单级 cache 若要 64 MB，命中延迟必然几十周期——每条访存都背不动；若只有 32 KB，缺失率又压不住。唯一出路是**梯队**：L1 用 32～64 KB 把命中延迟锁在 4 周期；L2 用 256 KB～2 MB 在 ~12 周期接住 L1 的漏；L3 用几十 MB 在 ~40 周期接住全核的漏。每一级的存在理由都是"用可接受的延迟换大幅下降的缺失率"。

**流量预算示例**（回顾第 1 章例 3 的语言）：每核每秒 2 G 次访存，L1 全局缺失率 4% → L2 每秒接收 80 M 次请求；L2 局部缺失率 25% → L3 接收 20 M 次；L3 局部缺失率 20% → DRAM 4 M 次 ≈ 256 MB/s（按 64 B/请求）。**设计各级带宽时按最坏核 × 最坏负载再加 50% 余量**——这就是从缺失率到带宽需求的完整推导链，第 16 章设计流程会正式用它。

## 5.2 包含性策略：包含、非包含、互斥

**图示化对比**（想象三层的抽屉柜）：

- **包含（inclusive）**：L3 拥有所有 L2/L1 的内容（大抽屉里必须能找到小抽屉的一切）。**优点**：一致性侦听只需查 L3——L3 没有的数据必然不在任何核的私有 cache 里，一次查找回答全局问题（这叫"隐式目录"）；芯片外的侦听也只需探 L3。**缺点**：①有效容量重复计价（8 核各 512 KB L2 + 8 MB L3，实际"独有"数据只有 8 MB − 4 MB = 4 MB）；②L3 逐出一行（因容量压力）时必须**反向无效化**所有核的对应私有行——back-invalidate 风暴会猝不及防地打爆 L1 命中率。
- **互斥（exclusive）**：同一数据至多存于一级，L2 专收 L1 的逐出（victim），L3 专收 L2 的逐出。**优点**：总有效容量 = 各级之和（AMD 长期路线：512 KB L2 + 若干 MB L3 全部可用）；**缺点**：逐出链路复杂（L1 逐出→写进 L2 时可能又把 L2 的行挤到 L3…），一致性无隐式目录可依赖，需额外目录。
- **非包含非互斥（NINE）**：不保证任何关系。Skylake 的 L3 从包含改为 NINE，动机正是包含式的两大痛点：back-invalidate 与有效容量缩水。代价是要补一套**侦听过滤器/重复标签目录**（snoop filter / duplicate tag directory，本质上就是一份记录"谁可能有什么"的目录）来维持一致性——与第 6 章目录协议直接接轨。

**选择逻辑速记**：核数少、追求简单 → 包含；追求容量极致 → 互斥；核多、L3 大 → NINE + 目录（现代高性能 CPU 的主流归宿）。

**图 5-1　包含 / 互斥 / NINE：三种多级内容关系（色块 = 数据块）**

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 680 300" width="100%" role="img">
  <title>包含、互斥与非包含非互斥三种多级 cache 内容关系对比</title>
  <text x="340" y="24" text-anchor="middle" font-size="14" font-weight="500" fill="#333333">包含 / 互斥 / NINE：同一份数据在多级 cache 中的三种关系</text>
  <rect x="40" y="40" width="200" height="216" rx="12" fill="#E6F1FB" stroke="#185FA5" stroke-width="0.5"/>
  <text x="140" y="64" text-anchor="middle" font-size="13" font-weight="500" fill="#0C447C">包含（Inclusive）</text>
  <g font-size="11" fill="#444441">
    <text x="52" y="98">L1</text><text x="52" y="148">L2</text><text x="52" y="198">L3</text>
  </g>
  <g>
    <rect x="85" y="80" width="30" height="24" fill="#378ADD" stroke="#185FA5"/><text x="100" y="96" text-anchor="middle" font-size="11" fill="#FFFFFF">A</text>
    <rect x="121" y="80" width="30" height="24" fill="#1D9E75" stroke="#0F6E56"/><text x="136" y="96" text-anchor="middle" font-size="11" fill="#FFFFFF">B</text>
    <rect x="85" y="130" width="30" height="24" fill="#378ADD" stroke="#185FA5"/><text x="100" y="146" text-anchor="middle" font-size="11" fill="#FFFFFF">A</text>
    <rect x="121" y="130" width="30" height="24" fill="#1D9E75" stroke="#0F6E56"/><text x="136" y="146" text-anchor="middle" font-size="11" fill="#FFFFFF">B</text>
    <rect x="157" y="130" width="30" height="24" fill="#EF9F27" stroke="#854F0B"/><text x="172" y="146" text-anchor="middle" font-size="11" fill="#FFFFFF">C</text>
    <rect x="85" y="180" width="30" height="24" fill="#378ADD" stroke="#185FA5"/><text x="100" y="196" text-anchor="middle" font-size="11" fill="#FFFFFF">A</text>
    <rect x="121" y="180" width="30" height="24" fill="#1D9E75" stroke="#0F6E56"/><text x="136" y="196" text-anchor="middle" font-size="11" fill="#FFFFFF">B</text>
    <rect x="157" y="180" width="30" height="24" fill="#EF9F27" stroke="#854F0B"/><text x="172" y="196" text-anchor="middle" font-size="11" fill="#FFFFFF">C</text>
    <rect x="193" y="180" width="30" height="24" fill="#D4537E" stroke="#993556"/><text x="208" y="196" text-anchor="middle" font-size="11" fill="#FFFFFF">D</text>
  </g>
  <text x="140" y="238" text-anchor="middle" font-size="11" fill="#888888">L3 ⊇ L2 ⊇ L1；有效独有容量最小</text>
  <rect x="240" y="40" width="200" height="216" rx="12" fill="#E1F5EE" stroke="#0F6E56" stroke-width="0.5"/>
  <text x="340" y="64" text-anchor="middle" font-size="13" font-weight="500" fill="#085041">互斥（Exclusive）</text>
  <g font-size="11" fill="#444441">
    <text x="252" y="98">L1</text><text x="252" y="148">L2</text><text x="252" y="198">L3</text>
  </g>
  <g>
    <rect x="285" y="80" width="30" height="24" fill="#378ADD" stroke="#185FA5"/><text x="300" y="96" text-anchor="middle" font-size="11" fill="#FFFFFF">A</text>
    <rect x="285" y="130" width="30" height="24" fill="#1D9E75" stroke="#0F6E56"/><text x="300" y="146" text-anchor="middle" font-size="11" fill="#FFFFFF">B</text>
    <rect x="285" y="180" width="30" height="24" fill="#EF9F27" stroke="#854F0B"/><text x="300" y="196" text-anchor="middle" font-size="11" fill="#FFFFFF">C</text>
    <rect x="321" y="180" width="30" height="24" fill="#D4537E" stroke="#993556"/><text x="336" y="196" text-anchor="middle" font-size="11" fill="#FFFFFF">D</text>
    <rect x="357" y="180" width="30" height="24" fill="#7F77DD" stroke="#534AB7"/><text x="372" y="196" text-anchor="middle" font-size="11" fill="#FFFFFF">E</text>
  </g>
  <text x="340" y="238" text-anchor="middle" font-size="11" fill="#888888">每块至多存于一级；总容量相加</text>
  <rect x="440" y="40" width="200" height="216" rx="12" fill="#FAECE7" stroke="#993C1D" stroke-width="0.5"/>
  <text x="540" y="64" text-anchor="middle" font-size="13" font-weight="500" fill="#712B13">NINE（非包含非互斥）</text>
  <g font-size="11" fill="#444441">
    <text x="452" y="98">L1</text><text x="452" y="148">L2</text><text x="452" y="198">L3</text>
  </g>
  <g>
    <rect x="485" y="80" width="30" height="24" fill="#378ADD" stroke="#185FA5"/><text x="500" y="96" text-anchor="middle" font-size="11" fill="#FFFFFF">A</text>
    <rect x="521" y="80" width="30" height="24" fill="#7F77DD" stroke="#534AB7"/><text x="536" y="96" text-anchor="middle" font-size="11" fill="#FFFFFF">E</text>
    <rect x="485" y="130" width="30" height="24" fill="#1D9E75" stroke="#0F6E56"/><text x="500" y="146" text-anchor="middle" font-size="11" fill="#FFFFFF">B</text>
    <rect x="521" y="130" width="30" height="24" fill="#EF9F27" stroke="#854F0B"/><text x="536" y="146" text-anchor="middle" font-size="11" fill="#FFFFFF">C</text>
    <rect x="485" y="180" width="30" height="24" fill="#EF9F27" stroke="#854F0B"/><text x="500" y="196" text-anchor="middle" font-size="11" fill="#FFFFFF">C</text>
    <rect x="521" y="180" width="30" height="24" fill="#D4537E" stroke="#993556"/><text x="536" y="196" text-anchor="middle" font-size="11" fill="#FFFFFF">D</text>
  </g>
  <text x="540" y="238" text-anchor="middle" font-size="11" fill="#888888">无包含保证；靠目录 / 侦听过滤器维护一致性</text>
  <text x="340" y="280" text-anchor="middle" font-size="11" fill="#999999">注：L3 逐出 A 时，包含式必须反向无效化（back-invalidate）L1/L2 中的 A</text>
</svg>

## 5.3 降冲突与降延迟的武器

- **Victim cache（Jouppi 1990）**：直接映射 cache 旁边挂一个 8～16 项的全相联小 cache，专收被逐出的"受害者"。当冲突抖动发生（A 踢 B、B 又回来踢 A），被踢者大概率还在 victim cache 里，一次快速救回。它以极小的代价修补直接映射的冲突缺失——HP PA-7200 的 assist cache 是其经典商用实现；思想在现代演化成"与 L2 之间的逐出缓冲区"。
- **Skewed-associative**：让地址在不同"路分区"用**不同的散列函数**选位置——两个互相冲突的地址在分区 1 撞车，在分区 2 大概率错开，等价于提高了相联度而不增加比较器。代价：散列逻辑与逐出策略复杂。
- **Way prediction**：对组相联的每一次访问，先**猜哪一路命中**，直接把那一路的数据读出来与 tag 并行验证——猜对则命中延迟≈直接映射；猜错则补一次全组查找（多花 1～2 周期）。预测器用 PC/地址历史训练，精度可达 85%～95%。ARM 的 Cortex-A 系广泛采用，把 4 路组相联"伪装"成近似 2 周期命中。**注意与"预取"的区别**：way prediction 猜的是**位置**，预取猜的是**要不要提前搬数据**。

## 5.4 硬件预取：四种武器

预取 = 在 CPU 发出访问之前，硬件主动把"大概率马上要访问"的块搬进 cache。三把尺子：**覆盖率**（消除了多少本会发生的缺失）、**精度**（预取的块有多少真被用到——1−污染率）、**及时性**（到得太早被逐出、太晚没盖住延迟，都不行）。

1. **Next-line 预取**：取第 i 行时顺手取 i+1——纯空间局部性，最便宜，icache 标配。
2. **步长预取（stride）**：记录某条指令的历史地址差（stride），发现恒定步长就按差外推。流缓冲（stream buffer，Jouppi）是代表：一队按 stride 排队的预取请求，CPU 访问队头命中则弹出并续队。数组按固定步长扫（哪怕 stride=512 B 的列访问）时威力巨大。
3. **流预取（stream）**：检测到某 4 KB 区域内顺序访问，就整片正向/反向流水线搬运——面向 memcpy/流媒体这类大流量；通常部署在 L2/L3，一次开多条流。
4. **全局历史缓冲（GHB）**：把全系统的缺失地址记进一个环形缓冲，再按"缺失地址 → 下一个缺失"做相关训练——能学到不规则但重复的模式（如指针追逐的稳定轨迹、嵌套数组的复合步长），是学术与商用预取器（如 Intel 的 spatial/stream 混合体）的底座。

**部署位置**：越靠近 CPU 的预取越要克制（及时性要求高、污染代价大）；L2/L3 的预取可以更激进。**共同风险**：精度差 → 污染 → 命中率不升反降；带宽滥用 → 抢占真实缺失的通路（第 17 章误区专题）。

## 5.5 软件预取：x86 PREFETCH 的用法与陷阱

```c
for (i = 0; i < N; i++)
    sum += a[i] * b[i];
```

改造：`__builtin_prefetch(&a[i+D], 0, 1); __builtin_prefetch(&b[i+D], 0, 1);`（GCC 内建，最终生成 `PREFETCHT0`）。三个决策：

1. **距离 D**：目标是"预取发起 → 数据到位"的时延被 D 次迭代消化。经验公式：D ≈ 延迟(周期) ÷ 每次迭代消耗的访存周期数。DRAM 200 周期、每迭代 2 次访存 2 周期吞吐 → D≈100 起步，实测微调。太早（被逐出）与太晚（没盖住延迟）都白干。
2. **提示级别**：T0（各级都用）、T1/T2（不进 L1）、NTA（非临时，用完即弃防污染）。数组求和只消费一次 → NTA 更友好。
3. **安全性**：x86 的 PREFETCH 对非法地址**不产生异常**（仅作提示），但其他架构（部分 ARM 实现）未必——跨平台代码要对指针做边界判断。此外过度预取会挤占带宽，标准库 `memcpy` 内的软件预取是精调过的，不要在外层重复叠加。

## 5.6 案例分析：Intel Skylake 的多级预取器

公开资料口径：**L1D** 有基于指令指针（IP）的步长预取器——按"哪条 load 指令、以什么步长访问"训练，DCU prefetcher；**L2** 有两个：spatial prefetcher（相邻行预取：访问一行就预取其 128 B 对偶行——把 64 B 块当成 128 B 对看待）与 streamer（在 L2 内检测多达 32 条向上/向下的流，跨页延续）；**L3** 侧同样有流检测，且能跨核共享预取成果。整体设计哲学：**L1 克制（及时性优先）、L2 主力（容量缓冲大、污染容忍度高）、L3 兜底**。评估一个预取器配置时，请同时盯三组计数器：预取数、有用预取数（被命中）、因污染多出的缺失数——只看总缺失率会被"预取把缺失提前消掉但没被用上"的假象欺骗。

## 常见误区与注意事项

1. **把局部缺失率当全局**：文献说"L2 缺失率 25%"若指局部，全局可能只有 1%——报流量与算 AMAT 前先统一口径。
2. **以为包含式"天然正确"**：back-invalidate 风暴与有效容量税是真实代价，核越多越痛。
3. **way prediction ≠ 预取**：一个省"选路"延迟，一个省"缺失"延迟。
4. **预取只看覆盖率**：覆盖率 90% 而精度 40% 的预取器是负资产（污染 + 带宽）。
5. **软件预拍距离拍脑袋**：距离必须由延迟与迭代访存密度推出并实测校准。
6. **忘了预取不改语义**：预取永远只是提示，不改变程序可见行为——它失败只损失性能，不损失正确性（这一点让它可以大胆激进）。

## 本章小结

- 多级梯队用"可接受的每级延迟换逐级下降的缺失率"；流量预算从各级全局缺失率推出。
- 包含式提供隐式目录但收容量税与 back-invalidate；互斥式容量相加但管理重；NINE + 侦听过滤器是当代主流。
- Victim cache 救冲突、skewed 错开冲突、way prediction 猜路省延迟——三件小而美的武器。
- 硬件预取四型（next-line/stride/stream/GHB）按"离核越近越克制"部署；软件预取三决策（距离/级别/边界）。
- 预取评估三件套：覆盖率、精度、及时性——缺一不可。

## 思考题与练习

**1.（计算题）** 8 核 CPU，各核 L2 512 KB，L3 8 MB 包含式。求"独占 L3 空间"的最坏值；若改互斥式，总有效容量是多少？
*（参考：包含式最坏 8 MB − 4 MB = 4 MB；互斥式 = 4 + 8 = 12 MB。）*

**2.（分析题）** 某程序在直接映射 L1 上缺失率 8%，加 16 项 victim cache 后降到 3%。判断被 victim cache 救回的 5 个百分点主要属于 3C 中的哪一类？若不加 victim cache 而是把 L1 加倍（同样降到 3%），两种方案在延迟/面积/能耗上如何对比？
*（要点：冲突缺失；victim cache 以极小面积换命中，加倍 L1 却抬高所有访问的命中延迟。）*

**3.（计算题）** 软件预取：DRAM 延迟 300 周期，循环每迭代执行 4 条访存指令（1 周期/次发射，其余 2 计算周期，共 6 周期/迭代）。预取距离 D 至少多少才能盖住延迟？若 D 过大到 10 倍于 L1 行数会怎样？
*（参考：D ≥ 300/6 = 50；过大的 D 使预取块在消费前就被逐出——白干。）*

**4.（概念题）** 解释 GHB 预取器为何能学会"步长预取器学不会"的模式，并举一个程序中的真实例子。

**5.（设计题）** 你负责一颗 4 核芯片的 L2 预取器选型：负载为视频转码（大块顺序流）+ 数据库索引查找（随机但有重复轨迹）。给出你的预取器组合、部署层级与置信度门槛设计。

## 拓展阅读

1. H&P, *A Quantitative Approach*, 6th ed., §2.2–2.3（多级包含与预取的权威论述）。
2. Norman Jouppi, ISCA 1990（victim cache 与 stream buffer 的原始论文，一篇顶半章）。
3. Somayeh Sardashti & David Wood, "Decoupled Compressed Cache (DCC)"或更基础的 skew 论文： André Seznec, "A Case for Two-Way Skewed-Associative Caches," ISCA 1993。

---
---

# 第 6 章 缓存一致性（Cache Coherence）

## 本章导读

到目前为止我们只谈单核。一旦每个核都有自己的私有 cache，同一份数据就存在多份副本——核 A 改了自己那份，核 B 还在美滋滋地读自己那份旧数据，程序语义当场崩塌。**缓存一致性协议（cache coherence protocol）**就是解决"多份副本谁说了算"的机制，它是多核设计中最精妙也最容易出 bug 的部分。本章按"问题 → 策略（无效 vs 更新）→ 经典协议（MSI → MESI → MOESI）→ 扩展机制（总线侦听 vs 目录）→ 真实实现（MESIF）"的顺序展开。第 14 章会把本章的概念延伸到 CPU 与 NPU 的异构世界。

## 学习目标

1. 说清一致性要保证的两件事（写传播、写串行化）并能举出违反它们的反例；
2. 论证写无效与写更新的取舍，解释现代处理器几乎全选写无效的原因；
3. 在给定读写序列下，逐事务跟踪 MSI/MESI 的状态转换并画出状态表；
4. 解释 MESI 的 E 态与 MOESI 的 O 态各自省掉了什么总线事务；
5. 对比总线侦听与目录（全位/有限指针/链式）的扩展性，并估算目录存储开销。

## 核心概念

| 术语（中文） | 英文 | 一句话定义 |
|---|---|---|
| 缓存一致性 | cache coherence | 所有核对同一地址的观察序满足单写者与写串行化 |
| 写传播 | write propagation | 一个核的写最终对其他核可见 |
| 写串行化 | write serialization | 所有核对同一地址的写看到相同顺序 |
| 写无效 / 写更新 | write-invalidate / write-update | 广播"副本作废" / 广播"新值" |
| 总线侦听 | bus snooping | 每个缓存控制器监听总线上的事务并自查 |
| 侦听过滤器 | snoop filter | 记录"谁可能有什么"的目录，免于广播侦听 |
| MSI / MESI / MOESI | 协议名 | 修改/共享/无效 + 独占 + 拥有态的状态机族 |
| 独占态 | Exclusive (E) | 干净且唯一的副本，可静默升级为 M |
| 拥有态 | Owned (O) | 脏且共享：我负责回写，其余副本可读 |
| 前递态 | Forward (F) | MESIF 中负责响应读的那个干净共享副本 |
| 目录 | directory | 集中记录每块缓存于哪些核的结构 |
| 缓存间传输 | cache-to-cache transfer | 缺失时直接从别的核的 cache 拿数据（不过 DRAM） |

## 6.1 多核时代的难题

**反例**：初始 `x=0`，核 A 执行 `x=1`，核 B 随后 `r = x`。若 A 的写只落在自己的 L1（写回策略，第 4 章），B 的 L1 里还缓存着旧值 0——B 读到 0。程序的直觉被硬件背叛了。

一致性协议必须保证两件事：

- **写传播（write propagation）**：对某地址的写，最终必须到达所有持有副本的 cache（作废或更新它们）；
- **写串行化（write serialization）**：所有处理器看到对同一地址的写以**相同顺序**发生（不同地址之间不要求全序——那是内存模型的话题，第 14 章）。

**实现一致性的两个基础设施问题**：写发生时如何找到所有副本（广播 or 目录）；数据由谁供给（DRAM or 某个 cache）。前者引出 6.3/6.5 的两大机制，后者引出 cache-to-cache 传输与 O/F 态的巧思。

## 6.2 写无效 vs 写更新

核 A 要写，而别核可能有副本时，两条路：

- **写无效（write-invalidate）**：广播"持有地址 X 副本的，立刻作废"。别核下次读 X 时缺失，再通过协议拿最新值。**主流选择**。理由：一次广播只作废（不传数据）；之后若写者继续写（常见——时间局部性），无人打扰；失效的副本不再占用总线。
- **写更新（write-update）**：广播新值，所有副本就地刷新。读多写少且副本被反复读时省掉了缺失；但每次写都要全网送数据，写写之间容易乒乓（两个核交替写同一地址，每次都广播）。现代通用处理器几乎不用（某些老的、总线带宽富裕的机器如 Dragon 协议用过）。

**类比**：写无效像群发"旧版文件作废，要看的来找我拿最新版"；写更新像每次改文件都把新版群发全员。前者一次打扰、按需拉取；后者人人即时最新，但聊天记录刷屏。

## 6.3 总线侦听与 MSI 协议

**总线侦听（snooping）**：所有 cache 挂在同一根共享总线上，每个 cache 控制器偷听总线上所有事务——听到与自己有关的（自己持有该地址的行），就自动反应。总线天然提供全序（事务在总线上排队），写串行化免费获得。代价：广播流量随核数线性增长，总线本身是单点瓶颈——适合 ≤8 核。

**MSI 状态机**（M 修改 / S 共享 / I 无效）：

- **处理器侧请求**：PrRd（核要读）、PrWr（核要写）；
- **总线侧事务**：BusRd（别人缺失来读）、BusRdX（别人要写，要独占）、BusUpgr（自己已有 S，升级为 M）。

**转换规则**（按"当前态 × 事件 → 新态 + 动作"理解）：

| 当前态 | PrRd | PrWr | 总线 BusRd | 总线 BusRdX |
|---|---|---|---|---|
| M | 命中，留 M | 命中，留 M | 写回 DRAM，转 S，供数据 | 写回，转 I，供数据 |
| S | 命中，留 S | 发 BusUpgr，转 M | 留 S | 转 I |
| I | 发 BusRd，转 S | 发 BusRdX，转 M | 留 I | 留 I |

**跟一遍序列**（两核，初始 x 行在双方均 I，DRAM 中 x=0）：

1. A 读 x：A 发 BusRd → DRAM 供数 → A=S；
2. B 读 x：B 发 BusRd → A 侦听到（自己 S），不动作 → DRAM 供数（或 cache-to-cache）→ A=S, B=S；
3. A 写 x：A 发 BusUpgr → B 侦听到 BusUpgr 而 B=S → B 作废转 I → A=M（新值只在 A 的 cache 里）；
4. B 读 x：B 发 BusRd → A（M 态）侦听到 → A 写回 DRAM 并转 S、直接供数据给 B → A=S, B=S。

第 4 步中的"直接供数"就是 cache-to-cache transfer——省掉等 DRAM 的时间。

## 6.4 MESI 与 MOESI：给 MSI 打的两个补丁

**MESI（=MSI + Exclusive）**：MSI 的痛点——核 A 想写一块只有自己缓存的干净数据（比如刚从 DRAM 读入、别核根本没碰过），按 MSI 也要发 BusUpgr 占用总线。MESI 加一个 **E（独占，干净且全网唯一）** 态：从 DRAM 读入时若确认无他核副本则进 E；E 态下写**静默升级**为 M，零总线流量。省下的不是延迟而是一致性带宽——多核高带宽负载下积少成多。代价：系统要能区分"独占读"与"共享读"（读请求带期望，目录/总线裁决后告知）。

**MOESI（=MESI + Owned）**：MESI 的痛点——M 态的行被别核读走后，M 态核必须先把脏数据写回 DRAM 才能转 S（见 6.3 第 4 步），白白一次 DRAM 写带宽。MOESI 加 **O（拥有，脏且共享）** 态：脏行被共享后，M 态核转 **O** 而不写回——O 态者保留"回写责任"，其余核持 S 脏副本；后续再有核来读，直接由 O 态核供数（cache-to-cache），DRAM 一直不掺和，直到 O 态行被逐出才回写。AMD 长期采用。

**一句话记法**：E 省"独占写"的总线事务，O 省"脏共享"的 DRAM 写回。

**图 6-1　MESI 状态转换图（蓝实线 = 本核请求；红虚线 = 总线侧事务）**

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 680 380" width="100%" role="img">
  <title>MESI 协议状态转换图</title>
  <defs>
    <marker id="arr61b" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
      <path d="M2 1L8 5L2 9" fill="none" stroke="#185FA5" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
    </marker>
    <marker id="arr61r" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
      <path d="M2 1L8 5L2 9" fill="none" stroke="#993C1D" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
    </marker>
  </defs>
  <text x="340" y="24" text-anchor="middle" font-size="14" font-weight="500" fill="#333333">MESI 状态转换图（针对某个 cache 行）</text>
  <rect x="70" y="170" width="92" height="52" rx="8" fill="#B4B2A9" stroke="#5F5E5A" stroke-width="0.5"/>
  <text x="116" y="192" text-anchor="middle" font-size="13" font-weight="500" fill="#444441">I 无效</text>
  <text x="116" y="210" text-anchor="middle" font-size="11" fill="#5F5E5A">无副本</text>
  <rect x="294" y="80" width="92" height="52" rx="8" fill="#C0DD97" stroke="#3B6D11" stroke-width="0.5"/>
  <text x="340" y="102" text-anchor="middle" font-size="13" font-weight="500" fill="#173404">S 共享</text>
  <text x="340" y="120" text-anchor="middle" font-size="11" fill="#27500A">干净 · 多副本</text>
  <rect x="510" y="80" width="92" height="52" rx="8" fill="#B5D4F4" stroke="#185FA5" stroke-width="0.5"/>
  <text x="556" y="102" text-anchor="middle" font-size="13" font-weight="500" fill="#0C447C">E 独占</text>
  <text x="556" y="120" text-anchor="middle" font-size="11" fill="#0C447C">干净 · 唯一</text>
  <rect x="510" y="260" width="92" height="52" rx="8" fill="#F5C4B3" stroke="#993C1D" stroke-width="0.5"/>
  <text x="556" y="282" text-anchor="middle" font-size="13" font-weight="500" fill="#712B13">M 修改</text>
  <text x="556" y="300" text-anchor="middle" font-size="11" fill="#712B13">脏 · 唯一</text>
  <line x1="130" y1="170" x2="300" y2="118" stroke="#185FA5" stroke-width="1.5" marker-end="url(#arr61b)"/>
  <text x="180" y="128" font-size="11" fill="#185FA5">PrRd（BusRd）</text>
  <line x1="140" y1="222" x2="510" y2="292" stroke="#185FA5" stroke-width="1.5" marker-end="url(#arr61b)"/>
  <text x="270" y="278" font-size="11" fill="#185FA5">PrWr（BusRdX）</text>
  <line x1="386" y1="106" x2="522" y2="258" stroke="#185FA5" stroke-width="1.5" marker-end="url(#arr61b)"/>
  <text x="452" y="164" font-size="11" fill="#185FA5">PrWr（BusUpgr）</text>
  <line x1="556" y1="132" x2="556" y2="260" stroke="#185FA5" stroke-width="1.5" marker-end="url(#arr61b)"/>
  <text x="564" y="200" font-size="11" fill="#185FA5">PrWr（静默）</text>
  <line x1="505" y1="285" x2="382" y2="120" stroke="#993C1D" stroke-width="1.5" stroke-dasharray="4,3" marker-end="url(#arr61r)"/>
  <text x="392" y="244" font-size="11" fill="#993C1D">BusRd：写回 + 供数</text>
  <line x1="508" y1="296" x2="164" y2="216" stroke="#993C1D" stroke-width="1.5" stroke-dasharray="4,3" marker-end="url(#arr61r)"/>
  <text x="230" y="310" font-size="11" fill="#993C1D">BusRdX：写回 + 作废</text>
  <line x1="292" y1="132" x2="140" y2="178" stroke="#993C1D" stroke-width="1.5" stroke-dasharray="4,3" marker-end="url(#arr61r)"/>
  <text x="150" y="146" font-size="11" fill="#993C1D">BusRdX/Upgr：作废</text>
  <line x1="508" y1="100" x2="390" y2="100" stroke="#993C1D" stroke-width="1.5" stroke-dasharray="4,3" marker-end="url(#arr61r)"/>
  <text x="449" y="92" text-anchor="middle" font-size="11" fill="#993C1D">BusRd</text>
  <polyline points="600,80 600,48 116,48 116,170" fill="none" stroke="#993C1D" stroke-width="1.5" stroke-dasharray="4,3" marker-end="url(#arr61r)"/>
  <text x="358" y="42" text-anchor="middle" font-size="11" fill="#993C1D">BusRdX</text>
  <polyline points="140,170 140,64 490,64 505,80" fill="none" stroke="#185FA5" stroke-width="1.5" marker-end="url(#arr61b)"/>
  <text x="315" y="58" text-anchor="middle" font-size="11" fill="#185FA5">PrRd（BusRd，全网无副本）→ E</text>
  <line x1="80" y1="348" x2="112" y2="348" stroke="#185FA5" stroke-width="1.5"/>
  <text x="120" y="352" font-size="11" fill="#444441">本核请求（PrRd / PrWr）</text>
  <line x1="300" y1="348" x2="332" y2="348" stroke="#993C1D" stroke-width="1.5" stroke-dasharray="4,3"/>
  <text x="340" y="352" font-size="11" fill="#444441">总线侧事务（他核触发，需侦听 / 目录路由）</text>
</svg>

## 6.5 目录协议：把广播换成查表

总线侦听的天花板是核数。大规模系统的解法是**目录（directory）**：为每块（通常以 cache 行粒度，按 DRAM 地址组织）维护一份记录——状态 + 哪些核持有副本。写者只向目录查询，目录再**点对点**地给持有者发无效化/取数消息。三种实现：

- **全位目录（bit-vector）**：每块 N 位（每核 1 位）+ 状态位。精确、查找 O(1)；存储开销 = 块数 × N 位——32 GB 主存、64 B 块 → 5 亿块 × 8 核 × 1 bit ≈ 500 MB 目录，核越多块越多越吓人。
- **有限指针目录（limited pointer，≈Dir_i B）**：只存 i 个指针（典型 i=4）。超过 i 份共享时溢出处理：退化为广播，或强制多出的核作废（降低共享度）。省存储，换复杂度。
- **链式目录（chained）**：持有者串成链表，目录只存链头；无效化沿链传播。存储最省，但延迟与链维护复杂。

**现代实践**：中规模（8～32 核）用"片上目录 + 侦听过滤器"混合——目录只记录"可能持有"的核集合（过滤掉不可能的），消息仍走片上网络；代表如 AMD 的 L3 目录（配合第 5 章的 NINE L3）、Intel mesh 的目录。**侦听过滤器本质就是"目录信息存一份在互连边上"的变体**。

## 6.6 真实处理器：Intel MESIF 与 AMD MOESI

- **Intel（MESIF）**：MESI + **F（Forward）**。干净的 S 态副本可能有几十份，若别核来读、大家都是 S，谁供数？广播争抢或都等 DRAM 都不优雅。MESIF 规定：同一时刻**只有一个** S 态副本升级为 F，由它负责响应供数、供完交出 F。QPI/UPI 互连上的一致性协议即基于此（F 态在 Intel Nehalem/QPI 时代的公开资料中即有描述）。
- **AMD（MOESI + 目录）**：Zen 系列私有 L2 之间靠 L3 内的目录协调——L3 分片既是"受害者缓存收容所"（配合互斥/非包含策略，见 5.2）又是目录所在。脏数据可以在核间以 O 态长期流转，不惊动 DRAM。
- **Arm DSU（DynamIQ Shared Unit）**：集群内 L3 一致域 + 内置侦听过滤器，大/小核（第 13 章）在同一域内享受 MESI 类语义；域外设备经一致性互连（CCI/CMN）以 I/O 一致性接入——这正是第 13～14 章异构故事的入口。

## 常见误区与注意事项

1. **一致性 ≠ 内存模型**：一致性管"单地址的写序"，内存模型（第 14 章）管"不同地址访问的全局可见顺序与重排许可"。两者常被混为一谈。
2. **MSI 的 S 态不保证数据最新于 DRAM**——M 态写回之前，DRAM 是旧的；S/O 态副本可能比 DRAM 新。别用 DRAM 当真相之源，**真相在协议状态机里**。
3. **以为 BusUpgr 之后别的核"立刻"知道**：无效化是异步消息，需要确认（ack）后写者才能确信独占——协议实现里有微妙的时序窗口。
4. **把 E 当成"我独占所以别人不能有"**——E 的语义是"干净且唯一"，它是缓存态不是权限锁；OS/页表并不因此获得原子性。
5. **目录溢出不当回事**：有限指针目录在"全核共享"负载（如自旋锁、全局计数器）下频繁溢出，性能断崖——要测最坏共享度。
6. **一致性缺失是第 4 种缺失（4C 之一）**：核间无效化导致的缺失既非容量也非冲突——分析多核缺失率时要单独归类（perf 的相应计数器）。

## 本章小结

- 一致性 = 写传播 + 写串行化；多核私有 cache + 写回策略必然要协议伺候。
- 写无效以一次广播换后续安静，成为绝对主流；写更新只在特定读多写少场景有意义。
- MSI 是基线状态机；E 省独占写的总线事务（MESI），O 省脏共享的 DRAM 写回（MOESI），F 解决"多副本谁供数"（MESIF）。
- 总线侦听靠广播与天然全序，扩展性差；目录用显式记录换点对点消息，三种目录是存储与复杂度的谱系；现代设计混合使用（目录+侦听过滤器）。
- 一致性是第 14 章异构协议（CHI/CXL）与第 17 章"一致性盲区"误区的地基。

## 思考题与练习

**1.（分析题）** 两核 MESI，初始双方 I。序列：A 写 x；B 读 x；B 写 x；A 读 x。逐步列出每次操作后 A/B 的状态与发出的总线事务。
*（参考：A:M；B 读→A:S,B:S；B 写→A:I,B:M；A 读→A:S,B:S（或 B:O in MOESI）。）*

**2.（概念题）** 解释 MESI 的 E 态为什么能"静默升级"为 M，而 S 态必须发总线事务。
*（要点：E 保证全网唯一副本，写它不可能违背写传播；S 可能存在他核副本，必须先无效化。）*

**3.（计算题）** 64 GB 主存、64 B 块、64 核。全位目录每块需要多少位？总目录容量？若用 4 指针有限指针目录，每块多少位，总容量？
*（参考：全位 64+2=66 bit/块，共 1G 块 → ≈8.25 GB；4 指针 ≈4×6+2=26 bit → ≈3.25 GB。）*

**4.（分析题）** 两个核乒乓写同一地址（伪共享 false sharing：同行的两个不同变量）。在写无效 MESI 下描述总线上发生什么，性能表现如何，并给出软件侧的两种修法。
*（要点：行粒度无效化乒乓——BusUpgr/RdX 风暴；修法：变量按 cache 行对齐分开放、缩小共享范围/改用每核局部变量再合并。）*

**5.（设计题）** 你在设计 16 核 SoC 的一致性方案：核间为片上 mesh。选择总线侦听还是目录？目录放哪一级、用哪种目录？说明带宽与存储的取舍。

## 拓展阅读

1. H&P, *A Quantitative Approach*, 6th ed., 第 5 章 5.2 节——一致性协议的标准教科书论述（含 MSI/MESI 完整状态图）。
2. Sorin, Hill & Wood, *A Primer on Memory Consistency and Cache Coherence*, 2nd ed., Morgan & Claypool（免费电子版）——一致性 + 内存模型的最佳系统教材，本章与第 14 章的共同母本。
3. Censier & Feautrier, "A New Solution to Coherence Problems in Multicache Systems," IEEE TC 1978——目录思想的原始论文。

---
---

# 第 7 章 Cache 设计中的性能、功耗与面积权衡

## 本章导读

前五章攒齐了一整套"机制弹药"：映射、替换、写策略、多级、一致性。本章回答设计者最终要回答的问题——**这些参数到底定成多少？** 我们把第 2 章的五维权衡坐标（容量/延迟/带宽/能耗/面积）落到三个具体层面：参数敏感性（各参数怎么影响缺失率/延迟/功耗）、低功耗技术（怎么让 cache 少吃饭多干活）、关键路径与时序（4 周期命中预算花在哪），最后学习用 CACTI/McPAT 把这些直觉变成数字。本章是第二部分的收官，也是第 16 章设计方法论的预演。

## 学习目标

1. 定性画出容量、块大小、相联度三个参数对缺失率/命中延迟/功耗的影响趋势曲线，并指出收益递减点；
2. 解释 bank 划分、路停机（way gating）、drowsy 等低功耗技术的机制与代价；
3. 拆解一次 L1 load 的关键路径，解释 VIPT 的页内偏移约束及其对 L1 容量/相联度的限制；
4. 用 CACTI 对给定配置估算延迟/面积/能耗，并解读输出；
5. 对比嵌入式与高性能 CPU 的 cache 设计目标函数差异。

## 核心概念

| 术语（中文） | 英文 | 一句话定义 |
|---|---|---|
| 参数敏感性 | parameter sensitivity | 输出指标随输入参数变化的斜率与拐点 |
| 收益递减 | diminishing returns | 指标改善随投入增加而放缓的现象 |
| 组划分 | banking | 把阵列切成多个独立 bank，按地址激活其一 |
| 路停机 | way gating / shutdown | 整路断电或降低活动度以省功耗 |
| 昏睡缓存 | drowsy cache | 行级降压保持、按需唤醒的低漏电技术 |
| 虚拟索引物理标记 | VIPT | index 用虚拟地址低位、tag 用物理地址的 L1 组织 |
| 关键路径 | critical path | 决定周期时间的最长时序路径 |
| 访问端口 | port | 阵列可并行服务的一套独立读写口 |
| CACTI | — | HP 实验室的 cache 面积/延迟/功耗分析器 |
| McPAT | — | 整芯片级功耗面积时序估算工具 |
| 帕累托前沿 | Pareto frontier | 无一维可再改善而不损另一维的设计点集合 |

## 7.1 参数敏感性：三大旋钮各自买什么、花什么

**容量**：缺失率随容量近似按幂律下降（工作集主导），从 16→32 KB 可能砍掉一半缺失，64→128 KB 也许只再降 20%；而命中延迟随容量上升（字线更长、位线更多、译码更深），L1 从 4 周期变 5 周期的代价是**所有**访存慢 25%。所以 L1 容量的选择是"缺失率收益 vs 全员延迟税"的精确权衡——这也是 Apple 敢用 128 KB L1（按 128 B 块、8 路的逆向解读，index+offset = 7+7 = 14 位，恰好压在其 16 KB 大页的 14 位页内偏移上）而 Intel 停在 32～48 KB 的分野。

**块大小**：大块（64→128 B）摊薄每次缺失的固定开销、吃满 DRAM 行缓冲的连续性——空间局部性好的负载缺失率降；但小局部性负载下大块搬运浪费带宽（用不到的部分白搬），且行数变少、冲突与污染加剧。峰值带宽与缺失率的乘积（**缺失流量 = 缺失率 × 块大小 × 访存速率**）存在 U 形谷底，主流停在 64 B，是 DRAM 突发长度、总线宽度与污染折中的结果。

**相联度**：1→2→4→8 路削掉的是冲突缺失（收益集中在前几档，见 3.4 节），8 路以上通常只剩 1～2 个百分点；而代价是并行比较器、N:1 多路器、替换状态位——直接顶在命中延迟的关键路径上。

**一个"读表"习惯**：看到任何参数翻倍，立刻问三句——缺失率降多少？命中延迟涨多少？面积功耗涨多少？三答案齐了才叫做了权衡。

## 7.2 低功耗设计：让 cache 少吃饭多干活

SRAM 的功耗分两块：**动态**（每次访问的充放电，∝ 活动的位数 × V²）与**静态/泄漏**（漏电流，∝ 通电的单元数 × 时间）。攻击方向相应两类：

- **Banking（组划分）**：把 32 KB 阵列切成 4 个 8 KB bank，按地址低位选 bank——每次访问只激活 1/4 的字线/位线/灵敏放大，动态功耗近似降为 1/4（代价：bank 间地址映射要避免热点、跨 bank 的一致性管理略复杂）。这是**默认人人都做**的优化。多 bank 同时也是带宽的手段（每周期可并行服务多个不同 bank 的请求）——同一个结构，性能与功耗双收。
- **路停机（way gating / dynamic way sizing）**：检测负载冷热（如缺失率低、或路计数器不活跃），把 N 路中的若干路整块断电/时钟门控，等效"动态缩小容量"。研究（Albonesi 等的 way shutdown 系列）显示典型负载下可省可观泄漏而性能损失很小；代价是状态保存/恢复逻辑与激进断电带来的冷启动缺失。
- **Drowsy cache**：不断电，而是把闲置行的供电降到保留态（约 0.7V），读之前需唤醒（一拍延迟）。行级粒度省泄漏，但唤醒时机难掐；作为学术方案影响深远，商用实现多被工艺进步与电源门控替代。
- **读路径省电**：L1 常用单端读/8T 单元（位线不预充满摆幅）、先 tag 比对再激活命中路的数据阵列（way-selection gating，避免读出所有路再丢 N−1 路的数据）。

**心法**：功耗优化的本质是**降低活动性**（少翻转、少激活）与**降低通电面积时间积**（不用就断/降压），一切技术都是这两句的变奏。

## 7.3 关键路径分析：4 周期命中预算的花法

一次 load 的流水（以 4 周期 load-to-use 的高性能核为例，周期划分示意）：

```
周期1: TLB 查找（或与阵列访问并行）；生成 index
周期2: tag 阵列 + 数据阵列并行访问（同 index 驱动）
周期3: N 路 tag 并行比较 → 命中路选择；数据阵列读出
周期4: 对齐/符号扩展/旁路转发 → 数据进 ALU 或寄存器
```

每一环都可能成为关键路径：TLB 容量、阵列的 RC 延迟（容量敏感）、比较器宽度（tag 位数敏感）、N:1 多路器深度（路数敏感）。**VIPT 的页约束**在此显形：index 取自虚拟地址低位（为了不等 TLB 就启动阵列访问），而 tag 必须是物理地址（一致性按物理地址）——两者只对"虚拟低位 = 物理低位"的位段成立，即 **index + offset ≤ 页内偏移位数**。4 KB 页 → 12 位；32 KB/64 B/8 路 = 6+6=12 位恰好压线 ✓；64 KB/64 B/8 路 = 6+7=13 位越线 ✗——要么升相联度（Intel 的经典解法：Prescott 把 L1D 从 8 KB/4 路升到 16 KB/8 路，维持 6+6=12 压线 ✓），要么接受两周期 L1，要么等大页普及（Apple M1 用 16 KB 页把上限抬到 14 位，128 KB/128 B/8 路正好放下）。**这一个约束解释了过去二十年 L1 配置表里一半的"怪现象"**。

## 7.4 CACTI 实操：把直觉变成数字

CACTI（HP Labs 起源，现开源维护）输入一组配置——工艺（如 32 nm）、容量、块大小、相联度、bank 数、端口数、目标（cacti 的优化目标选项）——输出面积、各路径延迟、每次访问的动态能耗、泄漏功耗、周期频率建议。

**示例解读**（32 nm、32 KB、8 路、64 B、单读口，典型输出量级）：

| 输出项 | 量级 | 对应直觉 |
|---|---|---|
| 面积 | ~0.1–0.2 mm² | 能塞进核旁，占核面积 5–10% |
| 访问延迟 | ~0.3–0.5 ns | 4 周期 @3GHz 的预算内（含比较/选择） |
| 动态能耗/读 | ~10–50 pJ | 占核每次访问能耗的可观份额 |
| 泄漏 | 随工艺急剧上升 | 先进工艺下与动态同量级 |

用法的正确姿势是**跑对比而不是信绝对值**：同一工艺同条件下，扫"32 KB 4 路 vs 32 KB 8 路 vs 64 KB 8 路"三组，看延迟差多少、面积差多少，再结合缺失率数据（来自模拟器，第 16 章）合成决策——CACTI 管物理，模拟器管行为，二者合璧。McPAT 则以类似机制向上打包出整芯片的功耗面积模型（喂 gem5 的配置即可）。

## 7.5 案例研究：嵌入式 vs 高性能的目标函数

| 设计点 | Arm Cortex-M7（嵌入式） | Intel Skylake（高性能） |
|---|---|---|
| L1D | 16 KB，4 路，32 B 块 | 32 KB，8 路，64 B 块 |
| 命中目标 | **1 周期**（单周期核） | 4 周期 load-to-use |
| 端口 | 1–2 | 3 读 2 写 ×256b（供 3 个 AGU） |
| 首要目标 | 能效、确定性、面积 | IPC、带宽 |
| 哲学 | "小而确定"——缺失直接访慢速 SRAM 也可接受 | "大而吞吐"——缺失交给 L2/L3 预取机器兜底 |

两列都是"正确设计"，差别只在目标函数：嵌入式用 MHz/W、MHz/mm² 说话；高性能用 IPC 说话。**读任何芯片的 cache 配置时，先问它的目标函数是什么**——这是把全书知识串成体系的关键一问。

## 常见误区与注意事项

1. **把"容量翻倍缺失率减半"当线性规律**——收益递减是常态，决策要看拐点不看斜率起点。
2. **算带宽只数缺失次数**：缺失流量 = 次数 × 块大小，加大块后次数降但每次更贵，必须乘起来看。
3. **VIPT 约束意识缺失**：随手设计 48 KB/64B/4 路（6+7=13>12）的 L1 还想单周期命中，物理上不自洽。
4. **低功耗技术当免费午餐**：banking 要防热点、way gating 有冷启动缺失、drowsy 有唤醒延迟——每一项都要连着性能账一起算。
5. **CACTI 数字当圣经**：绝对值随工艺假设浮动很大，其价值在**同口径对比**。
6. **忽略端口成本**：每加一个读写口，位线/字线负载近似加倍，面积与延迟双涨——高性能核的三端口 L1 是花大钱买的。

## 本章小结

- 三大旋钮的账本：容量买缺失率花延迟；块大小买空间局部性花带宽与污染；相联度买冲突缺失花比较与选择延迟。
- 功耗心法两条：降活动性（banking、路选择门控）与降通电面积时间积（way gating、drowsy、断电）。
- 关键路径 = TLB →（VIPT 并行）阵列 → 比较选择 → 转发；VIPT 的 index+offset ≤ 页内偏移约束框死了传统单周期 L1 的容量/相联度组合。
- CACTI/McPAT 提供"同口径物理对比"，与行为模拟器配合完成设计闭环。
- 嵌入式与高性能的 cache 差异源于目标函数差异——先问目标函数再读参数表。

## 思考题与练习

**1.（计算题）** 4 KB 页、64 B 块。求单周期 VIPT L1 在 4 路、8 路、16 路下的最大容量；若要 64 KB 单周期，给出三种可行改造及各自代价。
*（参考：4 路 64 KB？不——index+off≤12：4 路时 index≤6 → 容量 ≤ 2⁶×4×64 B=16 KB；8 路 ≤32 KB；16 路 ≤64 KB。改造：两周期 L1、用大页/后备方案、或双 bank 分拍。）*

**2.（分析题）** 某负载缺失率随块大小：32 B→6%，64 B→4%，128 B→3.5%。访存率 1 G 次/s。分别计算三种块大小的缺失流量（GB/s）。若 DRAM 带宽 25 GB/s，选哪个？
*（参考：1.92 / 2.56 / 4.48 GB/s——但还要算上块内有用比例；纯流量角度 32B 最省，综合 64B 通常胜出。）*

**3.（概念题）** 解释 way-gating 为什么在"工作集小"的负载下几乎免费，而在"工作集贴近全容量"时反而有害。
*（要点：冷负载断路不影响命中；热负载断路等于人为制造容量缺失。）*

**4.（工具题）** 用 CACTI 对 32 KB/8 路/64 B 与 64 KB/8 路/64 B（同工艺同端口）各跑一组，记录面积/延迟/动态能耗，写一段 100 字的权衡结论（若暂时没有环境，写出你预期的方向与依据）。

**5.（设计题）** 给定预算：L1D 面积 ≤0.15 mm²@32 nm、命中 ≤2 周期 @2 GHz、目标负载工作集 40 KB。给出你的容量/相联度/块/bank 配置与完整论证（缺失率假设可自定，须写明依据）。

## 拓展阅读

1. Moinuddin K. Qureshi & Yale Patt, "The Impact of Cache Energy and Latency on the Microarchitecture," 是把本章权衡定量化的代表文献之一；更系统地读 H&P 第 2 章。
2. S. Wilton & N. Jouppi, "CACTI: An Enhanced Cache Access and Cycle Time Model," IEEE JSAC 1996——工具的原始论文（现代版本参数已大变，思想未变）。
3. K. Flautner et al., "Drowsy Caches: Simple Techniques for Reducing Leakage Power," ISCA 2002——低功耗 cache 的经典论文。

---
---

# 第 8 章 NPU 计算模式与存储需求分析

## 本章导读

从本章起进入第三部分——另一条技术路线的主场。前 7 章的 cache 建立在"赌局部性"上；NPU 设计师看了一眼神经网络负载，发现**根本不用赌**：卷积的循环结构在编译期完全可知，访问模式规整得像阅兵。于是他们把管理权从硬件收回软件，用 scratchpad memory（SPM）替换 cache。本章先解剖神经网络算子的数据流特征，量化"三类数据各自的复用机会"，然后论证 SPM 路线的三大理由（可预测性、能耗、实时性），最后给出典型 NPU 的存储层次模板。本章是第 9～12 章的总纲。

## 学习目标

1. 描述卷积、全连接、池化、激活四类算子的访存模式与计算/访存比特征；
2. 对给定卷积层参数，计算输入/权重/输出各自的复用次数与总搬运量（理想最少 DRAM 流量）；
3. 从可预测性、能耗、实时性三方面论证"NPU 为什么偏爱 SPM"；
4. 默画典型 NPU 的三级存储层次模板并说明每级职责；
5. 计算一个算子的算术强度（AI = FLOPs / Bytes）并判断其受计算还是带宽限制。

## 核心概念

| 术语（中文） | 英文 | 一句话定义 |
|---|---|---|
| 神经网络处理器 | NPU (Neural Processing Unit) | 面向神经网络算子的专用加速器统称 |
| 卷积 | convolution | 滑窗加权求和，CNN 的核心算子 |
| 全连接层 | FC (fully-connected) | 矩阵-矩阵/向量乘 |
| 深度可分离卷积 | depthwise separable conv | 逐通道卷积 + 1×1 卷积的组合（MobileNet） |
| 特征图 | feature map (IFM/OFM) | 输入/输出特征图张量 |
| 复用 | reuse | 同一数据参与多次计算的比例 |
| 算术强度 | arithmetic intensity (AI) | 每搬运一字节对应多少次运算 |
| 屋顶线模型 | Roofline model | 用 AI 判断计算/带宽瓶颈的图模型（第 12 章展开） |
| 统一缓冲 | unified buffer | TPU 中片上 SRAM 的名称（作输入输出池） |
| 脉动阵列 | systolic array | 数据在 PE 网格中节拍流动的计算阵列 |
| 部分和 | partial sum (PSUM) | 输出通道累加过程中的中间值 |
| 描述符驱动 | descriptor-driven | NPU 按 DMA 描述符序列自动执行 |

## 8.1 神经网络算子的数据流特征

**卷积**（CNN 主体）：输入 `C×H×W`，`Cout` 组 `Kh×Kw×C` 的卷积核，滑窗计算输出 `Cout×H'×W'`。本质是七层嵌套循环（批、输出通道、输出坐标 y/x、核内 ky/kx、输入通道 c）。访存特征：**滑窗**——同一输入像素被相邻多个输出窗口反复使用（空间重叠），同一权重扫过整幅图，同一输出被 C 次乘加累加。这是所有算子中"可复用性"最高的一类。

**全连接（FC）**：`Y[M] = W[M×K] × X[K]`。矩阵乘：权重只随批次复用，输入/输出流过。特征是"权重大、一次性"——批量越大权重复用越高（训练时 batch 维度救了带宽）。

**池化（pooling）**：2×2 或 3×3 窗口取 max/avg。无权重、访存/计算比极高——**纯带宽型算子**，存储设计的好坏直接决定它快不快。

**激活函数/逐元素操作**（ReLU、scale、加残差）：每个元素读一次写一次，零复用。同样是带宽型，理想优化是**与相邻算子融合**（fusion）：上一算子的输出不过 DRAM/不过 SPM 大循环，直接流进激活再流进下一层——这是编译器（第 12 章）最重要的省带宽手段。

**小结**：神经网络 = 少数几个**极度规整、循环结构已知、无数据依赖分支**的算子的排列组合。这正是它对"可预测性"友好的根源。

## 8.2 卷积中的三类数据与复用机会

以标准卷积层为例（C=Cin 输入通道，H×W 特征图，Cout 输出通道，K×K 核，stride=1，忽略边界效应）：

- **权重复用**：每个权重参与 `H×W` 次乘加 → 复用次数 = H·W；
- **输入复用**：每个输入像素被 `Cout × K²` 次乘加使用 → 复用次数 = Cout·K²；
- **输出复用**：每个输出元素累加 `C·K²` 次后写出 → 写出前的驻留复用 = C·K²。

**数字例**：`C=256, Cout=256, H=W=56, K=3`（ResNet 典型层）：

```
输入  : 256×56×56×1B(int8) ≈ 0.8 MB
权重  : 256×256×3×3×1B ≈ 0.59 MB
输出  : 256×56×56×1B ≈ 0.8 MB
乘加数: 256×256×56×56×3×3×2 ≈ 3.7 GFLOP（×2 为乘加折算；即 1.85 G 次 MAC）
算术强度 AI ≈ 3.7×10⁹ ÷ (0.8+0.59+0.8)×10⁶ ≈ 1700 FLOP/B
```

**读法**：每从 DRAM 搬 1 字节进来，能换约 1700 次运算——只要片上缓存装得下（这层总数据 <2.2 MB，1～2 MB SPM 即可全驻留），这一层就是纯计算受限，DRAM 几乎闲着。**这就是 NPU 设计的黄金公式：把复用发生在片上，DRAM 只见过路客一次**。

对照**深度可分离卷积**（MobileNet）：depthwise 部分每通道只乘一个 `K×K` 核——权重复用骤降（每权重仅 H·W 次，但输入复用只剩 K²），AI 掉到几十 FLOP/B 量级，**带宽成为瓶颈**——这解释了为什么 MobileNet 类网络"计算省了、带宽没省多少"，也解释了第 12 章为什么它的优化思路与 ResNet 完全不同。

## 8.3 为什么 NPU 常用 SPM 而非 Cache

回到第 2 章 2.3 节的两种哲学，现在用 NPU 视角逐条算账：

1. **可预测性**：CNN 的循环结构编译期已知——哪个 tensor 何时用、用多久、何时换，全部可以静态规划。cache 的"运行时猜测"在这里是多余的奢侈品：**猜得到的东西不需要赌**。SPM 让编译器把数据搬运排成确定的时间表（第 9 章 DMA 调度、第 10 章 tiling）。
2. **能耗**：cache 每次访问都要读 tag 阵列 + 并行比较 + 路选择，还要维护替换状态；SPM 的访问就是普通 SRAM 读。同容量下 SPM 每次访问能耗显著更低、且**没有缺失**（不存在"命中 1ns/缺失 100ns"的双态延迟）。对能效敏感的边缘 NPU，这笔账直接进产品竞争力。
3. **实时性**：cache 的缺失是概率事件，最坏情况延迟不可控；自动驾驶等硬实时场景要求**可认证的最坏执行时间（WCET）**——确定延迟的 SPM 天然友好，cache 需要"全部按缺失算"的悲观分析。
4. **面积**：省掉 tag 阵列与比较逻辑，同等硅面积能换更多有效容量（第 2 章算过 tag 税 ≈ 4%；加上比较器与替换逻辑，总开销更高）。

**代价也要摆在桌面上**：软件栈（编译器/驱动）复杂度暴涨；负载一旦不可预测（稀疏、动态形状、控制流），静态调度失灵——所以第 11 章会看到"SPM + 小 cache"的混合架构回归。**没有银弹，只有负载适配**。

## 8.4 典型 NPU 的存储层次：模板

**图示化描述**（通用模板，几乎可套用所有商用 NPU）：

```
┌────────────────────────── 芯片 ──────────────────────────┐
│  DRAM 控制器 ←── DMA 引擎（多通道，描述符驱动）          │
│        ↑↓                                              │
│  片上 SPM（几百 KB ~ 几 MB SRAM）                        │
│   ├─ 输入缓冲 (Input Buffer / Unified Buffer)           │
│   ├─ 权重缓冲 (Weight Buffer，常带压缩/解压单元)         │
│   └─ 输出缓冲 (Output Buffer / PSUM 累加器)             │
│        ↑↓                                              │
│  PE 阵列（每 PE：MAC + 数 KB 寄存器/局部 FIFO）           │
└──────────────────────────────────────────────────────────┘
```

三级职责（PE：处理单元 Processing Element，每单元核心是一颗 MAC 乘加器 Multiply-Accumulate）：**PE 寄存器**承接节拍级复用（脉动阵列内的流动，第 10 章）；**片上 SPM** 承接 tile 级复用（8.2 节的黄金公式在这里发生）；**DRAM** 只承担"每层/每 tile 数据过境一次"。DMA 引擎是 SPM 的生命线——它必须能被编程成与计算**异步并行**的搬运流（第 9 章双缓冲、第 15 章全系统调度）。

对比第 1 章的 CPU 金字塔：层级数量相近（三级 vs 五级），但**管理权完全反转**——CPU 层次由硬件协议自动管理，NPU 层次由编译器生成的 DMA 时间表显式管理。两种文明在同一颗 SoC 上如何共存，正是第四部分的主题。

## 案例/示例：给一个真实层算"搬运预算"

沿用 8.2 的层（0.8+0.59+0.8 ≈ 2.2 MB）：设 NPU 有 1 MB SPM、8 TOPS 算力、25 GB/s 有效 DRAM 带宽。

- 若 SPM 足够（≥2.2 MB）：本层耗时 ≈ 3.7 GFLOP ÷ 8 TOPS ≈ 0.46 ms，DRAM 流量 2.2 MB（进出各一次）→ 带宽占用 2.2MB/0.46ms ≈ 4.8 GB/s——**计算受限**；
- 若 SPM 只有 256 KB：必须 tile，输入/输出被分块重复搬运（具体次数取决于 tiling 策略，第 10 章算），DRAM 流量可能涨 3～10 倍 → 带宽需求 15～48 GB/s，一旦超过 25 GB/s 即 **带宽受限**，PE 大量空转。

同一颗 NPU，同一层，**存储容量与 tiling 的选择决定它是计算受限还是带宽受限**——这就是"存储决定 NPU 性能上限"的最短证明。

## 常见误区与注意事项

1. **拿 CPU cache 直觉套 NPU**：SPM 没有"命中"概念——数据要么在（因为你搬了），要么不在（因为你没搬），没有运气成分。
2. **只算计算量不算搬运量**：TOPS 数字好看，带宽跟不上就是空转；评估 NPU 永远先算 AI（FLOP/Byte）。
3. **认为 NPU 不需要任何 cache**：控制流、稀疏索引、动态形状负载会让纯 SPM 设计抓瞎（第 11 章展开）。
4. **忽略激活算子的带宽成本**：逐元素算子 AI≈2 FLOP/B（读一写一），不融合就是白付一次全图过境的带宽。
5. **把"复用次数"与"省下来的带宽"直接画等号**：复用能否兑现取决于片上容量与调度——放不下的复用是纸面上的复用。
6. **忘记批维/多任务并发**：多个模型流水线并发时，SPM 是要抢的稀缺资源（第 15 章 QoS）。

## 本章小结

- 神经网络算子 = 规整循环 + 已知数据依赖 → 编译期可静态规划搬运 → SPM 路线的合法性来源。
- 卷积三类数据各有复用公式；ResNet 典型层 AI ≈ 1700 FLOP/B，驻留片上即计算受限；MobileNet 的 DW 卷积复用坍缩 → 带宽受限。
- SPM 四大理由：可预测（不用赌）、能耗（无 tag 税）、实时（确定延迟）、面积（同容量更省）；代价是软件栈复杂与不规则负载失灵。
- NPU 存储模板：PE 寄存器（节拍级）→ SPM（tile 级）→ DRAM（过境一次），DMA 是生命线。
- 存储与 tiling 的选择直接翻转计算/带宽受限属性——第三部分后续四章都是这句话的展开。

## 思考题与练习

**1.（概念题）** 池化与 ReLU 的算术强度大约是多少 FLOP/B？这对其存储优化策略意味着什么？
*（参考：≈1–4 FLOP/B；策略=融合进相邻算子、避免单独过一次存储层次。）*

**2.（计算题）** 卷积层 `Cin=64, Cout=128, H=W=112, K=3, int8`。计算输入/权重/输出字节数、乘加次数、AI。若 SPM 为 512 KB，数据能否全驻留？
*（参考：输入 0.8 MB、权重 73.7 KB、输出 1.6 MB；MAC ≈ 0.92 G（即 1.85 GFLOP）；AI ≈ 750 FLOP/B；不能全驻留——总 2.47 MB > 512 KB，需 tile（第 10 章）。）*

**3.（分析题）** 从 8.3 节四条理由中任选两条，对比 cache 与 SPM 在该维度上的差异，并给出一个具体数字例子（能耗或延迟）。

**4.（概念题）** 解释"驻留片上即计算受限、驻留不下即带宽受限"这句话的因果链，并指出决定"驻留与否"的三个设计变量。

**5.（设计题）** 你的 NPU 目标网络以 depthwise 卷积为主（MobileNet 类）。在"加大 PE 阵列"与"加大 SPM/带宽"之间如何分配下一版芯片的预算？给出论证框架（不必精确数字）。

## 拓展阅读

1. Vivienne Sze, Yu-Hsin Chen, Tien-Ju Yang, Joel Emer, "Efficient Processing of Deep Neural Networks," Morgan & Claypool 2020（亦有免费综述版）——NPU 数据流与存储分析的系统教材，第 8～12 章共同的理论底座。
2. Norman Jouppi et al., "In-Datacenter Performance Analysis of a TPU v1," ISCA 2017——从存储视角读 TPU，能看到 24 MB 统一缓冲的每次出场。
3. Shafique et al., "A Survey of Accelerator Architectures for Deep Neural Networks," 计算机体系结构综述类文章（如 IEEE TM 2021 卷）——按"存储组织"给商用 NPU 分类的好地图。

---
---

# 第 9 章 Scratchpad Memory（SPM）设计

## 本章导读

第 8 章确立了"软件管理"的哲学，本章深入 SPM 的工程内部：**地址空间怎么组织、与 DMA 怎么配合、多 bank 怎么设计、容量怎么定**。这四个问题分别对应四个经典难题：管理复杂度、搬运与计算的重叠、带宽与冲突、面积利用率。学完本章，你应当能把一个"通用 SRAM 块"改造成一台真正能跑神经网络的 SPM 子系统。

## 学习目标

1. 区分 SPM 的三种管理方式（静态/动态/编译器自动）及各自的适用场景与代价；
2. 描述 DMA 描述符的结构，计算双缓冲隐藏搬运延迟的条件；
3. 解释多 bank 交织的目的、bank 冲突的成因与两种规避手段（padding 与布局）；
4. 对给定网络层序列与 SPM 容量，推导 tiling 粒度与 DMA 时间表；
5. 论证 SPM 容量与带宽之间的替代关系及其边界。

## 核心概念

| 术语（中文） | 英文 | 一句话定义 |
|---|---|---|
| 内存映射 SPM | memory-mapped SPM | SPM 占据物理地址空间一段的接法 |
| 局部存储 | local store | Cell SPE 风格的对 SPM 的称呼 |
| 静态分配 | static allocation | 编译期把对象固定映射到 SPM 地址 |
| 动态分配 | dynamic allocation | 运行时 malloc/free 式管理 SPM |
| 覆盖 | overlay | 多个对象分时复用同一 SPM 区域 |
| DMA 描述符 | DMA descriptor | 一次搬运的参数包（源/目的/长度/步长） |
| 双缓冲 | double buffering | A 算 B 搬、B 算 A 搬的乒乓结构 |
| 多缓冲/环形缓冲 | multi/ring buffering | 缓冲区循环使用，深度 >2 |
| Bank 交织 | bank interleaving | 连续地址轮转分布到各 bank |
| Bank 冲突 | bank conflict | 同周期两个请求落在同一 bank |
| 填充 | padding | 给张量维度加冗余元素以错开 bank 映射 |
| 带宽利用率 | bandwidth utilization | 实际吞吐 ÷ 峰值带宽 |

## 9.1 SPM 的地址空间管理：三种模式

**接法**：SPM 通常是内存映射的——物理地址空间里划出一段（如 0x4000_0000 起 1 MB），CPU 与 NPU 的加载/存储单元都能直接寻址。它**不是 cache**：没有 tag、没有缺失，访问永远命中、延迟固定（编译期确知，典型 1 至数个周期，随容量增大）。

- **静态分配**：编译期（或手写代码）决定每个 tensor 的 SPM 地址。最可预测、零运行时开销；但不同层的数据形状各异，固定划分容易"有的区挤死、有的区闲死"。适合形状固定的生产模型。
- **动态分配**：SPM 上跑一个微型分配器（malloc/free/arena）。利用率高、适应动态形状；代价是碎片与不确定性——实时系统要小心。
- **编译器自动管理（自动 SPM 分配）**：编译器分析 tensor 生命区间（liveness），把"下一段要用的数据"自动插入 DMA 并分配地址——学术上有大量成果（如 Saha 等的编译方案），商用 NPU 编译器（第 12 章 TVM/MLIR 路线）已内置此类 pass。这是"SPM 的复杂性由编译器买单"的理想态，也是 SPM 路线能否规模化的关键。
- **覆盖（overlay）**：静态方案的"分时复用"补丁——多个不会同时活跃的对象共享同一地址区，进出靠 DMA 换页。思想来自 1960 年磁带机时代，在 SPM 上还魂。

## 9.2 SPM 与 DMA 的共舞：粒度与双缓冲

**DMA 描述符**（一个典型 2D 搬运）：源地址（DRAM）、目的地址（SPM）、块长、块数、块间步长（stride）——支持把"每行 64 B、共 56 行、行距 4096 B"这种张量切片一次描述完（2D DMA），避免 56 个一维描述符的开销。描述符本身也放在内存队列里，DMA 引擎逐个取用并按依赖关系发起——**这是 NPU "descriptor-driven" 执行模型的核心**。

**粒度选择**：搬运粒度太小 → 描述符开销与 DRAM 随机访问延迟（每次 ~100 ns）占比飙升；太大 → SPM 峰值占用高、计算要等整块到齐。工程甜点通常是"几十 KB 一个 tile"（结合 DRAM 行缓冲与突发长度效率）。

**双缓冲（ping-pong）——本节最重要的一张时间线**：

```
不重叠：  [搬运 T1][计算 T1][搬运 T2][计算 T2]...        总时 = Σ(搬+算)
双缓冲：  [搬运 T1][搬运 T2 | 计算 T1][计算 T2 | 搬运 T3]  总时 ≈ max(搬运, 计算) × n
```

**图 9-1　双缓冲：让搬运与计算重叠**

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 680 310" width="100%" role="img">
  <title>双缓冲时间线：串行执行与搬运计算重叠的对比</title>
  <text x="340" y="24" text-anchor="middle" font-size="14" font-weight="500" fill="#333333">双缓冲时间线：串行 vs 重叠执行</text>
  <text x="80" y="58" font-size="12" font-weight="500" fill="#444441">串行执行：总时间 = Σ（搬运 + 计算）</text>
  <rect x="110" y="66" width="80" height="26" fill="#85B7EB" stroke="#185FA5" stroke-width="0.5"/>
  <text x="150" y="83" text-anchor="middle" font-size="11" fill="#0C447C">搬 T1</text>
  <rect x="194" y="66" width="120" height="26" fill="#97C459" stroke="#3B6D11" stroke-width="0.5"/>
  <text x="254" y="83" text-anchor="middle" font-size="11" fill="#173404">算 T1</text>
  <rect x="318" y="66" width="80" height="26" fill="#85B7EB" stroke="#185FA5" stroke-width="0.5"/>
  <text x="358" y="83" text-anchor="middle" font-size="11" fill="#0C447C">搬 T2</text>
  <rect x="402" y="66" width="120" height="26" fill="#97C459" stroke="#3B6D11" stroke-width="0.5"/>
  <text x="462" y="83" text-anchor="middle" font-size="11" fill="#173404">算 T2</text>
  <text x="536" y="83" font-size="12" fill="#777777">…</text>
  <text x="80" y="150" font-size="12" font-weight="500" fill="#444441">双缓冲（乒乓）：稳态周期 = max（搬运，计算），本例 = 计算时间</text>
  <text x="76" y="180" font-size="11" fill="#444441">DMA</text>
  <rect x="110" y="166" width="80" height="26" fill="#85B7EB" stroke="#185FA5" stroke-width="0.5"/>
  <text x="150" y="183" text-anchor="middle" font-size="11" fill="#0C447C">搬 T1</text>
  <rect x="194" y="166" width="80" height="26" fill="#85B7EB" stroke="#185FA5" stroke-width="0.5"/>
  <text x="234" y="183" text-anchor="middle" font-size="11" fill="#0C447C">搬 T2</text>
  <rect x="278" y="166" width="80" height="26" fill="#85B7EB" stroke="#185FA5" stroke-width="0.5"/>
  <text x="318" y="183" text-anchor="middle" font-size="11" fill="#0C447C">搬 T3</text>
  <rect x="362" y="166" width="80" height="26" fill="#85B7EB" stroke="#185FA5" stroke-width="0.5"/>
  <text x="402" y="183" text-anchor="middle" font-size="11" fill="#0C447C">搬 T4</text>
  <text x="456" y="183" font-size="12" fill="#777777">…</text>
  <text x="70" y="238" font-size="11" fill="#444441">计算</text>
  <rect x="194" y="224" width="120" height="26" fill="#97C459" stroke="#3B6D11" stroke-width="0.5"/>
  <text x="254" y="241" text-anchor="middle" font-size="11" fill="#173404">算 T1</text>
  <rect x="318" y="224" width="120" height="26" fill="#97C459" stroke="#3B6D11" stroke-width="0.5"/>
  <text x="378" y="241" text-anchor="middle" font-size="11" fill="#173404">算 T2</text>
  <rect x="442" y="224" width="120" height="26" fill="#97C459" stroke="#3B6D11" stroke-width="0.5"/>
  <text x="502" y="241" text-anchor="middle" font-size="11" fill="#173404">算 T3</text>
  <line x1="194" y1="160" x2="194" y2="256" stroke="#993C1D" stroke-width="1" stroke-dasharray="3,3"/>
  <text x="200" y="274" font-size="11" fill="#993C1D">T2 搬运开始 = T1 计算开始（乒乓交接）</text>
  <line x1="194" y1="258" x2="318" y2="258" stroke="#444441" stroke-width="1"/>
  <line x1="194" y1="254" x2="194" y2="262" stroke="#444441" stroke-width="1"/>
  <line x1="318" y1="254" x2="318" y2="262" stroke="#444441" stroke-width="1"/>
  <text x="256" y="298" text-anchor="middle" font-size="11" fill="#888888">稳态周期 = 一个计算段（搬运被完全隐藏）</text>
</svg>

A/B 两块缓冲交替：T1 在算时，T2 已经在路上。**隐藏搬运延迟的条件：搬运时间 ≤ 计算时间**（缓冲深度足够时，稳态吞吐由较慢者决定）。若计算 0.1 ms、搬运 0.3 ms，双缓冲也救不了——此时要么增大 tile（提高每次搬运的计算量）、要么加带宽、要么加压缩。多缓冲（深度 3～4）进一步吸收 DRAM 延迟抖动与多任务争抢。

**同步**：DMA 完成 ↔ 计算完成之间需要信号量/事件握手（硬件信号量或事件计数器，第 13 章展开其跨核版本）。

## 9.3 多 Bank SPM：带宽与冲突

**动机**：PE 阵列每周期要吃多个数（如 8×8 阵列每周期要 8 个输入 + 广播权重 + 写 8 个部分和）。单 bank 单端口的 SRAM 一周期只能供一次访问——**bank 数必须 ≥ 每周期并发访问数**。

**交织（interleaving）**：把连续地址轮转分给各 bank（地址低位选 bank），使"顺序读一行数据"天然分散到所有 bank 并行供数——这是对空间局部性负载的最优默认。

**Bank 冲突**：两个并发请求落到同一 bank → 串行化，带宽当场减半起步。**成因常不在地址连续，而在布局**：

- 例：8 个 bank 按地址低位交织（bank = addr[2:0]，即 8 字节粒度），而数据按 16 B 对齐的行存放、每行取同一相位 → 全部落同一 bank；
- 例：转置访问（读列）时 stride 恰为 bank 总宽的整数倍 → 列元素同 bank。

**规避**：(1) **padding**——给行宽加几个填充元素，让 stride 与 bank 数错开素数关系（经典如 FFT 的 ±1 padding）；(2) **bank-aware 布局**——编译器知道访问模式，干脆换一种地址映射（如 swizzle/迷宫式重排，把 (h,w,c) 的存储顺序调转成 (w,h,c) 局部），让热点访问天然错 bank。**冲突检测**：仿真期对每周期并发访问做 bank 号统计即可（第 16 章工具链）。

## 9.4 容量与带宽的权衡：需求随层而变

网络各层对 SPM 的胃口天差地别：浅层特征图大（H×W 大）而权重薄，深层相反；DW 卷积要的是带宽不是容量。三种应对：

1. **统一大池（TPU 路线）**：一块大 SPM 谁需要谁用（软件分配），灵活、利用率均衡；代价：总线/仲裁设计重，峰值带宽受限于总口。
2. **分池专用（输入池/权重池/输出池）**：每池独立端口与带宽，互不干扰，控制简单；代价：比例定死（如 1:1:2），遇到非常规层比例失配就浪费。
3. **容量换带宽**：容量不够 → tile 变小 → 同一数据多次重读 → 带宽需求上升（第 10 章给出精确公式）。**容量与带宽是同一枚硬币的两面**：加 SPM 是花面积降带宽，加带宽是花引脚/功耗降容量——预算永远在这两者间摇摆。

**容量利用率的"过小抖动"与"过大浪费"**：过小 → DMA 频繁、tile 碎、描述符开销与延迟暴露；过大 → 面积与泄漏白白烧钱（第 17 章误区专题）。经验做法：统计目标网络族的驻留需求分布（P50/P90/P99），容量定在能覆盖 P90 的位置。

## 案例/示例：NVIDIA NVDLA 的 SPM 子系统

NVDLA（开源 NPU，文档齐全，极佳的学习标本）的片上 SRAM 是可选组件（典型 512 KB），与片外 DRAM 双数据源可选——每层执行前由编译器决定该 tensor 从哪取。其内存子系统支持 2D DMA（含跨步 gather）、多 bank 交织，且"卷积缓冲"与"数据 RAM"分池。读它的公开文档可以验证本章每个概念的真实形态：描述符格式（第 9.2）、bank 组织（9.3）、双数据源容量策略（9.4）。对照 TPU v1 的 24 MB 统一缓冲（第 10 章），可看出"分池小 SPM + DRAM 直通"与"统一大 SPM"两条路线的真实工程取舍。

## 常见误区与注意事项

1. **"SPM 就是慢一点的寄存器"**——SPM 是可编程地址空间，其设计核心在管理（分配+搬运+同步）而非阵列本身。
2. **双缓冲当万能药**：搬运时间 > 计算时间时双缓冲只救延迟不救吞吐；稳态吞吐 = max(搬, 算)。
3. **bank 冲突靠"多加 bank"解决**——加 bank 解决容量型冲突；布局型冲突（stride 对齐）加再多 bank 也撞，必须 padding/swizzle。
4. **忽视描述符开销**：一维细粒度 DMA 在小 tile 下描述符取用/更新的开销能吃掉 20%+ 性能——用 2D DMA 与描述符批量提交。
5. **静态分配写死比例**：模型一换（如换成 transformer）分池比例全废；除非产品形态固定，优先统一池或可配置划分。
6. **忘了 CPU 也要访问 SPM**：调试、参数写入、小规模数据都靠 CPU 读写 SPM——地址通路与权限（第 13 章 IOMMU）要在架构期留好。

## 本章小结

- SPM 管理三模式：静态（确定）、动态（灵活）、编译器自动（理想态）；overlay 是静态方案的分时补丁。
- DMA 描述符（含 2D 跨步）是搬运的基本单位；双缓冲以 max(搬运, 计算) 决定稳态吞吐；粒度在描述符开销与 SPM 峰值占用间取甜点。
- bank 数 ≥ 每周期并发访问数；顺序交织适配空间局部性；布局型冲突靠 padding/swizzle 解决。
- 统一大池 vs 分池专用是灵活性/带宽的取舍；容量与带宽互为替代（面积 vs 引脚功耗），容量覆盖目标网络族的 P90 需求。
- NVDLA 是本章所有概念的可查证实物；TPU v1 是统一大池路线的极端样本。

## 思考题与练习

**1.（计算题）** 某层计算耗时 0.2 ms/ tile，单 tile 搬运 128 KB，DMA 有效带宽 8 GB/s。判断双缓冲后该层是计算受限还是搬运受限；若要完全隐藏搬运，带宽至少多少？
*（参考：搬运 16 µs < 计算 200 µs → 计算受限；已满足隐藏条件。）*

**2.（设计题）** 8×8 PE 阵列每周期需并发读 8 个输入、写 8 个部分和（输入与输出分池）。给出每池的 bank 数下限与交织粒度建议，并说明什么访问模式仍会冲突。

**3.（分析题）** stride 为 64 B 的 gather 访问落在 8 bank × 8 B 交织的 SPM 上，会发生什么？给出两种修法并评估各自的存储开销。
*（要点：64 B stride 恰为 bank 总宽整数倍 → 全同 bank；padding 改 stride 或改交织粒度/加 swizzle。）*

**4.（概念题）** 比较"统一大池"与"输入/权重/输出三分池"在以下三个场景的表现：标准 CNN、depthwise CNN、多层算子融合。哪个方案整体更稳？

**5.（综合题）** 给定 SPM 512 KB、网络层序列（输入/权重/输出大小分别为 400/100/400、600/200/600、200/50/200 KB），设计一个带双缓冲的 DMA 时间表（文字描述每步谁在算、谁在搬），并指出哪一层是瓶颈。

## 拓展阅读

1. Sze et al., *Efficient Processing of DNNs*, 2020，第 4～5 章——SPM/DMA/数据流的一体化分析。
2. NVDLA 开源文档（github.com/nvdla 与 nvdla.org）——真实 NPU 的 SPM 与 DMA 寄存器级文档。
3. Rajeshwari Banakar et al., "Scratchpad Memory: A Design Alternative for Cache On-chip Memory in Embedded Systems," CODES 2002——SPM vs cache 能耗对比的经典量化论文。

---
---

# 第 10 章 数据流架构与存储适配

## 本章导读

同一个卷积层，把循环怎么摆、让哪类数据驻留在哪一级存储，会得到性能与能耗完全不同的机器——**数据流（dataflow）就是"循环顺序 + 数据驻留策略"的架构化表达**。本章讲四种经典数据流（权重固定/输出固定/行固定/无局部复用）、它们各自对存储层次的索取，然后给出配套的编译侧武器：循环变换（tiling、交换、展开）与 im2col 的存储账本，最后解剖两个实物：Google TPU v1 与 NVIDIA DLA。本章与第 8 章的"复用公式"无缝衔接：**复用公式说机会，数据流说兑现**。

## 学习目标

1. 用"谁驻留 PE、谁流动、存储怎么排"三问描述 WS/OS/RS/NLR 四种数据流；
2. 论证每种数据流适合的算子类型及其对 SPM 容量/带宽的不同索取；
3. 对给定卷积层与 SPM 容量，推导两级 tiling 的分块尺寸与 DRAM 流量公式；
4. 计算 im2col 的内存膨胀倍数并与直接卷积比较带宽/容量开销；
5. 描述 TPU v1 的存储结构与数据通路，指出它是哪种数据流的极致化。

## 核心概念

| 术语（中文） | 英文 | 一句话定义 |
|---|---|---|
| 数据流架构 | dataflow architecture | 以数据驻留与流动方式定义的加速器组织 |
| 权重固定 | WS (Weight Stationary) | 权重驻留 PE，输入流过，部分和流动 |
| 输出固定 | OS (Output Stationary) | 部分和驻留不动，输入权重流过 |
| 行固定 | RS (Row Stationary) | 滤波器行/输入行分维驻留，三类复用同时发生 |
| 无局部复用 | NLR (No Local Reuse) | 什么都不驻留，靠带宽直供 |
| 循环分块 | tiling / blocking | 把大循环切小块塞进存储层次 |
| 循环交换 | loop interchange | 调整循环层次顺序改变访问模式 |
| 循环展开 | loop unrolling | 复制内层循环暴露指令级/数据级并行 |
| im2col | im2col (image to column) | 把卷积展开成 GEMM 的数据重排 |
| 脉动阵列 | systolic array | 数据按节拍在 PE 网格中流动的阵列 |
| 统一缓冲 | Unified Buffer (UB) | TPU v1 的 24 MB 片上 SRAM |
| 权重 FIFO | Weight FIFO | TPU v1 中向阵列供权重的四级缓冲 |

## 10.1 四种经典数据流

**读法说明**：对每种数据流回答三个问题——**谁驻留**（PE 本地保存什么，吃寄存器/局部 SRAM）、**谁流动**（在 PE 间传递什么，吃互连与相邻级存储）、**SPM 排什么**（大池子里放谁、DMA 按什么节奏供）。

- **权重固定（WS）**：权重装载进 PE 后不动（一层算完才换），输入特征图逐元素/逐行流入阵列，部分和沿阵列逐拍累加传递、流出边界时恰好运算完成。**吃存储的方式**：权重复用最大化——每权重从 SPM 到 PE 只走一次；输入要"广播/流动"，对互连规整性要求高。代表：Google TPU v1（脉动式 WS）。最适合卷积这类权重总量小、扫全图的算子。
- **输出固定（OS）**：每个 PE 认领一个输出元素，部分和在 PE 本地累加寄存器里滚雪球，输入与权重成对流过。**吃存储的方式**：部分和不过存储（累加在寄存器）——省了最大的一笔中间流量；输入每元素要送到多个 PE（复用靠阵列扇出）。适合输出规模大、累加链长的层（也是许多小核 NPU 的默认）。
- **行固定（RS，Eyeriss）**：把卷积核按"行"拆开：一行滤波器权重、一行输入、一行部分和分别固定在不同维度的 PE 网格上，三类复用（输入/权重/部分和）**同时**在不同维度发生。**吃存储的方式**：总搬运能耗理论最优（Sze 组的量化：比 WS/OS 再省 1.5～2×），代价是控制与映射复杂度高、通用性弱。代表：MIT Eyeriss（科研标杆）。
- **无局部复用（NLR）**：什么都不驻留，数据从存储直灌计算单元——本质是"存储带宽极大化"路线（相当于把赌注全押在带宽上），可视为脉动阵列的反面（如某些粗粒度向量/GEMM 核近似此型）。价值在于作对照：它揭示了**其它三种数据流本质上都是在用"驻留"换"带宽"**。

**选择速记**：权重大复用高 → WS；部分和压力大 → OS；能耗极致 → RS；带宽便宜计算贵 → NLR。商用 NPU 多为混合体（如按层切换 WS/OS）。

## 10.2 数据流决定存储调度

把 10.1 的三问翻译成 DMA 时间表：

- **WS 的调度**：层开始前 DMA 把整层权重搬进权重池（或权重 FIFO 链）；计算期间 DMA 只需持续供输入流、回收输出流——**流量最小、节奏最稳**，双缓冲最易实现；
- **OS 的调度**：输入按输出分块所需的"窗口集合"搬运（有重叠——同一输入属于多个输出块），权重要反复重播（每个输出块都要全套权重）——**权重流量随 tiling 增多**，权重池要能快速重播；
- **RS 的调度**：按"行条带"供数据，行缓冲深度与滤波器行数对齐，DMA 要维持多行滑窗的在片供给；
- **NLR 的调度**：DMA 就是主角——带宽预算必须 ≥ 阵列峰值吞吐 ÷ AI，否则架构崩塌。

**两级 tiling 的 DRAM 流量公式**：设循环按输出通道、输入通道、空间三个维度分块，分块数分别为 Tm、Tc、Ts（即 Cout 切 Tm 块、Cin 切 Tc 块、H×W 切 Ts 块），则每一类数据的 DRAM 流量 = **该数据总量 × 它"装不下、必须重播"的循环维度的分块数**：

```
DRAM 输入流量 = 输入总量 × Tm      （输出通道分块 → 输入随权重块重读）
DRAM 权重流量 = 权重总量 × Ts      （空间分块     → 权重随空间块重播）
DRAM 输出/PSUM 流量 = 输出总量 × Tc（输入通道分块 → 部分和重读写；驻留则 Tc=1）
```

**读法**：某类数据在哪个循环维度上"被切了块"且循环顺序迫使它跨块重访，流量就乘上该维度的分块数；循环交换（10.3）改变的正是"谁乘上谁"。验证：第 18.2 节案例中权重按 Cout 分 3 块（Tm=3），输入输出各 50 KB 被重读 3 遍 → 该层流量 = (50+50)×3 + 590 = 890 KB，与本公式一致。这正是"循环变换即存储优化"的含义：**改循环顺序 = 改哪份数据要多读几遍**。第 12 章的 tiling 实战会用它做完整数字例。

## 10.3 循环变换：编译器的存储武器

- **tiling（分块）**：把 `for c: for h: for w:` 改成 `for ct: for ht: for wt: for c in ct: for h in ht: for w in wt:`——外三层决定"哪块数据进 SPM"，内三层决定"块内怎么算"。**tile 尺寸 = SPM 需求**：输入 tile、输出 tile、（若不驻留）权重 tile 三者之和 ≤ SPM 容量是硬约束。
- **循环交换（interchange）**：调整 `for c` 与 `for m`（输出通道）的内外关系，改变"权重先换还是输入先换"——直接改写 10.2 的流量公式。编译器搜索空间的主要维度之一。
- **循环展开（unrolling）**：内层展开 ×4 → 每次迭代处理 4 个乘加，寄存器压力上升但暴露并行、摊薄循环开销；也影响 SPM 每周期的供数需求（接第 9 章 bank 设计）。
- **im2col 与直接卷积的账本**：im2col 把每个滑窗的输入展开成矩阵一行（列），卷积变成 `矩阵(Cout×K·K·Cin) × 矩阵(K·K·Cin × H·W)` 的 GEMM。**代价**：输入膨胀 `K²` 倍（每个输入元素被复制进 K² 个窗口）——`3×3` 卷积即 9 倍内存膨胀。**收益**：GEMM 的规整访问让现有 GEMM 核（含脉动阵列、Tensor Core）全速运转，带宽虽涨但全是规则流。**决策**：计算单元吞吐高到值得用 9 倍带宽换（GPU/服务器侧常是），或 SPM/带宽紧张时选直接卷积（边缘 NPU 常是）——不是谁对谁错，是 AI/带宽比的一道算术题。

**数字例（im2col 账本）**：沿用第 8 章层（输入 0.8 MB，K=3）：im2col 后输入矩阵 ≈ 0.8 MB × 9 = 7.2 MB——若 SPM 1 MB，im2col 需要 8 个 tile 轮转而直接卷积可能 1～2 个；但 im2col 每 tile 是纯 GEMM，MAC 利用率 95%+，直接卷积要处理边界与滑窗寻址，利用率 70～85%。收支相抵看具体硬件。

## 10.4 实物解剖：TPU v1 与 NVDLA

**Google TPU v1（2015 披露，ISCA 2017 论文）**——统一缓冲路线的极致：

```
DRAM（8 GB DDR3，约 34 GB/s；注意这是 HBM 普及之前的设计）
  ↕ DMA（隐式，由指令流驱动）
Unified Buffer 24 MB SRAM ←→ 256×256 MAC 脉动阵列(int8) → 累加器 4 MB → 激活/池化/归一化单元 → 回 UB
                    ↑
        Weight FIFO（4 级深，双缓冲）← 权重从 DRAM 压缩格式解出
```

**图 10-1　Google TPU v1 的存储结构与数据通路**

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 680 370" width="100%" role="img">
  <title>TPU v1 存储结构：统一缓冲、Weight FIFO、脉动阵列与累加器</title>
  <defs>
    <marker id="arr101" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
      <path d="M2 1L8 5L2 9" fill="none" stroke="#444441" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
    </marker>
  </defs>
  <text x="340" y="24" text-anchor="middle" font-size="14" font-weight="500" fill="#333333">TPU v1：存储结构与数据通路（权重固定 · 脉动式）</text>
  <rect x="240" y="40" width="200" height="40" rx="8" fill="#B4B2A9" stroke="#5F5E5A" stroke-width="0.5"/>
  <text x="340" y="64" text-anchor="middle" font-size="12" fill="#444441">片外 DRAM（权重 · 激活）</text>
  <rect x="150" y="120" width="250" height="46" rx="8" fill="#B5D4F4" stroke="#185FA5" stroke-width="0.5"/>
  <text x="275" y="147" text-anchor="middle" font-size="12" fill="#0C447C">统一缓冲 Unified Buffer（24 MB）</text>
  <rect x="450" y="120" width="170" height="46" rx="8" fill="#F5C4B3" stroke="#993C1D" stroke-width="0.5"/>
  <text x="535" y="140" text-anchor="middle" font-size="12" fill="#712B13">Weight FIFO ×4</text>
  <text x="535" y="157" text-anchor="middle" font-size="11" fill="#712B13">（双缓冲，解压后入阵）</text>
  <rect x="210" y="210" width="260" height="52" rx="8" fill="#CECBF6" stroke="#534AB7" stroke-width="0.5"/>
  <text x="340" y="232" text-anchor="middle" font-size="12" fill="#26215C">矩阵乘单元 256×256 MAC</text>
  <text x="340" y="250" text-anchor="middle" font-size="11" fill="#3C3489">（int8 脉动阵列）</text>
  <rect x="210" y="296" width="150" height="40" rx="8" fill="#C0DD97" stroke="#3B6D11" stroke-width="0.5"/>
  <text x="285" y="320" text-anchor="middle" font-size="12" fill="#173404">累加器（4 MB）</text>
  <rect x="390" y="296" width="200" height="40" rx="8" fill="#FAC775" stroke="#854F0B" stroke-width="0.5"/>
  <text x="490" y="320" text-anchor="middle" font-size="12" fill="#412402">激活 / 池化 / 归一化</text>
  <line x1="300" y1="80" x2="300" y2="120" stroke="#444441" stroke-width="1.5" marker-start="url(#arr101)" marker-end="url(#arr101)"/>
  <line x1="440" y1="66" x2="520" y2="120" stroke="#444441" stroke-width="1.5" marker-end="url(#arr101)"/>
  <text x="512" y="92" font-size="11" fill="#777777">压缩权重</text>
  <line x1="290" y1="166" x2="290" y2="210" stroke="#444441" stroke-width="1.5" marker-end="url(#arr101)"/>
  <text x="282" y="192" text-anchor="end" font-size="11" fill="#777777">输入逐行流入</text>
  <line x1="500" y1="166" x2="440" y2="210" stroke="#444441" stroke-width="1.5" marker-end="url(#arr101)"/>
  <text x="520" y="196" text-anchor="end" font-size="11" fill="#777777">权重驻留</text>
  <line x1="295" y1="262" x2="290" y2="296" stroke="#444441" stroke-width="1.5" marker-end="url(#arr101)"/>
  <text x="282" y="284" text-anchor="end" font-size="11" fill="#777777">部分和</text>
  <line x1="360" y1="316" x2="390" y2="316" stroke="#444441" stroke-width="1.5" marker-end="url(#arr101)"/>
  <line x1="490" y1="296" x2="400" y2="170" stroke="#444441" stroke-width="1.5" stroke-dasharray="4,3" marker-end="url(#arr101)"/>
  <text x="486" y="240" text-anchor="end" font-size="11" fill="#777777">成品回 UB</text>
  <text x="340" y="360" text-anchor="middle" font-size="11" fill="#888888">数据流：权重经 FIFO 进阵列"驻留"，输入从 UB 流入，部分和沿阵列流动、成品经激活单元回 UB</text>
</svg>

数据流定性：**权重固定（脉动式）**——权重经 FIFO 进阵列后"驻留"一个时间片，输入从 UB 逐行流入、部分和沿列流动、成品从底部流出。存储设计的三个亮点：①UB 一池统管输入输出（容量大，软件分配，呼应 9.4 的统一池路线）；②Weight FIFO 的双缓冲 = 第 9 章双缓冲在"权重通路"上的应用；③4 MB 累加器把 int32 部分和的驻留从阵列里拿出来（输出固定思想的局部借用）——**一台机器里混用多种数据流思想**是常态。

**NVIDIA NVDLA（开源）**——分池可配置路线：可选 512 KB 片上 SRAM、卷积缓冲（weight/activation 分池、可旁路直读 DRAM）、2D DMA（含跨步）、按"每层描述符"驱动。与 TPU 的 UB 路线对照着读，能看见同一张设计空间地图上的两个不同落点。

## 常见误区与注意事项

1. **把"数据流"当单选题**——真实 NPU 常按层混合（卷积走 WS、全连接走类 NLR 的 GEMM），数据流是每层调度决策不是机器国籍；
2. **im2col=落后、直接卷积=先进**——两者是带宽换吞吐的交易，答案随硬件代际变化；
3. **tiling 只盯容量约束**——tile 大小同时决定 DRAM 流量、bank 冲突模式与双缓冲深度，要联立求解；
4. **以为脉动阵列自动省带宽**——它省的是"阵列到存储的端口带宽"（数据在 PE 间流动），外部 DRAM 流量仍由数据流与 tiling 决定；
5. **循环交换当免费的编译器小事**——它是改写流量公式的第一杠杆，手工调优高手与自动调优器（第 12 章）的主战场；
6. **忽略部分和的存储地位**——int32 PSUM 常是 SPM 里最大的隐形住户（比 int8 数据大 4 倍），预算时忘了它就会"装得下 int8 却爆容量"。

## 本章小结

- 数据流 = 驻留策略 + 流动方式；WS 保税重、OS 税部分和、RS 三维同时驻留能耗最优、NLR 全押带宽。
- 数据流决定 DMA 时间表的形状（谁先搬、谁重播、谁双缓冲）；两级 tiling 的流量公式由循环摆法决定。
- 循环变换四件套（tile/交换/展开/im2col）是编译器操作存储的扳手；im2col 以 K² 内存膨胀换 GEMM 规整性。
- TPU v1 = 统一池 + 脉动 WS + 部分和外置累加器的教科书组合；NVDLA = 分池可配置 + 2D DMA 的工程化样本。
- 本章的流量公式与 tiling 推导，正是第 12 章自动调优器要搜索的目标函数。

## 思考题与练习

**1.（概念题）** 用"三问"（谁驻留/谁流动/SPM 排什么）分别描述 WS 与 OS 对同一个 3×3 卷积层的执行过程。

**2.（计算题）** 输入 1 MB、权重 0.5 MB、输出 1 MB 的层，im2col（K=3）后输入膨胀为多少？若 SPM 为 2 MB、GEMM 利用率 95% vs 直接卷积 75%（计算时间按 1/利用率 折算），在"计算 0.4 ms 满负荷"与"带宽 20 GB/s"下比较两种方案总耗时。
*（参考：膨胀后 9 MB；im2col 搬运 9+0.5+1=10.5MB→0.525 ms 搬运；直接卷积流量 2.5 MB→0.125 ms 但计算 0.533 ms；具体权衡留作完整推导。）*

**3.（分析题）** 为什么 RS（行固定）能同时兑现三类复用？画（文字描述）Eyeriss 中"一行滤波器 + 一行输入"的映射来解释。

**4.（设计题）** 给定 SPM 1 MB、层参数 `Cin=64, Cout=128, H=W=112, int8`（数据见第 8 章练习 2），给出一个两级 tiling 方案（通道方向切 Tc）并计算 DRAM 输入流量随 Tc 的变化（Tc=16/32/64 三档）。

**5.（综合题）** 你要在 TPU v1 式架构上跑一个 depthwise 网络（权重复用坍缩）。指出三个性能瓶颈点，并各给一个不改硬件的软件侧缓解。

## 拓展阅读

1. Y.-H. Chen, J. Emer, V. Sze, "Eyeriss: A Spatial Architecture for Energy-Efficient Dataflow for Convolutional Neural Networks," ISCA 2016——RS 数据流与"数据流能耗分析框架"的奠基论文。
2. Norman Jouppi et al., "In-Datacenter Performance Analysis of a Tensor Processing Unit," ISCA 2017——TPU v1 全参数（UB/FIFO/阵列）的一手来源。
3. TVM 文档 "Compute and Schedule" 教程——把本章循环变换在真实编译器里跑一遍的最佳途径。
4. A. Parashar et al., "Timeloop: A Systematic Approach to DNN Accelerator Evaluation," ISPASS 2019；H. Kwon et al., "MAESTRO," MICRO 2019——数据流-存储映射的定量建模工具，可把本章四种数据流的存储开销算成可对比的数字（配套工具 Scale-Sim 适合脉动阵列的周期级模拟）。

---
---

# 第 11 章 NPU 中的片上缓存与混合存储

## 本章导读

前三章把 SPM 路线讲成了"必然选择"，本章来拆这个"必然"的边界：当负载不再规整——稀疏张量的 gather、动态形状、控制流、元数据小对象——纯 SPM 的静态调度会失灵，此时 **cache 反而回到 NPU 里**。本章讨论混合存储架构（SPM 管大块规则数据、cache 管不规则尾巴）、稀疏压缩格式（CSR/CSC）对存储层次的冲击，以及多核 NPU 中 NoC 与存储的交互。可以把本章理解为"第 2～7 章 CPU 武艺在 NPU 国度的回马枪"。

## 学习目标

1. 列出 NPU 中仍需要 cache 的四类场景并解释为什么 SPM 应付不了；
2. 描述一个典型 SPM+cache 混合架构的分工边界与数据分类规则；
3. 解释 CSR/CSC 稀疏格式的访存行为及其对带宽与层次设计的破坏性；
4. 描述结构化稀疏（N:M）与非结构化稀疏在存储友好度上的分野；
5. 说明多核 NPU 中 NoC 的基本职责与存储事务的路由方式。

## 核心概念

| 术语（中文） | 英文 | 一句话定义 |
|---|---|---|
| 稀疏性 | sparsity | 张量中零元素占比 |
| 剪枝 | pruning | 删除（置零）不重要的权重/通道 |
| 行压缩存储 | CSR (Compressed Sparse Row) | 按行存非零元素 + 列索引 + 行指针 |
| 列压缩存储 | CSC (Compressed Sparse Column) | CSR 的列版 |
| 步长索引 | stride-indexed format | 用增量编码非零元位置的轻量稀疏格式 |
| 结构化稀疏 | structured sparsity (N:M) | 每 M 个权重保留 N 个的规则稀疏（如 2:4） |
| 收集 | gather | 按索引数组随机取数的访存模式 |
| 片上网络 | NoC (Network-on-Chip) | 路由器网格构成的片内通信网络 |
| 路由算法 | routing (XY 等) | 报文在 NoC 中的行走规则 |
| 组播 | multicast | 一份数据同时送往多个目的（如权重广播） |
| 零值跳过 | zero skipping | 计算时跳过零元素省下的计算与能耗 |

## 11.1 NPU 何时仍需要 Cache：四类场景

1. **稀疏与 gather**：embedding 查表（推荐系统、NLP 词嵌入）本质是"按索引数组随机取行"——地址运行时才知道，静态 DMA 时间表写不出来。这类访问对 cache 是好负载（表项有热点——流行词/热门商品被反复查），对 SPM 是噩梦。
2. **动态形状与控制流**：早期退出（early-exit）网络、目标检测的 RoI 池化（候选框数量取决于上一阶段输出）、transformer 的变长序列——分支与循环边界运行时才确定。SPM 的描述符流需要"运行时再生成"，复杂度高；cache 天生按需响应。
3. **元数据与小型不规则对象**：算子图调度信息、描述符链、同步变量、模型参数中的 scale/bias 零件——小、散、生命周期乱。给它们专门排 DMA 是杀鸡用牛刀，一个 direct-mapped 小 cache 就能收拾。
4. **通用性/生态要求**：跑轻量 OS、驱动代码的"近 CPU"小核（很多 NPU 配一个控制核）本身就是一个微型 CPU——cache 是它的必需品。

**判断口诀：地址编译期可知 → SPM；地址运行时才知或高复用热点 → cache；两者混合 → 分流。**

## 11.2 混合存储架构：分工与数据分类

**图示化描述（一个典型混合 NPU）**：

```
                 ┌── 大块规则流（特征图 tile、权重块）
 DMA ⇄ SPM ⇄ PE 阵列
                 └── 小块规则流（scale/bias）→ 可走"旁路 FIFO"

 CPU/NPU 控制核 ⇄ 小 cache(L1)   ← 元数据、描述符、同步变量
 不规则引擎（gather 单元）⇄ 共享 L2(带 cache) ← embedding 表、索引
```

**分工规则**（编译器/驱动按 tensor 属性打标签）：

- `静态、大、规整` → SPM（吃带宽与容量红利）；
- `动态、小、热点` → cache（吃命中红利，容忍不确定延迟——因为它们不在性能关键路径上）；
- `动态、大` → 混合：分层搬运（粗粒度 DMA 到 SPM，细粒度片上随机访问由 cache 兜住）。

**一致性问题的处理**是混合架构的暗礁：SPM 内容软件可见、不参与硬件一致性；cache 参与一致性（第 13～14 章）。两者交换数据时（SPM 的结果被 cache 侧读取）需要显式的"clean/invalidate"或经过一致域的通路——忘记就会读到旧数据，这是第 17 章"一致性盲区"误区的标准案例。真实参照：Qualcomm Hexagon（SPM/本地内存 + 传统 cache 层次并存）、许多车载 NPU（大 SPM + 控制核 cache）。

## 11.3 稀疏与压缩存储：CSR/CSC 的冲击

**CSR 结构**（一个 5 元素×5 列的稀疏行）：三数组——`data[]`（非零值）、`col_idx[]`（每个非零元的列号）、`row_ptr[]`（每行在 data 中的起点）。省了零元素的存储，但付出三重代价：

1. **间接访存**：`data[col_idx[j]]`——先读索引、再读数据，两跳依赖，DRAM 随机延迟暴露无遗；
2. **负载不均**：各行非零数差异大，固定并行划分会导致 PE 间忙闲悬殊（需要动态任务窃取——又一个控制流回归的例子）；
3. **带宽税**：col_idx 与 row_ptr 本身要占带宽（int32 索引可能比 int8 数据还贵）。**"稀疏省计算但费存储带宽"**——只有稀疏度足够高（如 >70% 零）且硬件有零跳过时才划算。

**量化后的权重与"结构化稀疏"是存储友好路线**：int8/int4 直接砍容量与带宽（无任何间接性）；N:M 结构化稀疏（NVIDIA 的 2:4：每 4 个权重留 2 个，位置元数据固定 2 bit/组）让稀疏变成**规则解码**而非随机寻址——存储层次可以流水线化处理。**分野总结：非结构化稀疏 = 存储的敌人（不规则），结构化稀疏/量化 = 存储的朋友（规则压缩）**。片上解压单元通常放在权重池与阵列之间（解压带宽放大 2～4×）。

## 11.4 NoC 与存储的交互

多核/多 tile NPU（大模型时代的标配）中，"访问存储"变成"报文穿越路由器网格"：

- **拓扑与路由**：2D mesh 最常见；XY 路由（先横后纵）简单且免死锁。每个 tile 的 SPM 既是本地存储又是网络端点——**远程 tile 的 SPM 访问 = 一个网络事务**，延迟由跳数 × 每跳延迟决定（几拍～十几拍）。
- **事务类型**：读请求/数据返回/写（可选应答）；**组播**服务权重广播（一份权重同拍发给多 tile，比逐个单播省 N 倍带宽）；**原子操作**服务多核同步（第 13 章的信号量落到 NoC 上就是原子事务）。
- **与 DRAM 的关系**：mesh 边缘挂内存控制器（可能多个），"谁离 DRAM 近谁占便宜"——数据放置与任务映射要联合优化（第 15 章系统级调度）。
- **QoS 与死锁**：路由器要区分"计算必需流量"与"预取/搬运营销流量"（虚拟通道/优先级）；协议设计要避免"请求等数据、数据等缓存、缓存等请求"的环形等待（虚拟通道打破协议层死锁——与第 14 章 CHI 的机制同源）。

## 案例/示例：2:4 结构化稀疏的存储账

NVIDIA Ampere+ 的稀疏 Tensor Core：权重按 2:4 存储为"压缩值 + 2 bit 位置掩码"——**存储与带宽直接省一半**，硬件按掩码成对恢复、双倍 MAC 吞吐。从存储视角看这个设计的聪明之处：稀疏性被约束成**每 4 个一组选 2 个**的规则模式，解码逻辑是固定流水线而非随机寻址——不规则负载被驯化成规则压缩。反例是70% 非结构化剪枝的 BERT 权重跑在普通 NPU 上：CSR 索引的随机访问让 DRAM 延迟完全暴露，实测常常"模型更小、推理更慢"。**同是稀疏，存储命运迥异——规则性是唯一分水岭。**

## 常见误区与注意事项

1. **"NPU 不该有 cache"教条化**：负载谱系连续，混合架构才是普遍现实；关键是分界规则要清晰、可编译器执行。
2. **SPM 与 cache 间一致性想当然**：两者对"谁有最新数据"的答案不同（软件 vs 协议），交换点必须显式维护。
3. **稀疏=省资源**：非结构化稀疏把税转嫁给带宽与控制流，不稀疏到一定度数就是负资产。
4. **忽略索引数组的带宽**：col_idx/row_ptr 可能比数据本身更贵（int32 vs int8）。
5. **NoC 只当导线用**：组播、原子、QoS、死锁避免都是 NoC 的"存储级"职责，架构期不设计后期无法补救。
6. **零跳过能耗当白捡**：跳过计算可以，但零值仍可能被搬进存储——要在格式层面就"不存零、不搬零"。

## 本章小结

- NPU 需要 cache 的四场景：gather/动态形状/元数据/控制核——共同特征是"地址运行时才知"。
- 混合架构按 tensor 属性分流：静态大块进 SPM、动态热点进 cache；SPM↔cache 数据交换要显式一致性维护。
- CSR/CSC = 省存储费带宽（间接访存 + 索引税 + 负载不均）；量化与 N:M 结构化稀疏 = 规则压缩，存储友好；规则性是稀疏价值的分水岭。
- 多核 NPU 中存储访问网络化：mesh + XY 路由 + 组播 + 原子 + QoS；数据放置与任务映射联合优化。
- 本章的"混合"思想在第 13～15 章放大为整个 SoC 的异构协同。

## 思考题与练习

**1.（概念题）** 对以下数据各选 SPM/cache/FIFO 并说明理由：(a) 3×3 卷积权重；(b) embedding 查表的热门词向量；(c) 每通道 scale/bias；(d) 算子图的调度描述符链。

**2.（计算题）** 90% 稀疏的矩阵（int8 数据、int32 列索引、CSR）与稠密 int8 相比：存储省多少？带宽省多少（假设索引与数据都要过 DRAM）？
*（参考：存储 0.1×数据+0.1×4 索引+行指针 ≈ 稠密的 50%+；带宽同量级——索引税吃掉大半收益。）*

**3.（分析题）** 为什么 2:4 稀疏对存储层次"友好"而 70% 非结构化稀疏"不友好"？从寻址规则性、解码流水线、带宽三个角度作答。

**4.（设计题）** 设计一个 4-tile NPU 的 NoC 存储事务：卷积权重需要广播到 4 个 tile，部分和要在 tile 间交换。给出事务类型、路由与优先级设计，并说明死锁规避手段。

**5.（综合题）** 一个推荐模型 = embedding gather（30% 时间）+ 三层 MLP（70%）。给出混合存储架构的完整数据流方案（哪个引擎、哪级存储、如何同步）。

## 拓展阅读

1. Sze et al., *Efficient Processing of DNNs*——稀疏与压缩存储的专章。
2. Song Han et al., "EIE: Efficient Inference Engine on Compressed Deep Neural Network," ISCA 2016——稀疏权重 + CSR 在加速器上的开山之作（也是"索引税"问题的原始现场）。
3. NVIDIA Ampere 白皮书与 "sparse tensor cores" 技术博客——2:4 结构化稀疏的一手资料。

---
---

# 第 12 章 NPU 存储层次优化实例

## 本章导读

第三部分的收官章：把前四章的武器（复用公式、SPM/DMA、数据流/tiling、混合架构）对准真实网络与真实工具链。四条主线：**ResNet 与 MobileNet 的存储优化为什么是两种游戏**；**TVM/MLIR 如何把 tiling 自动化**；**Roofline 模型如何一张图诊断存储瓶颈**；**数据搬运的能耗账为什么压倒计算账**。本章之后，你从"懂概念的人"变成"能上手做优化分析的人"——第 16～18 章的方法论与实战都建立在这些具体案例上。

## 学习目标

1. 对 ResNet 类与 MobileNet 类网络分别给出 SPM 容量/tiling/带宽的优化优先级排序；
2. 解释 TVM 的调度原语（split/reorder/cache_read/cache_write）与本章 tiling 公式的对应关系；
3. 给定 NPU 的峰值算力与带宽，画（文字描述）Roofline 图并判断任一层是计算还是带宽受限；
4. 复述 Horowitz 能耗表的关键数字并推出"最小化数据移动"设计原则；
5. 综合运用以上工具为一个具体网络做完整的存储优化方案。

## 核心概念

| 术语（中文） | 英文 | 一句话定义 |
|---|---|---|
| 屋顶线模型 | Roofline model | 以 AI 为横轴、吞吐为纵轴的性能上限图 |
| 峰值算力 | peak compute (OPS) | 理论最大每秒运算数 |
| 峰值带宽 | peak bandwidth | 存储层次的理论最大吞吐 |
| 拐点 | ridge point | 屋顶线从带宽坡到计算顶棚的转折 AI 值 |
| 调度原语 | schedule primitives | TVM 中变换循环的算子（split/reorder/…） |
| 自动调优 | auto-tuning (AutoTVM/Ansor) | 搜索空间中自动搜最优调度的机制 |
| 缓冲化 | cache_read / cache_write | TVM 把中间张量显式引入片上缓冲的原语 |
| 数据搬运能耗 | data movement energy | 数据移动一级存储所消耗的能量 |
| 操作数驻留 | operand residency | 数据停留在产生它/消费它的层级附近 |
| 算子融合 | operator fusion | 相邻算子的中间结果不过存储层次直接流转 |

## 12.1 ResNet 与 MobileNet：两种存储游戏

**ResNet50 的存储画像**（int8）：主体是标准 3×3 卷积——典型层权重 0.3～2.4 MB（512 通道的 3×3 层即 2.36 MB，深层常超 SPM 权重池）、特征图大、AI 高（10³ 量级 FLOP/B，见第 8 章）。**优化优先级**：

1. **容量优先**：让每层的输入+输出 tile（权重通常可全驻留）塞进 SPM，DRAM 流量即刻降到"每层过境一次"；
2. **tiling 策略**：沿 H×W 切空间 tile（保 K² 窗口完整）比沿 C 切更省（通道切开会让输入重读 ×Cout 分块数）；典型 tile：`Tc=64~128、Th×Tw=16×16~32×32`；
3. **带宽其次**：只要容量够，25 GB/s 足够喂饱 8 TOPS。

**MobileNet 的存储画像**：depthwise 卷积 AI 低（几十 FLOP/B）——**带宽优先**：

1. **融合是救命稻草**：DW conv + BN + ReLU + PW conv 融成一个流水（中间特征图不过 DRAM）——MobileNetV2 的倒残差块（1×1 升维 → DW → 1×1 降维）如果不融合，中间张量反复过境，带宽直接爆表；
2. **通道并行而非空间并行**：DW 无跨通道复用，按通道并行让每 bank 独立供数（呼应第 9 章 bank 设计）；
3. **容量反而宽松**：DW 权重极小（每层几十 KB），SPM 压力小于 ResNet。

**一句话总结**：ResNet 是"把 tile 塞进去"的容量游戏，MobileNet 是"把数据串起来"的融合游戏。

## 12.2 编译器协同：TVM/MLIR 的存储规划

**TVM 的核心抽象**：计算（`compute`，纯数学表达式）与调度（`schedule`，循环怎么摆）分离。存储优化全部发生在调度层，用原语显式表达：

- `split(c, Tc)` → 生成 `c_outer, c_inner`，这就是 10.3 的 tiling；
- `reorder(...)` → 循环交换，改写流量公式（10.2）；
- `cache_read(A, "local")` / `cache_write(B, "local")` → 把张量切片显式搬进片上缓冲（对应 SPM staging + DMA），编译器据此生成双缓冲搬运代码；
- `unroll` / `vectorize` → 展开与向量化，接第 9 章的供数需求。

**自动调优**：AutoTVM/Ansor 在上述原语参数空间（tile 尺寸、循环顺序、缓冲深度）里搜索，目标函数 = 实测/模型化延迟（内含搬运时间与计算时间的 max——正是第 9 章双缓冲公式）。**MLIR** 路线用多层方言（linalg → vector → 后端）逐步下降，`bufferization` pass 负责"值的世界到地址的世界"的转换——地位等同 TVM 的 cache_read/write。**读法**：这些编译器不是黑魔法，它们搜的就是本章的公式空间；理解公式才能看懂（并纠正）编译器的选择。

## 12.3 Roofline 模型：一张图诊断一切

**模型**：任何代码在特定机器上的吞吐上限：

```
吞吐(OPS) = min( 峰值算力,  峰值带宽 × AI )     AI 单位: OPS/Byte
```

**画法（文字坐标系）**：横轴 AI（对数），纵轴吞吐（对数）。左下到右上的斜线是"带宽坡"（吞吐 = BW × AI）；水平的"顶棚"是峰值算力；**拐点 AI\* = 峰值算力 ÷ 峰值带宽**。

**图 12-1　Roofline 模型（8 TOPS / 25 GB/s，拐点 AI\* = 320 OPS/B）**

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 680 350" width="100%" role="img">
  <title>Roofline 模型示意：带宽坡与计算顶棚</title>
  <text x="340" y="24" text-anchor="middle" font-size="14" font-weight="500" fill="#333333">Roofline 模型：可达吞吐 = min（峰值算力，带宽 × AI）</text>
  <g stroke="#cccccc" stroke-width="0.5">
    <line x1="80" y1="290" x2="640" y2="290"/>
    <line x1="80" y1="220" x2="640" y2="220"/>
    <line x1="80" y1="150" x2="640" y2="150"/>
  </g>
  <line x1="80" y1="50" x2="80" y2="290" stroke="#444441" stroke-width="1.5"/>
  <line x1="80" y1="290" x2="640" y2="290" stroke="#444441" stroke-width="1.5"/>
  <g font-size="11" fill="#888888" text-anchor="middle">
    <text x="100" y="308">1</text><text x="240" y="308">10</text>
    <text x="380" y="308">100</text><text x="520" y="308">1000</text>
  </g>
  <g font-size="11" fill="#888888" text-anchor="end">
    <text x="72" y="294">0.1</text><text x="72" y="224">1</text><text x="72" y="154">10</text>
  </g>
  <text x="360" y="334" text-anchor="middle" font-size="11" fill="#888888">算术强度 AI（OPS / Byte，对数）</text>
  <text x="60" y="40" font-size="11" fill="#888888">可达吞吐（TOPS，对数）</text>
  <line x1="184" y1="290" x2="451" y2="157" stroke="#185FA5" stroke-width="2.5"/>
  <line x1="451" y1="157" x2="620" y2="157" stroke="#E24B4A" stroke-width="2.5"/>
  <text x="96" y="252" font-size="11" fill="#185FA5">带宽坡</text>
  <text x="96" y="266" font-size="11" fill="#185FA5">吞吐 = 25 GB/s × AI</text>
  <text x="560" y="146" text-anchor="middle" font-size="11" fill="#A32D2D">计算顶棚 8 TOPS</text>
  <line x1="451" y1="157" x2="451" y2="290" stroke="#999999" stroke-width="1" stroke-dasharray="3,3"/>
  <circle cx="451" cy="157" r="5" fill="#E24B4A" stroke="#A32D2D"/>
  <text x="458" y="230" font-size="11" fill="#777777">拐点 AI* ≈ 320</text>
  <text x="458" y="244" font-size="11" fill="#777777">（= 算力 ÷ 带宽）</text>
  <circle cx="552" cy="157" r="5" fill="#378ADD" stroke="#185FA5"/>
  <text x="552" y="138" text-anchor="middle" font-size="11" fill="#0C447C">ResNet 3×3 层（AI ≈ 1700）</text>
  <text x="552" y="176" text-anchor="middle" font-size="11" fill="#0C447C">计算受限</text>
  <circle cx="338" cy="213" r="5" fill="#1D9E75" stroke="#0F6E56"/>
  <text x="338" y="196" text-anchor="middle" font-size="11" fill="#085041">MobileNet DW（AI ≈ 50）：带宽受限</text>
  <text x="120" y="70" font-size="11" fill="#999999">融合可把坡上的点推向顶棚（中间流量消失，AI 上升）</text>
</svg>

一个层算出 AI 后打点：点在坡上 = 带宽受限（加带宽/省流量才有用）；点在棚上 = 计算受限（加算力/提利用率才有用）。

**数字例**：NPU 8 TOPS（int8）、有效带宽 25 GB/s → 拐点 AI\* = 8×10¹² ÷ 25×10⁹ = **320 OPS/B**。

- ResNet 层 AI ≈ 1700 > 320 → 计算受限（顶棚），优化看 MAC 利用率（tiling 让阵列吃满）；
- MobileNet DW 层 AI ≈ 30～80 → 带宽受限（坡上），优化看融合与流量削减；
- 算子融合把相邻层合并后，合并体的 AI 按合并公式重算（中间流量消失使 AI 显著上升）——**融合是"把点从坡上推向棚上"的手段**，Roofline 把它的价值可视化。

**注意**：Roofline 用的是**有效**带宽（含预取/DMA 效率），且忽略延迟（只看吞吐）——诊断第一层够用，精细分析要回到时间线仿真。

## 12.4 能耗视角：搬运能耗压倒一切

**Horowitz 的经典数据（45 nm，数量级至今仍被引用）**：

| 操作 | 能耗 |
|---|---|
| 8 bit 整数加法 | ~0.03 pJ |
| 8 bit 乘法 | ~0.2 pJ |
| 32 bit 浮点加法 | ~0.9 pJ |
| 32 bit 浮点乘法 | ~3.7 pJ |
| **读 8 KB 片上 SRAM** | **~5 pJ** |
| **读 32 KB 片上 SRAM** | ~10～20 pJ |
| **读片外 DRAM** | **~640～2000 pJ** |

**三个读数**：

1. 一次 DRAM 访问 ≈ **数千次** int8 乘加——"存一次顶算千次"；
2. 片上 SRAM 比 DRAM 便宜约两个数量级——层次每降一级，搬运费涨 10～100 倍；
3. 计算本身几乎免费——**NPU 的能耗优化 ≈ 数据移动优化**。

**设计原则推论**（第三部分的总纲）：①数据在产生处附近消费（算子融合、输出固定）；②复用尽最大化在最低层级发生（第 8 章复用公式 × 本章能耗表 = Eyeriss 优化能耗的完整数学）；③DRAM 流量最小化是第一目标（压缩、稀疏、tiling 全为此服务）；④同等流量下选择字宽窄的格式（int8/int4 的能耗优势是全方位的）。

## 案例/示例：把四件工具串成一个完整分析

**对象**：MobileNetV2 的一个倒残差块（int8），NPU 参数同 12.3（8 TOPS / 25 GB/s / SPM 512 KB）。

1. **Roofline 定性**：DW 层 AI ≈ 50 → 带宽受限；1×1 层 AI ≈ 300+ → 接近拐点；
2. **融合方案**：1×1 → DW → 1×1 三段融合，中间特征图只在 SPM 流转，DRAM 只见块输入输出；
3. **tiling**：按通道切 2 块 × 空间 28×28 tile，SPM 峰值占用 ≈ 420 KB < 512 KB ✓；
4. **能耗核算**：融合前中间张量过 DRAM 两次 ≈ 2×(0.8 MB×640 pJ/B) ≈ 1 mJ/块·次；融合后该项归零——只此一项，该块能耗降约 40%（其余为计算与片上搬运）；
5. **验证**：TVM 调度里 `cache_read/cache_write` + 融合 pass 表达同样方案，实测对照。

这就是"复用公式 → Roofline → 融合/tiling → 能耗核算"的完整闭环——第 18 章的综合设计题将要求你独立走一遍。

## 常见误区与注意事项

1. **Roofline 用峰值带宽自欺**：有效带宽要打折（预取效率、bank 冲突、多任务争抢），用纸面峰值画图会得出"计算受限"的假诊断。
2. **融合无脑最大化**：融合增加寄存器/SPM 驻留压力，融合到装不下时被迫缩小 tile，可能得不偿失——融合与 tiling 要联立搜索。
3. **能耗表数字当普适常数**：工艺/电压/距离不同数字会漂，**数量级关系**（DRAM ≫ SRAM ≫ 计算）才是稳定结论。
4. **tiling 只对齐容量上限**：tile 贴着 SPM 满容量切，双缓冲的 B 块没地方放——容量预算要给乒乓留一倍余量。
5. **编译器输出当终点**：Ansor 搜到的调度受搜索空间与代价模型限制，手算流量公式经常能再抠出 20%（这也是你学本章公式的意义）。
6. **忽略批维度**：batch>1 时权重复用倍增，AI 上升，ResNet 层可能从坡上跳到棚上——单样本分析不等于部署结论。

## 本章小结

- ResNet = 容量游戏（tile 塞进 SPM，DRAM 每层过境一次）；MobileNet = 融合游戏（DW+PW 串联，中间量不过境）。
- TVM/MLIR 的调度原语就是本章公式的可执行形式；自动调优搜的是同一个空间——懂公式才能驾驭编译器。
- Roofline：吞吐 = min(算力, 带宽×AI)，拐点 = 算力/带宽；点的位置决定优化方向；融合把点推向顶棚。
- 能耗铁律：DRAM 访问 ≈ 数千次 int8 计算；SRAM 便宜 DRAM 两个数量级；**能耗优化 = 数据移动优化**。
- 完整分析闭环 = 复用公式 → Roofline → 融合/tiling → 能耗核算 → 编译器验证。

## 思考题与练习

**1.（计算题）** NPU 16 TOPS、50 GB/s。求拐点 AI；对 AI 分别为 100、320、640 OPS/B 的三个层，判断各自受限类型并各给一条针对性的优化建议。
*（参考：AI\*=320；100 带宽受限（融合/省流量）；320 拐点附近（两者都要）；640 计算受限（提 MAC 利用率）。）*

**2.（计算题）** 用 Horowitz 表估算：int8 的 3×3×256×256 卷积层（第 8 章例），数据从 DRAM 过境一次 vs 片上全复用，两种方案的"搬运能耗/每次推理"差多少数量级？

**3.（分析题）** 为什么 MobileNetV2 的倒残差块"升维→DW→降维"结构比同参数量的普通卷积对存储层次更不友好？融合能挽回多少？

**4.（设计题）** 给 12.3 的 NPU 加预算：+1 MB SPM 或 +25 GB/s 带宽，二选一。对 (a) ResNet50、(b) MobileNetV2 两个目标负载分别做出选择并论证。

**5.（综合题）** 用 TVM（或读懂其论文）对同一个卷积层写出两种调度（有无 `cache_read`），解释生成的伪代码在 SPM/DMA 行为上的差异，并与手算流量公式对账。

**6.（开放题）** Ansor 等自动调优器的代价模型通常用简化解析式估计搬运/计算时间。结合 12.1～12.3 讨论：哪些硬件效应（bank 冲突、DMA 描述符开销、多任务带宽争抢、DRAM 行缓冲命中率）是它的代价模型难以捕捉的？这是否解释了第 12.6 节误区 5 所说"手算流量公式经常能再抠出 20%"？自动搜索最优数据流、编译器与存储层级协同设计至今仍是开放研究问题——你认为突破口在代价模型、搜索空间还是硬件可观测性？

## 拓展阅读

1. Sze et al., *Efficient Processing of DNNs*，第 7～8 章（Roofline 与能耗分析的系统化）。
2. Tianqi Chen et al., "TVM: An Automated End-to-End Optimizing Compiler for Deep Learning," OSDI 2018；以及 Ansor 论文（OSDI 2020）——本章 12.2 的一手来源。
3. Mark Horowitz, "Computing's Energy Problem (and what we can do about it)," ISSCC 2014——那张能耗表的原始出处，被引用数以万计。

---
---

# 第 13 章 CPU 与 NPU 的存储交互方式

## 本章导读

前 12 章的两条技术线——CPU 的 cache 文明与 NPU 的 SPM 文明——从本章起搬进同一颗 SoC。要回答的第一个问题是**内存怎么摆**（共享、分离还是统一）；第二个是**双方怎么握手**（中断、信号量、原子、事件）；第三个是**任务怎么下发**（传地址还是拷数据）；第四个是**一致性怎么延伸**（Arm DSU 与 CCI/CCN 一致性互连）。本章是"器件级设计"到"系统级设计"的转折点——从此我们讨论的不再是某块 SRAM，而是两套世界观的数据通路。

## 学习目标

1. 对比共享内存、分离内存、统一内存三种架构的编程模型与性能取舍；
2. 描述中断、信号量、原子操作、事件/fence 四类同步机制的层次与代价；
3. 解释"地址传递 vs 数据拷贝"两种任务下发模式的适用条件与 IOMMU 的角色；
4. 说明 Arm DSU 的域内一致性与 CCI/CCN/CMN 把设备接入一致域的方式（ACE vs ACE-Lite）；
5. 画（文字描述）一个典型手机 SoC 的存储互连拓扑并标注各链路的一致性级别。

## 核心概念

| 术语（中文） | 英文 | 一句话定义 |
|---|---|---|
| 共享内存架构 | shared memory architecture | CPU 与 NPU 访问同一物理 DRAM |
| 分离内存架构 | discrete memory | NPU 拥有私有内存（经总线/链路访问） |
| 统一内存 | unified memory | 同一 DRAM + 统一地址空间 + 一致的访问路径（Apple 语境） |
| I/O 内存管理单元 | IOMMU / SMMU | 给设备做地址翻译与隔离的页表硬件 |
| 门铃 | doorbell | 设备队列的"有新任务"通知寄存器 |
| 描述符环 | descriptor ring | CPU 与设备共享的生产者-消费者环形队列 |
| 硬件信号量 | hardware semaphore | 互连上的原子读改写单元 |
| 事件计数器 | event counter | 跨核完成通知的计数/等待原语 |
| ACE 协议 | AMBA ACE | Amba 一致性扩展：可被侦听的完整主设备 |
| ACE-Lite | AMBA ACE-Lite | 只能发起一致性访问、不能被侦听的 I/O 一致性 |
| 一致性互连 | coherence fabric/interconnect | CCI/CCN/CMN 等实现一致性协议的片上互连 |
| 缓存维护操作 | cache maintenance | clean（写回）/invalidate（作废）显式操作 |

## 13.1 共享内存 vs 分离内存 vs 统一内存

**共享内存（最常见）**：CPU 与 NPU 挂在同一 DRAM 控制器上。优点：零拷贝成为可能（数据天然互相可见）、编程模型简单、内存按需分配；缺点：带宽是共享池（争抢 → 第 15 章 QoS）、NPU 的访问延迟受 CPU 流量影响。

**分离内存**：NPU 带私有 DRAM（服务器加速卡形态：GPU 卡上的 GDDR/HBM）。优点：专属带宽巨大、延迟确定；缺点：数据要跨 PCIe/链路搬运（拷贝税）、两套地址空间编程复杂、内存利用率碎片化。

**统一内存（Apple 路线）**：技术上仍是共享内存（一块 LPDDR 焊在封装内），但理念更进一步——**统一地址空间 + 硬件保证一致性 + 一样近的物理距离**，让软件完全感觉不到"两边"。代价：所有引擎共享同一带宽上限（无 HBM 洪峰）、内存容量上限焊死。它证明了：**"统一"的瓶颈不在地址而在带宽**——当 GPU/NPU 都想喂饱自己时，一块 LPDDR 就捉襟见肘（这也是 M 系列 Ultra 用双 die 拼带宽的原因）。

**术语提醒**：统一内存架构的缩写 UMA 在体系结构教材里常指"一致内存访问"（Uniform Memory Access，NUMA 的反义词），与 Apple 语境的 unified memory 不是一回事——读文献时先辨语境。

## 13.2 数据共享与同步机制：四个层次

**场景**：CPU 把输入 tensor 写好，要告诉 NPU"开工"；NPU 干完，要告诉 CPU"收货"。工具从粗到细：

1. **中断（粗粒度、异步）**：NPU 完成一整批任务后向 CPU 发中断。CPU 无需轮询、可睡眠省电；代价：中断延迟（µs 级）与上下文切换——适合大任务粒度。
2. **信号量/互斥（中粒度）**：共享内存里放一个标志变量，双方用**原子读改写**争抢。硬件基础：互连提供的原子操作单元（或 CPU 侧 LL/SC、NPU 侧原子指令）。注意：跨一致域的信号量在 cache 与 SPM 之间有一致性暗坑（第 17 章案例）。
3. **原子操作（细粒度）**：fetch-add、exchange 等——计数器、无锁队列的积木。
4. **事件/fence（驱动层）**：现代驱动把上述原语包装成"事件对象 + 等待/完成"的显式依赖图（如 GPU 的 timeline semaphore、Linux 的 dma-fence）。**软件流水线（第 15 章）的粘合剂**。

**生产者-消费者的标准结构——描述符环 + 门铃**：CPU 在共享内存的环形队列里写任务描述符（写完更新尾指针），然后向 NPU 的**门铃寄存器**写一下（触发 NPU 取描述符）；NPU 处理完更新头指针、发中断。DMA 引擎、网卡、NPU 全用这个结构——它是异构世界的"函数调用协议"。

## 13.3 任务下发：地址传递 vs 数据拷贝

- **地址传递（zero-copy）**：描述符里只放 tensor 的指针、长度、stride、格式。前提：①NPU 能直接访问那块内存（共享内存天然满足；分离内存需要 IOMMU 映射）；②**一致性已解决**——CPU 写的数据在 CPU cache 里、NPU 从 DRAM 读，会读到旧值！（解法见 13.4 与第 15 章：一致性互连让 NPU 直取 cache 内容，或显式 clean）。优点：零搬运、低延迟；缺点：共享缓冲区生命周期管理（CPU 不能提前复用/释放——需要 fence 保护）。
- **数据拷贝**：把输入整块拷到 NPU 专用/对齐的缓冲区再开工。优点：隔离干净、对齐与格式可控、无一致性烦恼；缺点：白付一次带宽与延迟（1 MB 拷贝 @25 GB/s ≈ 40 µs——高频小任务下致命）。

**IOMMU/SMMU 的角色**：地址传递模式下，设备的"虚拟地址"经 SMMU 翻译成物理页——带来**隔离**（NPU 只能访问被授权的页，防止恶意/故障 DMA 乱写内核内存）与**地址空间统一**（CPU 与 NPU 用同一虚拟地址引用同一数据，免去 pin + 翻译查询）。代价：页表行走延迟（TLB 命中后可忽略，miss 时几 µs 级）与 TLB 容量压力——大模型连续大块访问对此友好。

## 13.4 异构缓存一致性的延伸：DSU 与 CCI/CCN

回顾第 6 章：一致性的核心是"找到所有副本"。异构系统的办法是把设备分级接入：

- **Arm DSU（DynamIQ Shared Unit）**：一个 CPU cluster 内部的一致性域——L3（含侦听过滤器）+ 一致性总线，大核小核（第 1 部分提过的 big.LITTLE 后继）在同一域内享受 MESI 类语义。**但 DSU 只管 CPU**：NPU/GPU 不在 DSU 里。
- **一致性互连（CCI-550 / CCN-508 / CMN 系列）**：把 DSU（作为一个主设备）与 GPU/NPU 等挂在同一个更大的一致性网络里。协议分级：
  - **ACE 完整主设备**（如 GPU）：有自己的 cache、可被侦听（被动响应别人的无效化请求）、能主动发起一致性访问——域内"一等公民"；
  - **ACE-Lite（I/O 一致性）主设备**（如 NPU/DMA）：**可以发起**一致性读写（读时能拿到别人 cache 里的最新值，写时能正确无效化别人的副本），但**自己没有可被侦听的 cache**（自己内部是 SPM/私有 SRAM，软件负责）。这是 NPU 的标准接法——**读写经协议保证新鲜，自己藏的东西自己管**。
- **非一致接入**（普通 AXI）：设备直连 DRAM，不经协议——一切一致性靠软件 clean/invalidate。legacy 但仍大量存在于外设。

**一次"新鲜的读"怎么发生**：NPU（ACE-Lite）发起一致性读 → 互连查目录/侦听 → 发现某 CPU 核 L2 持有脏副本 → 该核供数（cache-to-cache）→ NPU 拿到最新值，DRAM 都没碰。对比非一致方案：CPU 先 `clean`（把脏数据压回 DRAM）、再通知 NPU、NPU 从 DRAM 读——多一次全量写回与软件握手。**I/O 一致性用硬件换软件，是现代 SoC 的默认选择。**

## 案例/示例：一台手机 SoC 的存储拓扑（文字图）

（ISP：图像信号处理器 Image Signal Processor，手机相机管线的专用引擎。）

```
CPU 大核×4 ┐
CPU 小核×4 ┤(各自 L1/L2)
           ┴─ DSU(L3+snoop filter, 域1)
                     │
        CMN 一致性互连(域2: MESI 类 + 目录)
             ├─ GPU (ACE: 自带 L1/L2, 可被侦听)
             ├─ NPU (ACE-Lite: SPM 内部, I/O 一致)
             ├─ ISP/显示 (ACE-Lite 或非一致)
             └─ DRAM 控制器 (LPDDR5x, 共享带宽池)
```

**图 13-1　手机 SoC 的存储互连拓扑与一致性分级**

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 680 410" width="100%" role="img">
  <title>手机 SoC 存储互连拓扑：DSU、CMN 一致性互连与各引擎的接入级别</title>
  <defs>
    <marker id="arr131" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
      <path d="M2 1L8 5L2 9" fill="none" stroke="#444441" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
    </marker>
  </defs>
  <text x="340" y="24" text-anchor="middle" font-size="14" font-weight="500" fill="#333333">手机 SoC：存储互连拓扑与一致性分级</text>
  <rect x="60" y="44" width="170" height="38" rx="8" fill="#B5D4F4" stroke="#185FA5" stroke-width="0.5"/>
  <text x="145" y="67" text-anchor="middle" font-size="12" fill="#0C447C">CPU 大核 ×4（L1/L2）</text>
  <rect x="250" y="44" width="170" height="38" rx="8" fill="#B5D4F4" stroke="#185FA5" stroke-width="0.5"/>
  <text x="335" y="67" text-anchor="middle" font-size="12" fill="#0C447C">CPU 小核 ×4（L1/L2）</text>
  <rect x="60" y="112" width="360" height="46" rx="8" fill="#85B7EB" stroke="#185FA5" stroke-width="0.5"/>
  <text x="240" y="139" text-anchor="middle" font-size="12" fill="#042C53">DSU：共享 L3 + 侦听过滤器（CPU 一致域）</text>
  <rect x="60" y="214" width="360" height="50" rx="8" fill="#378ADD" stroke="#185FA5" stroke-width="0.5"/>
  <text x="240" y="243" text-anchor="middle" font-size="12" fill="#F2F8FD">CMN 一致性互连（目录 + MESI 类协议）</text>
  <rect x="470" y="60" width="150" height="44" rx="8" fill="#CECBF6" stroke="#534AB7" stroke-width="0.5"/>
  <text x="545" y="78" text-anchor="middle" font-size="12" fill="#26215C">GPU</text>
  <text x="545" y="95" text-anchor="middle" font-size="11" fill="#3C3489">（ACE：可被侦听）</text>
  <rect x="470" y="136" width="150" height="60" rx="8" fill="#F5C4B3" stroke="#993C1D" stroke-width="0.5"/>
  <text x="545" y="158" text-anchor="middle" font-size="12" fill="#712B13">NPU（ACE-Lite）</text>
  <text x="545" y="176" text-anchor="middle" font-size="11" fill="#712B13">内部 SPM（软件管理）</text>
  <rect x="470" y="222" width="150" height="52" rx="8" fill="#C0DD97" stroke="#3B6D11" stroke-width="0.5"/>
  <text x="545" y="243" text-anchor="middle" font-size="12" fill="#173404">ISP / 显示 / 编解码</text>
  <text x="545" y="261" text-anchor="middle" font-size="11" fill="#27500A">（ACE-Lite 或非一致）</text>
  <rect x="60" y="304" width="360" height="44" rx="8" fill="#B4B2A9" stroke="#5F5E5A" stroke-width="0.5"/>
  <text x="240" y="330" text-anchor="middle" font-size="12" fill="#444441">DRAM 控制器（LPDDR：全 SoC 共享带宽池）</text>
  <line x1="145" y1="82" x2="145" y2="112" stroke="#444441" stroke-width="1.5" marker-end="url(#arr131)"/>
  <line x1="335" y1="82" x2="335" y2="112" stroke="#444441" stroke-width="1.5" marker-end="url(#arr131)"/>
  <line x1="240" y1="158" x2="240" y2="214" stroke="#444441" stroke-width="1.5" marker-start="url(#arr131)" marker-end="url(#arr131)"/>
  <line x1="420" y1="239" x2="445" y2="239" stroke="#444441" stroke-width="1.5"/>
  <line x1="445" y1="82" x2="445" y2="248" stroke="#444441" stroke-width="1.5"/>
  <line x1="445" y1="82" x2="470" y2="82" stroke="#444441" stroke-width="1.5"/>
  <line x1="445" y1="166" x2="470" y2="166" stroke="#444441" stroke-width="1.5"/>
  <line x1="445" y1="248" x2="470" y2="248" stroke="#444441" stroke-width="1.5"/>
  <line x1="240" y1="264" x2="240" y2="304" stroke="#444441" stroke-width="1.5" marker-start="url(#arr131)" marker-end="url(#arr131)"/>
  <text x="340" y="380" text-anchor="middle" font-size="11" fill="#888888">一致性三域：DSU（CPU 域）⊂ CMN（系统一致域）⊂ 非一致外设；NPU 的 SPM 对协议不可见（软件管理）</text>
</svg>

**读图要点**：三个一致性层级（DSU 域 < CMN 域 < 非一致外设）；NPU 的 SPM 在 ACE-Lite 之后**对协议不可见**（软件显式管理，第 11 章的混合架构在系统里的位置）；带宽是全 SoC 共享的最后一道窄门（第 15 章 QoS 的战场）。

## 常见误区与注意事项

1. **"共享内存 = 数据自动一致"**：共享的是物理介质，不是"新鲜度"——没有一致性互连或显式 clean 时，CPU cache 里的新值 NPU 看不见。
2. **ACE-Lite 设备以为能被侦听**：它只能"发起"一致性访问，自己内部的 SPM/私有 cache 不参与协议——从 SPM 到共享内存的发布必须软件显式（写回/flush）。
3. **零拷贝不做生命周期管理**：CPU 提前复用缓冲区会撕裂 NPU 正在读的数据——fence/引用计数不是可选项。
4. **把中断当同步**：中断是通知不是屏障——中断到达不代表 NPU 的写都已可见（内存模型问题，第 14 章）。
5. **忽略 SMMU 翻译的 TLB 效应**：scatter-gather 页表下 NPU 遍历稀疏 tensor 可能 TLB thrash——大页/连续物理内存是标配优化。
6. **统一内存当无限带宽**：统一的是地址与一致性，带宽上限没变——多引擎并发时它比分离内存更挤。

## 本章小结

- 三种内存摆法的本质：共享内存卖共享与零拷贝，分离内存卖专属带宽，统一内存卖编程模型——带宽是统一的真瓶颈。
- 同步四层：中断（大粒度省 CPU）、信号量/原子（细粒度）、事件/fence（软件流水线粘合剂）；描述符环+门铃是异构任务下发的通用协议。
- 地址传递 vs 拷贝的核心不是延迟而是**一致性与生命周期**；IOMMU/SMMU 给地址传递配上隔离与统一地址空间。
- 一致性分级延伸：DSU（CPU 域）→ CMN 一致性域（ACE 全权公民 / ACE-Lite I/O 一致）→ 非一致外设；NPU 标准姿势是 ACE-Lite——发起侧一致、内部自管。
- 手机 SoC 拓扑是本章所有概念的实物拼图，也是第 14、15 章的舞台背景。

## 思考题与练习

**1.（概念题）** 解释"共享内存"与"缓存一致"是两个正交概念，并各举一个"共享但不一致""一致但内存分离"的架构例子。
*（提示：后者可考虑两核各自 cache + 一致性协议但物理上同一 DRAM 的说法辨析；前者即非一致设备直连 DRAM。）*

**2.（分析题）** 信号量自旋等待中，两个核轮流对同一地址做原子加——在写无效 MESI 下描述该行在两个核间的状态迁移与总线流量，并说明为什么"用事件/fence 代替自旋"在大任务粒度下更省。

**3.（设计题）** NPU 需要访问 CPU 刚生成的 4 MB 输入。比较三条路径的总流量与软件复杂度：(a) NPU 经 ACE-Lite 一致性读；(b) CPU clean 后 NPU 直读 DRAM；(c) CPU 拷贝到 NPU 专用缓冲区。
*（要点：a 一次脏行传输；b 一次全量写回+一次读；c 写回+两次读写搬运。）*

**4.（概念题）** 描述符环 + 门铃为什么天然是"生产者-消费者"结构？单生产者单消费者时哪些指针操作可以免原子？

**5.（综合题）** 为 13.4 的手机 SoC 设计视频会议场景的数据通路（相机 → ISP → NPU 人脸检测 → CPU 编码），标注每段数据走什么存储、什么一致性级别。

## 拓展阅读

1. Arm AMBA AXI/ACE 协议规范（developer.arm.com 免费注册下载）——ACE/ACE-Lite 章节是一致性互连的一手定义。
2. Sorin, Hill & Wood, *A Primer on Memory Consistency and Cache Coherence*, 2nd ed.——第 8～10 章谈一致性的系统级延伸。
3. Apple "Unified Memory Architecture" 技术介绍与 AnandTech 对 M1 的深度评测——统一内存的工程现实。

---
---

# 第 14 章 异构缓存一致性与内存模型

## 本章导读

第 13 章把 NPU 接进了一致性互连，本章深入这张网的技术内芯：**NUMA 在异构系统中的表现**（为什么"内存的距离"成了设计变量）、**互连协议**（AMBA CHI 如何用目录化的事务实现一致性；PCIe 为什么不一致、CXL 如何补上这一课）、**内存模型**（弱内存模型下，一致性协议管不了的那部分顺序问题——异构编程 bug 的头号产地）。最后用手机 SoC 收束成一个完整案例。本章是全书"最接近系统软件视角"的一章，也是第 17 章"一致性盲区"误区的理论地基。

## 学习目标

1. 解释 NUMA 的延迟/带宽不对称性在 chiplet 与"CPU+加速器"两种尺度上的表现与对策（首次接触分配、迁移）；
2. 描述 AMBA CHI 的目录化请求-侦听-数据结构，对比它与经典总线侦听的差异；
3. 区分 PCIe（无一致性）、CXL.cache（设备可缓存主机内存）、CXL.mem（内存扩展）三种能力；
4. 用弱内存模型解释一个真实的异构同步 bug，并正确使用屏障（DMB/DSB、dma_wmb）修复；
5. 描述手机 SoC 中 CPU/GPU/NPU 的内存模型差异对上层驱动/运行时的要求。

## 核心概念

| 术语（中文） | 英文 | 一句话定义 |
|---|---|---|
| 非一致内存访问 | NUMA (Non-Uniform Memory Access) | 各处理器到各内存的距离/带宽不等的架构 |
| 首次接触分配 | first-touch policy | 页面在首次访问者一侧分配的 NUMA 策略 |
| AMBA CHI | AMBA Coherent Hub Interface | Arm 的目录化一致性互连协议（请求/侦听/数据通道分离） |
| 主节点 | home node / home agent | CHI 中负责某地址一致性仲裁的节点 |
| PCIe | PCI Express | 无一致性的标准 I/O 链路 |
| CXL.cache | Compute Express Link (cache) | PCIe 之上的一致性扩展：设备可缓存主机内存并参与主机一致性协议 |
| CXL.mem | CXL memory | 经 CXL 扩展的主机内存（延迟高于本地 DRAM） |
| 内存模型 | memory model (consistency) | 对共享内存访问顺序与可见性的体系结构承诺 |
| 顺序一致 | SC (Sequential Consistency) | 所有处理器看到统一的全局交错序 |
| 全存储排序 | TSO (Total Store Order) | x86 的模型：仅 store-load 可重排 |
| 弱内存模型 | weakly-ordered model | ARM/POWER/RISC-V：多种访存可重排，靠屏障约束 |
| 内存屏障 | memory barrier/fence | 强制顺序与可见性的指令（DMB/DSB/DMB-LD 等） |
| 发布/获取 | release / acquire | 成对使用的同步语义：写方发布、读方获取 |

## 14.1 NUMA 在异构系统中的表现

NUMA 的经典舞台是多 socket 服务器：本地内存 100 ns、远端内存 130～200 ns、远端带宽减半。**异构系统让 NUMA 以新形态回归**：

- **Chiplet 尺度**：多 die 拼接的 CPU/NPU，跨 die 访问要过 die-to-die 互连——延迟与带宽皆折价，"数据放哪个 die、任务在哪个 die 跑"重新成为调度变量；
- **加速器尺度**：CPU+HBM 加速器（或 CXL 内存池）= 天然两级 NUMA——本地 DRAM 快而小，远端内存大而慢；
- **NPU 私有 SPM 是"极端 NUMA"**：几十周期 vs 主存几百周期，只是它由软件管理（第 8～9 章）。

**对策谱系**：①**首次接触分配（first-touch）**——页在首访者本地分配（默认策略，对"初始化者=计算者"的负载正确）；②**显式绑定**——`numactl`/驱动把数据与任务钉在同一节点；③**自动迁移**——按访问统计热页搬家（Linux autoNUMA/NUMA balancing）。**异构场景的新变量**：带宽不对称比延迟不对称更伤——GPU/NPU 的吞吐对"远端带宽折价"极其敏感，放置策略要按带宽权重而非单纯延迟。

## 14.2 异构一致性协议：CHI、UPI 与 CXL

**AMBA CHI**（Arm 的当代一致性互连协议，CMN 系列互连的原语层）：与第 6 章经典总线侦听的两点本质差异——

1. **目录化 + 主节点（home）**：每个地址归属一个 home node（通常按地址散列到某互连节点），请求者只发请求给 home，home 查本地目录决定：独享应答 / 转发给持有者（侦听）取数 / 无效化广播。**点对点消息替代总线广播**——扩展到几十上百节点的关键；
2. **通道分离**：请求、侦听、数据、响应走独立通道（可乱序、可并行）——吞吐大增，但**协议层死锁风险**（循环等待）要用虚拟网络/信用机制规避（第 11 章 NoC 的教训在协议层重演）。

**Intel UPI**（多 socket 一致性链路）+ **mesh 互连**扮演同构 CPU 域内的等价角色。

**PCIe 与 CXL**：PCIe 本身**无一致性**——设备看到的内存只是"地址窗口"，别人 cache 里的新值它不知道。CXL 在 PCIe 电气/链路层之上叠了三个协议：**CXL.cache**（设备侧可有 cache，缓存**主机内存**的行，参与主机侧一致性协议——带反向无效化：主机逐出行时设备的副本也要失效）、**CXL.mem**（主机把设备后面的内存当延迟略高的扩展内存用）、**CXL.io**（原 PCIe 语义）。**分层读法**：CXL.cache 把加速器变成"远端的 ACE 全权公民"（对比第 13 章 ACE-Lite 的"半公民"），CXL.mem 则制造 14.1 的新型 NUMA 层——两者正在把"一致性"变成机架级的可组合服务。

**再往远看一步：近存计算与存内计算（NMC/PIM）**。CXL 的思路是把内存拉远、池化；PIM 反其道而行——把计算单元埋进存储芯片内部（HBM-PIM 在 DRAM die 旁放 ALU，UPMEM 把通用小核塞进内存模组），让"搬运"缩短为"就地计算"。从本书的坐标系看，PIM 是对存储墙最激进的回答：**不再优化层次，而是把层次压扁**——存储所在地即计算所在地。它的现实挑战有三：DRAM 工艺的逻辑管性能孱弱（算力密度低）、编程模型尚无共识（哪些算子下沉？谁做调度？）、以及与本章一致性协议的接缝（PIM 产出的新鲜数据对谁可见？）。目前仍处探索期，但作为"存储墙终局"的候选路线之一，值得保持跟踪。

## 14.3 内存模型：协议管不了的那部分顺序

**一致性协议保证的是"对同一地址的写全序可见"（第 6 章），不保证不同地址之间你想要的顺序**——后者由内存模型管辖。跨架构的谱系：

- **SC（顺序一致）**：理想国——所有访问全局一个交错序。代价是性能，现代硬件没人全盘提供；
- **TSO（x86）**：接近 SC，唯一放松是 store 后紧跟的 load 可以越过 store（store buffer 转发）——程序员几乎无感；
- **弱序（ARM/POWER/RISC-V）**：load-load、load-store、store-store 都可能重排（还有多拷贝原子性的精细差异），**想要顺序必须显式插屏障**。

**经典异构 bug**（CPU 写数据、NPU 轮询标志）：

```
CPU:  store data[0..N-1], VAL      // 写数据
      store flag, 1                 // 写标志
NPU:  while (load flag != 1);       // 等标志
      use load data[i]              // 读数据
```

在弱内存模型下，**CPU 的"写数据"与"写标志"可能被重排**（或写数据尚未全局可见），NPU 看到 flag=1 却读到旧 data——不是一致性的锅（协议对每个地址都尽职了），是**你没有发布（release）语义**。修法：

```
CPU:  store data[...] ; DMB (release) ; store flag=1
NPU:  load flag==1 ; DMB (acquire) ; load data[...]
```

（Linux 内核里对应 `dma_wmb()`/`dma_rmb()`，驱动代码的标准姿势。）**异构系统的三重放大**：CPU 是弱序（ARM）、互连是乱序通道（CHI）、设备端还有自己的重排/预取（SPM 写回时机）——每一层都可能吃掉你的隐式顺序假设。规则一句话：**同步点两侧，显式屏障；屏障的正确位置由"发布/获取"配对决定**。

## 14.4 案例：手机 SoC（高通骁龙类）的一致性全景

以公开资料口径描述当代旗舰 SoC（细节随代际变化，结构稳定）：

- **CPU 侧**：1+3+4（或 2+5 等）大小核，各自 L1/L2，经 Arm DSU（L3 + 侦听过滤器）成 CPU 一致域；
- **系统互连**：SoC 内一致性网络（Arm CMN 家族或厂商等效物），CPU 域、GPU（有自身 L1/L2、ACE 级接入）、Hexagon NPU/DSP、ISP、显示控制器、视频编解码器全部挂其上；DRAM 控制器（多通道 LPDDR）在网中央；
- **NPU/DSP 侧**：内部大 SPM（软件管理，第 9 章），对外以 I/O 一致性（ACE-Lite 类）发起访问——读 CPU 刚写的数据不必等 clean；自身 SPM 内容的发布靠驱动里的显式 fence/clean；
- **内存模型现实**：CPU（ARMv8 弱序）+ GPU（自有模型，驱动负责翻译）+ DSP（顺序模型或自有模型）——**上层的 AI 运行时（NNAPI/QNN）把屏障与 fence 封装成"事件"对象**，应用开发者感知不到，但每一个 bug 都藏在这层封装的缝隙里（第 17 章的实战案例来自这里）；
- **带宽现实**：AI 相机管线（ISP→NPU→编码）全走同一 LPDDR 池——QoS 配置（第 15 章）是旗舰 SoC 内存子系统的日常工作。

## 常见误区与注意事项

1. **一致性 = 顺序**：一致性协议只管单地址；跨地址顺序是内存模型的领地——两个都要正确才没有 bug。
2. **x86 习惯带到 ARM**：x86 TSO 下"能跑"的无屏障同步，移植到 ARM 手机上随机失败——弱序平台的驱动必须逐个同步点审计。
3. **屏障当万能钉子到处钉**：屏障过多会瘫痪流水线与互连；用发布/获取的**配对**思维精准放置。
4. **把 CXL 当"更快的 PCIe"**：CXL.cache 改变的是**一致性契约**（设备缓存 + 反向无效化），不只是带宽——软件栈（驱动、内存分配器的可共享性）要跟上。
5. **NUMA 只看延迟**：异构负载的瓶颈常在远端**带宽**折价；用带宽加权做放置决策。
6. **以为 home node 是瓶颈就会慢**：CHI 的目录在 home，但数据可由持有者直供——读延迟未必经过 home 的串行化。

## 本章小结

- NUMA 以 chiplet/加速器/CXL 三种形态回归异构系统；放置策略从"延迟敏感"转向"带宽加权"。
- CHI = 目录化 + home node + 通道分离，把一致性从总线广播升级为可扩展的点对点协议；UPI 服务 CPU 域。
- PCIe 无一致性；CXL.cache 让设备成为远端"全权公民"（可缓存+可被反向无效化），CXL.mem 制造新的 NUMA 层。
- 内存模型管协议管不到的跨地址顺序：弱序平台（ARM）上同步必须发布/获取配对 + 显式屏障；异构三重放大（CPU 重排×互连乱序×设备写回时机）让隐式假设无处遁形。
- 手机 SoC 是本章全景的实物：DSU 域 + CMN 域 + 各引擎私有模型，上层运行时用事件/fence 封装——bug 都藏在封装缝隙里。

## 思考题与练习

**1.（分析题）** 画出（或文字描述）14.3 的同步 bug 在弱序模型下的两种出错时序（CPU 侧重排 / NPU 侧重排），并分别标注屏障插在哪里能各自阻断。

**2.（概念题）** 解释为什么 CHI 需要 home node，而总线侦听不需要；由此说明两者在 8 核与 64 核下的扩展性差异。

**3.（对比题）** ACE-Lite（I/O 一致）与 CXL.cache 设备在"谁持有可被无效化的副本"上有何不同？各适合什么形态的加速器？

**4.（计算/分析题）** 16 核 NUMA 系统：本地访存 100 ns/40 GB/s 每核，远端 160 ns/20 GB/s。一个带宽型负载每核需要 5 GB/s。若一半数据被错误放置在远端，估算系统有效吞吐的损失；给出两种修正手段。

**5.（设计题）** 你在写 NPU 驱动的任务提交路径：写描述符（普通内存）→ 写门铃（MMIO，内存映射 I/O）。门铃写必须在描述符写之后全局可见。在 ARM 弱序 CPU 上，你需要什么屏障组合（提示：MMIO 写本身有序，但普通写与 MMIO 写之间）？说明每道屏障挡住了什么。

## 拓展阅读

1. Sorin, Hill & Wood, *A Primer on Memory Consistency and Cache Coherence*, 2nd ed.——内存模型与一致性双主题的唯一推荐主教材。
2. Arm AMBA CHI Architecture Specification（Arm developer 免费下载）——请求/侦听/数据通道与 home node 的一手定义。
3. CXL Consortium, CXL 2.0/3.0 Specification 与配套白皮书——CXL.cache/.mem 的官方语义。

---
---

# 第 15 章 数据搬移与调度优化

## 本章导读

器件（第 8～12 章）与协议（第 13～14 章）都齐了，最后一层战场是**系统调度**：怎么让 CPU、NPU、DMA、显示这些引擎在共享内存上**不打架且总在干活**。本章四个主题：零拷贝的完整条件（不只"传地址"那么简单）、计算与搬运的重叠（软件流水线）、多引擎的带宽仲裁与 QoS、以及自动驾驶这个"所有约束同时到场"的系统级案例。本章是第四部分的收官，也是通往第五部分"设计方法论"的桥梁——调度优化的思路与设计空间探索是同一套思维。

## 学习目标

1. 列出零拷贝成立的全部条件（对齐、一致性、生命周期、格式）并识别每个条件的破坏者；
2. 用软件流水线（预取→计算→回写三段式）计算流水深度与加速比上限；
3. 解释带宽仲裁的四种基本机制（固定优先级/轮转/加权轮转/信用制）及其公平性-延迟权衡；
4. 描述 QoS 的带宽/延迟二维保障与"显示 underflow"这类实时性反例；
5. 综合上述工具为一条多传感器数据流水线做带宽预算与调度设计。

## 核心概念

| 术语（中文） | 英文 | 一句话定义 |
|---|---|---|
| 零拷贝 | zero-copy | 数据在生产者与消费者之间不发生冗余搬运 |
| 缓冲池 | buffer pool | 预分配、循环复用的内存块集合 |
| 软件流水线 | software pipelining | 不同任务实例的各阶段在时间上交错执行 |
| 计算搬运重叠 | compute-transfer overlap | DMA 与计算并行的时间线组织 |
| 仲裁器 | arbiter | 多请求者争一个资源时的裁决逻辑 |
| 固定优先级 | fixed priority (FP) | 恒定优先序仲裁（可饿死低优先级） |
| 轮转/加权轮转 | RR / WRR (round robin) | 轮流服务/按权重配额服务 |
| 信用制 | credit-based | 用预分配的"令牌"控制占用与速率 |
| 服务质量 | QoS (Quality of Service) | 带宽/延迟/抖动的可承诺上限 |
| 实时性 | real-time (hard/soft) | 硬截止（错过即故障）/软截止（可偶发） |
| 带宽预算 | bandwidth budget | 各引擎在最坏情况下的流量分配表 |
| 显示欠载 | display underflow | 显示控制器取不到下一帧数据的实时事故 |

## 15.1 零拷贝：把"搬运税"清零的全部条件

零拷贝的口号人人会喊，工程上它是一条**条件链**，任何一环断裂都要退回拷贝：

1. **地址可达**：消费者能直接访问那块内存（共享内存 + IOMMU 映射，第 13 章）；
2. **一致性已解决**：生产者的写对消费者可见（一致性互连，或显式 clean/invalidate——注意 invalidate 路径上消费者 cache 里可能有旧副本，必须作废）；
3. **生命周期受控**：缓冲区在消费者用完前不被复用/释放——引用计数或 fence；
4. **物理属性合规**：对齐（NPU DMA 常要求 64/128 B 对齐）、物理连续或 scatter-gather 支持、cache 属性（可缓存/非缓存映射别搞错）；
5. **格式即数据**：生产者的输出布局恰好是消费者的输入布局（如 ISP 输出的 tile 排布 = NPU 期望的排布，第 10 章 swizzle 话题的系统级回响）——布局不匹配时的"重排拷贝"是隐藏最深的非零拷贝。

**工程形态**：Linux 的 dma-buf（跨驱动的缓冲区共享句柄）、GPU 的统一虚拟寻址、NPU 运行时的 tensor 池（预分配 + 生命周期跟踪）。**一个反直觉的结论**：在一致性互连良好、且数据量小时，"经 cache 的一致性读"（第 13 章路径 a）常常比"拷到专用对齐缓冲"更快——零拷贝不总需要专门设计，有时只需要**别去拷**。

## 15.2 异步搬运：重叠执行与软件流水线

**三段式软件流水线**（第 9 章双缓冲的系统级放大）：把任务链拆成 预取（DMA 进）→ 计算（NPU）→ 回写（DMA 出）三段，让**相邻任务实例**的三段交错：

```
实例1: [预取1][计算1][回写1]
实例2:        [预取2][计算2][回写2]
实例3:               [预取3][计算3][回写3]
稳态:   ... [预取k][计算k][回写k] ...      三引擎同时满转
```

**两条铁律**：①稳态周期 = max(预取, 计算, 回写)——流水线不创造带宽，只隐藏延迟（与第 9 章双缓冲同构，深度由缓冲数量决定）；②**每段之间的依赖必须显式 fence**（第 14 章）——依赖错了就是数据撕裂。

**利用率核算示例**：NPU 逐层推理，每层计算 0.2 ms、预取 0.15 ms、回写 0.05 ms。串行执行每层 0.4 ms、NPU 利用率 50%；三段流水后稳态每层 0.2 ms、利用率 100%（预取与回写在 DMA 引擎上并行）。**DMA 引擎是多任务并发的关键前提**——单通道 DMA 会把预取与回写串成瓶颈（这也是商用 NPU 都给多通道 DMA 的原因，第 9 章）。

## 15.3 多任务并发下的带宽仲裁与 QoS

**场景**：LPDDR 总带宽 60 GB/s，同时有：显示（必须 4 GB/s，超时即 underflow）、CPU（不可预测）、GPU 游戏（贪得无厌）、NPU 推理（可吞吐降级）。DRAM 控制器前的**仲裁器**决定谁先谁后：

| 机制 | 原理 | 优 | 劣 |
|---|---|---|---|
| 固定优先级 | 恒定序裁决 | 延迟保障简单 | 低优先级可饿死 |
| 轮转（RR） | 依序轮流 | 绝对公平 | 关键流量延迟无保障 |
| 加权轮转（WRR） | 按权重配额轮流 | 公平+可控配比 | 权重静态，不感知实时压力 |
| 信用制 | 每请求者一个信用池，按信用限速 | 可同时限带宽与突发 | 参数调校复杂 |

**QoS 的二维性**：带宽保障（"每 1 ms 内至少给我 4 GB/s"）与延迟保障（"我的请求等待不超过 5 µs"）是两回事——显示要的是延迟确定性（欠载是几 µs 级事故），NPU 要的是吞吐配额。**成熟设计 = 延迟敏感者走高优先级/短队列 + 吞吐型走加权配额 + CPU/GPU 落入弹性区**。

**DRAM 侧的隐藏变量**（回收第 2 章）：bank 间可并行、行缓冲命中率高者省带宽——仲裁器若把不同引擎的访问**按 bank 分组**调度（可感知地址），有效带宽能多挤 20～30%。这就是"调度即带宽"：同一物理 DRAM，调度好坏差出一代硬件。

**实时性反例（显示 underflow）**：显示控制器每 16.6 ms 要取下一帧（4 GB/s 均摊），但若某一瞬 GPU/NPU 把仲裁队列灌满，显示请求排队超过一个行周期，屏幕就闪一条裂缝。修法不是加总带宽，而是：显示走独立高优先级通道 + 预取深度加大（提前 2 帧取数）+ 其他引擎限突发。**系统级教训：QoS 故障永远是"最坏时刻"的故障，平均值再好看也没用。**

## 15.4 系统级案例：自动驾驶芯片的存储协同

**场景约束同时到场**：8 摄像头 + 4 雷达,硬实时（感知→规划→控制链路 <100 ms）、功能安全（ASIL-D，汽车安全完整性最高等级，不能有未定义行为）、功耗墙、多引擎共享内存。

**数据流水线与带宽预算（示意数字）**：

```
相机 RAW(8×2 GB/s=16) → ISP(去马赛克/畸变,零拷贝过共享 SRAM)
   → 感知 NPU(CNN+transformer, 峰值 30 GB/s, 权重预加载)
   → 融合/跟踪(CPU+DSP, 5 GB/s) → 规划控制(CPU, 2 GB/s)
显示/HMI: 3 GB/s ｜ 编码记录: 4 GB/s ｜ 余量: ~0
```

**设计要点逐条对应本章**：

1. **零拷贝链**：相机→ISP→NPU 全程共享缓冲池（dma-buf 式句柄），只传地址与 fence；ISP 与 NPU 共用一块片上 SRAM 做"直通管道"（数据不回 DRAM——把 16 GB/s 的中段流量从 LPDDR 预算里划掉，这是车载芯片都配大 SRAM 的根本原因）；
2. **软件流水线**：帧 k 在 NPU 感知时，帧 k+1 在 ISP 处理、帧 k-1 在融合——DMA 多通道 + fence 依赖图驱动；
3. **QoS 分区**：感知链路（延迟敏感+高吞吐）与记录编码（吞吐型可降级）分通道仲裁；安全岛（控制 CPU）独占小 SRAM，物理隔离于性能争抢之外——**功能安全的答案往往是"专属存储"而非"共享存储+QoS"**；
4. **最坏情况预算**：所有引擎按峰值同时活跃做表（上表即此），不满足则砍功能或加带宽——车载没有"平均情况"这个词。

## 常见误区与注意事项

1. **零拷贝=传地址**：条件链五环（可达/一致/生命周期/物理属性/格式）断一环就退化或出错；"布局重排拷贝"是最隐蔽的非零拷贝。
2. **流水线当永动机**：稳态周期 = max(各段)，最慢段是天花板——先治最慢段再谈流水深度。
3. **QoS 只做带宽**：延迟敏感流量（显示/控制）要的是**确定性**，配额制救不了排队尾巴；高优先级+深预取才行。
4. **平均带宽做预算**：系统级事故全发生在峰值重合时刻；预算表必须按全峰值联立。
5. **忽视 invalidate 方向**：消费者 cache 里的旧副本要作废（invalidate），生产者 cache 里的新值要写回（clean）——搞反方向是经典数据陈旧 bug。
6. **DMA 单通道跑流水线**：预取与回写会在 DMA 上串行——多通道/多引擎是软件流水线的硬件前提。

## 本章小结

- 零拷贝是五环条件链：地址可达、一致性、生命周期、物理属性、格式布局——dma-buf/tensor 池是工程形态，"别去拷"有时已是零拷贝。
- 软件流水线三段（预取/计算/回写）稳态周期 = max(各段)；DMA 多通道 + 显式 fence 是两块基石；流水线隐藏延迟不创造带宽。
- 仲裁四机制（FP/RR/WRR/信用）在公平与延迟间取舍；QoS 是带宽×延迟二维承诺；显示 underflow 是"最坏时刻"故障的教科书。
- DRAM 调度（bank 分组、行缓冲感知）本身能挤出 20～30% 有效带宽——调度即带宽。
- 自动驾驶案例的答案是：共享大 SRAM 直通管道 + 峰值联立预算 + 安全岛专属存储——零拷贝、流水线、QoS 三件套的系统级毕业考。

## 思考题与练习

**1.（计算题）** 流水线三段耗时：预取 120 µs、计算 200 µs、回写 60 µs。求稳态周期、加速比（对串行）、DMA 与计算引擎利用率各多少？若把计算优化到 100 µs 呢？
*（参考：200 µs、(380/200)=1.9×、DMA 利用率 (120+60)/200=90%、计算 100%；计算 100 µs 后稳态 120 µs、瓶颈转移到预取。）*

**2.（分析题）** 显示控制器需要"每 16.6 ms 得到 60 MB"。对比 WRR（显示权重 10%）与固定高优先级+深度预取两种方案在"GPU 突发灌满队列"场景下的表现。

**3.（设计题）** 为 15.4 案例设计缓冲池：帧尺寸 4 MB、流水深度 3 段×8 摄像头。总池多大？如何用 fence 管理复用？若 SRAM 只有 32 MB，哪些数据必须回落 DRAM？

**4.（概念题）** 解释"流水线隐藏延迟不创造带宽"，并用 15.1/15.2 的数字各举一例。

**5.（综合题）** 某手机的 AI 相机：CPU 后处理（10 GB/s）与 NPU 推理（20 GB/s）并发，LPDDR 可用 28 GB/s。设计一套 QoS 方案（优先级、配额、突发限制），并说明相机预览（延迟敏感）与照片连拍（吞吐敏感）应当如何区分对待。

## 拓展阅读

1. H&P, *A Quantitative Approach*, 第 5 章关于互连与多处理器带宽/延迟的章节；DRAM 调度可读 "Kim et al., ATLAS/TCM" 系列 DRAM 调度论文。
2. Linux 内核文档：DMA-API、dma-buf 与 fence 子系统——零拷贝与生命周期的工程实现一手资料。
3. 各车载芯片白皮书（NVIDIA Orin、TI TDA4、地平线征程系列公开资料）——对照 15.4 的直通 SRAM 与安全岛设计。

---
---

# 第 16 章 存储子系统设计流程

## 本章导读

前 15 章给了你"知识"，最后三章给"方法"。本章把设计一个存储子系统的完整流程串成闭环：**从负载出发提取需求 → 设计空间探索 → 模拟器评估行为 → CACTI/McPAT 评估物理 → 迭代收敛**。工具主角：perf/cachegrind（负载画像）、gem5/Sniper/自研 trace 模拟器（性能）、CACTI/McPAT（PPA）。本章结束处，你会拥有一张"从空白到签核"的路线图——第 18 章将用它跑两个完整案例。

## 学习目标

1. 用 perf/cachegrind 对真实程序做负载画像，产出缺失率、带宽需求、工作集报告；
2. 定义并离散化设计空间（容量×块×相联度×bank），用帕累托前沿筛选候选点；
3. 说明 gem5（SE/FS、Ruby）、Sniper、trace-driven 三类模拟器的精度-速度权衡与适用场景；
4. 用 CACTI/McPAT 完成候选点的面积/延迟/功耗评估并与模拟结果合成决策表；
5. 描述迭代闭环的收敛判据（满足目标 / 帕累托无改进 / 预算耗尽）。

## 核心概念

| 术语（中文） | 英文 | 一句话定义 |
|---|---|---|
| 负载画像/特征提取 | workload characterization | 用计数器/插桩提取访存行为统计 |
| 工作集足迹 | memory footprint | 程序活跃触达的地址集合大小 |
| 硬件性能计数器 | PMU (Performance Monitoring Unit) | CPU 内置的事件统计寄存器 |
| 设计空间探索 | DSE (Design Space Exploration) | 系统遍历参数组合寻找优解 |
| 全系统/用户态模拟 | FS/SE (Full/System Emulation) | gem5 的两种模式 |
| 内存系统模型 | Ruby (gem5) | gem5 的一致性/缓存时序建模框架 |
| 跟踪驱动模拟 | trace-driven simulation | 回放录制好的访存序列 |
| 执行驱动模拟 | execution-driven simulation | 连同指令执行一起模拟 |
| 帕累托前沿 | Pareto frontier | 多目标下的互不支配解集 |
| 敏感性分析 | sensitivity analysis | 输出对单参数扰动的响应曲线 |
| 签核指标 | sign-off criteria | 判定设计完成的量化标准 |

## 16.1 需求分析：从负载提取存储访问模式

**不测量就设计 = 蒙**。第一步是把目标负载的存储行为变成数字：

- **工具箱**：`perf stat`（硬件计数器：cache-misses、LLC-load-misses、前后台带宽）、`cachegrind/valgrind`（无 PMU 时的软件模拟，含指令级引用统计）、`VTune` 类 GUI（热点+缺失关联到源码行）、Pin/DynamoRIO（自建 trace 的采集前端）、MLPerf 类标准 benchmark（AI 负载）。
- **必答的六问**（负载画像报告模板）：①缺失率按层级分布（L1/L2/LLC）？②缺失的 3C/4C 构成？③工作集多大（足迹曲线：缺失率 vs cache 容量的扫描）？④读写比与写突发模式？⑤访问的空间模式（顺序/步长/随机）？⑥带宽需求峰值与持续性？
- **足迹曲线的做法**：跑一系列容量递增的模拟（cache 大小 16 KB→1 MB），画"缺失率-容量"曲线——**拐点即容量设计的目标区**（收益断崖前的最后一档）。这一张图值 1000 行争论。

**输出物**：《负载画像报告》——上面六问的答案 + 代表性 trace 的留存（供后续模拟器复用）。

## 16.2 设计空间探索：把参数问题变成搜索问题

**离散化**：容量 {16,32,64,128} KB × 块 {32,64,128} B × 相联度 {2,4,8,16} × bank {1,2,4} = 192 个组合——按今天的算力全部可跑，问题在于**每个点的评估成本**（模拟 + 物理工具）。

**分层剪枝**：①先用解析近似（AMAT 公式 + 缺失率经验曲线）粗筛掉明显差的 80%；②行为模拟（16.3）评估候选 20% 的 IPC/AMAT；③物理工具（16.4）给每个幸存者算面积/延迟/功耗；④合成**多目标决策表**，画帕累托前沿（性能-功耗、性能-面积两张图）。

**敏感性分析**：固定其余参数扫一个参数，确认每个参数在选定值附近"够平坦"（选定值左右 ±1 档，指标变化 <5%）——**陡坡上的设计点是脆弱的**（工艺角/负载变化会把它推下悬崖）。这份敏感性表也是向架构评审委员会答辩的核心材料。

## 16.3 性能评估：模拟器三件套

- **gem5**：学术与工业共用的开源框架。SE 模式（用户态、快、够用于单进程访存研究）与 FS 模式（全系统、能跑 OS、必要于一致性与驱动研究）；**Ruby** 子系统做缓存一致性/NoC 的详细时序（能自己定义协议状态机——第 6 章的知识在这里变成 SLICC 状态机代码）。慢：详细模式下 MIPS 级速度（每秒模拟百万级指令），要靠**检查点+warming+抽样**（SimPoint）技巧压缩成本。
- **Sniper**：多核时序模拟器，定位介于 gem5 与解析模型之间——够快能扫 DSE、够准能看多核争抢（带宽 contention、目录延迟）。
- **自研 trace-driven**：最轻量的路线——录制/生成访存 trace，回放到自己写的 cache 模型里数缺失。**适合教学与本征研究**（第 18 章实践作业就是它），不适合终点验证（trace 无法反馈：被模拟的 cache 决定后续访存的地址流时，trace 是错的——模拟内核态/自修改代码尤其如此）。

**选型速记**：扫参数空间用 Sniper/解析；一致性协议与微架构细节用 gem5+Ruby；课堂实验与快速假设检验用自研 trace 模拟器。

## 16.4 物理评估：CACTI 与 McPAT

- **CACTI**（第 7 章已上手）：每个候选配置输出面积/延迟/动态能耗/泄漏。要点：**同口径对比**（同一工艺库、同一端口假设），绝对值仅供量级参考。
- **McPAT**：在 CACTI 之上构建**整芯片**模型——喂它一份微架构配置（核数、cache 层次、互连类型），输出总面积/总功耗（动态+泄漏）。与 gem5 配对（gem5 输出活动量 → McPAT 按活动量算能耗）是学术圈标准流水线。
- **闭环合成**：决策表把三方数字并排——

| 候选 | AMAT（模拟） | IPC（模拟） | 面积（CACTI） | 功耗（McPAT） | 判定 |
|---|---|---|---|---|---|
| 32KB/4路 | 8.1 cyc | 1.62 | 0.11 mm² | 1.9 W | 帕累托候选 |
| 32KB/8路 | 7.6 cyc | 1.68 | 0.14 mm² | 2.3 W | 帕累托候选 |
| 64KB/8路 | 6.9 cyc | 1.71 | 0.24 mm² | 3.1 W | 帕累托候选（面积敏感场景淘汰） |

**没有"最优解"，只有"你愿意付哪个代价"**——帕累托前沿 + 目标函数（或约束），让取舍变成一行明账。

## 16.5 迭代优化：从初始设计到收敛

```
负载画像 → 初始设计（经验值+足迹曲线）
   ↓                         ↑
行为模拟（IPC/AMAT） →  不达标 → 修改参数/结构 →（回到模拟）
   ↓ 达标
物理评估（面积/功耗） →  超预算 → 缩容量/降相联度/加 banking →（回到模拟）
   ↓ 达标
敏感性/稳健性检查（陡坡?最坏负载?） → 有险 → 回退
   ↓ 通过
签核：决策表 + 帕累托图 + 敏感性表 → 架构评审 → RTL（超出本书范围）
```

**三条工程军规**：①**每轮迭代只动一两个参数**（否则归因失效）；②**最坏负载与代表负载并行评估**（平均值骗人，第 15 章）；③**留 20% 余量**（带宽、面积、功耗——后端实现与未来负载都会变差）。**收敛判据**：目标全满足 / 帕累托前沿不再前移 / 时间预算耗尽——三者其一即可签核进入下一阶段。

## 案例/示例：一次真实感的两轮迭代

**轮 1**：画像显示负载足迹拐点在 40 KB、读:写 = 3:1、缺失以容量缺失为主（3C 分析）→ 初始设计 48 KB/4 路/64 B。模拟：IPC 1.55，达标；CACTI：VIPT 越页（第 7 章约束：4 路 48 KB 需 index 7 位+6 位=13>12）！——**行为达标、物理不可行**。
**轮 2**：改为 32 KB/8 路（index 6 位合规）+ 硬件 stride 预取器（补偿容量缩小）。模拟：IPC 1.62 反超（预取吃掉了容量缺失）；功耗 +0.3 W（预取器与多余比较器）但在预算内 → 签核。
**教训**：第 7 章的"纸面约束"在真实流程里会在第一轮就拦下你——**物理工具要早进 loop，不要等行为全调完**。

## 常见误区与注意事项

1. **拿基准集当圣经**：SPEC/MLPerf 都只是采样；找到你的**产品负载**做主基准，标准集做泛化性检查。
2. **模拟器当真理**：gem5 也有模型误差（±10～20% 常见）；结论要问"差距是否大于模型误差"。
3. **trace 模拟器用于闭环场景**：访存流依赖被设计本身时（预取、一致性、页表）trace 会系统性失真。
4. **只扫单参数**：容量与块大小强耦合（大块配大 cache 才不冲突），全空间联合扫描或至少验证交互项。
5. **忽略最坏情况与角点**：PMU 计数与模拟都要收"最大最小值"不只平均；陡坡设计点直接回炉。
6. **工具链割裂**：行为（模拟器）与物理（CACTI）各跑各的、最后才对表——要每轮迭代都合表，分歧早暴露。

## 本章小结

- 流程五步：负载画像（六问+足迹曲线）→ DSE（离散化+分层剪枝）→ 行为模拟（gem5/Sniper/自研按精度-速度选）→ 物理评估（CACTI/McPAT 同口径对比）→ 迭代收敛（军规三条+三种签核判据）。
- 足迹曲线一张图定位容量；敏感性分析保证稳健；帕累托前沿把多目标取舍变成明账。
- 物理约束（VIPT、面积）要早进 loop——案例证明行为达标的设计可能物理不可行。
- 自研 trace 模拟器是教学利器但不是终点验证——第 18 章将亲手写它。

## 思考题与练习

**1.（工具题）** 用 `perf stat -e cache-references,cache-misses,L1-dcache-load-misses,LLC-load-misses ./your_program` 跑任一程序，把各层缺失率填进六问模板，写半页画像结论（无 Linux 环境则用 cachegrind）。

**2.（设计题）** 为"容量×相联度"设计分层剪枝方案：解析近似阶段用什么公式淘汰 80%？幸存的 20% 用哪个工具评估？写出各阶段的判据。

**3.（概念题）** trace-driven 与 execution-driven 模拟的本质区别是什么？各举一个必须用后者的研究问题。
*（提示：预取器研究、自修改代码、OS 页表行为。）*

**4.（计算题）** 决策表三个候选如 16.4。若目标函数 = IPC − 0.5×面积(mm²) − 0.2×功耗(W)，选哪个？若约束改为面积 ≤0.15 且功耗 ≤2.5 呢？
*（参考：候选 1：1.62−0.055−0.38 = 1.185；候选 2：1.68−0.07−0.46 = 1.15；候选 3：1.71−0.12−0.62 = 0.97 → 候选 1 胜出；约束（面积 ≤0.15、功耗 ≤2.5）下候选 1、2 均可行，按目标函数仍选候选 1。）*

**5.（综合题）** 仿照 16.5 写你自己的"两轮迭代剧本"：设定负载画像数字与初始设计，自己设计第一轮暴露的问题（自选：VIPT 越页/带宽超限/功耗爆），给出第二轮修改与验证结论。

## 拓展阅读

1. gem5 与 Sniper 的官方文档/论文（Binkert et al., IEEE MICRO 2011；Carlson et al., SC 2011）。
2. Li et al., "McPAT: An Integrated Power, Area, and Timing Modeling Framework," MICRO 2009。
3. Jason Mars 等关于 SimPoint/抽样模拟的论文——长负载的模拟成本控制艺术。

---
---

# 第 17 章 常见设计误区与工程经验

## 本章导读

前面十七章讲"怎么做对"，本章讲"怎么不做错"。六个误区全部来自真实工程的 High 场景——每一个都符合同一个模式：**局部合理的设计决策，在系统级条件下互相作用后爆炸**。每条按"症状 → 根因 → 案例 → 对策"展开。最后是一份可以贴在工位上的经验清单。这一章很可能是你工作后翻回来最多的一章。

## 学习目标

1. 对每个误区能复述其"症状-根因"链，并指出它违背了前文哪条原理；
2. 掌握各误症的检测手段（计数器、仿真方法、审计清单）；
3. 学会"反向设计"思维：先想清楚最坏情况与系统级交互，再落局部参数。

## 核心概念

| 术语（中文） | 英文 | 一句话定义 |
|---|---|---|
| 过度设计 | over-engineering | 单维最优但系统级负收益的设计 |
| 预取污染 | prefetch pollution | 无用预取挤占有用行的现象 |
| 预取精度 | prefetch accuracy | 预取块中被真正消费的比例 |
| 带宽风暴 | bandwidth storm | 多源写/预取流量叠加超带宽 |
| 冷启动缺失 | cold-start misses | 断电/重配置后的强制缺失回潮 |
| bank 热点 | bank hotspot | 并发流量集中于少数 bank |
| 一致性盲区 | coherence blind spot | 数据在协议覆盖之外传递的路径 |
| 数据陈旧 | stale data | 消费者读到过期副本的正确性事故 |

## 17.1 六大误区逐条解剖

**误区一：相联度崇拜症**

- **症状**：L2 从 8 路提到 16 路"为了保险"，实测 IPC 几乎不动，功耗 +15%、关键路径逼近时序悬崖。
- **根因**：违背 3.4/7.1 节的收益递减曲线——8 路以后冲突缺失只剩 1～2 个百分点可削，而比较器/多路器/替换状态的延迟与功耗线性上涨；更糟的是它挤掉了同预算下"加容量"或"加预取"的选项（钱要花在刀刃上）。
- **案例回放**：某项目 L2 缺失率 12%，团队归因"冲突"直接加路；事后 3C 分析显示 12% 里 10% 是容量缺失——加路只治了 2%，换 16 路的钱加容量能治 8%。**教训：改参数前先做缺失分类，诊断先于处方**。
- **对策**：任何相联度变更前强制出 3C 报告；收益预估 <2 个百分点的加路申请直接驳回。

**误区二：写策略的系统性忽视**

- **症状**：多核扩展测试中带宽先爆——单核好好的设计，8 核一起跑，互连上全是写流量；或一致性验证后期发现写直达下"每次核间写同步都打穿到 LLC"。
- **根因**：写路径的账（4.3/4.5 节：写流量 = 写频次 × 策略系数）在单核测试里被 L2 大带宽掩盖；多核场景写流量还与一致性事务（BusUpgr/无效化）叠加——**写策略不只是 cache 的事，是一致性带宽的事**。
- **对策**：设计期就画"多核写带宽预算表"（每核写流量 × 核数 + 一致性放大系数）；流片前用 8 核同步压力负载（如自旋锁风暴）专项压测。

**误区三：预取污染（慷慨的预取器反噬）**

- **症状**：预取器覆盖率 90%，总性能却下降；perf 显示"预取命中"很高，但真实缺失率不降反升。
- **根因**：只考核覆盖率、不考核精度（5.4 节三指标）——大量无用预取把行填进 cache，逐出的是马上要用的行（污染），还占满缺失请求的带宽通道。典型肇事者：对随机访问负载开激进 stream 预取；预取距离错配（太早=被逐出，太晚=没盖住）。
- **对策**：预取器必须带**置信度门槛与自适应降级**（连续 N 次无用即收缩）；验收指标定"净性能+真实缺失率"而非预取命中数；污染敏感场景（工作集≈cache 容量）默认关闭激进预取。

**误区四：SPM 容量的"黄金尺寸"失焦**

- **症状 A（过小）**：NPU 实测吞吐只有峰值 40%——时间线显示 DMA 频繁触发、tile 碎成几十 KB、每次任务结束都有搬运空泡（双缓冲退化成单缓冲）。
- **症状 B（过大）**：竞品分析发现对方 SPM 小一半却同性能——多出的 1 MB 面积纯付了泄漏电费，还挤掉了 PE 阵列预算。
- **根因**：SPM 尺寸没有对准目标网络族的驻留需求分布（9.4 节 P90 法则被跳过）；或按"最大那层"配置（最坏情况主义用错了地方——层的驻留可以 tile 化，不必整层驻留）。
- **对策**：对目标网络族做"逐层驻留需求直方图"，容量覆盖 P90，剩余 10% 用 tiling 化解；同时跑"容量-吞吐曲线"确认过了拐点（再加容量吞吐不涨）。

**误区五：多引擎并发下的 bank 冲突**

- **症状**：单引擎调试一切正常，两引擎并发跑带宽掉 40%——仲裁器日志显示大量同 bank 串行化。
- **根因**：bank 冲突的检测只做了"单引擎 trace 分析"（9.3 节），而**冲突是并发地址流的卷积**——两个引擎各自的流都错开了 bank，但两者**互相**撞在同一个 bank 的同一拍。深度学习负载里两个引擎常以相同周期节拍访问（同步启动+固定流水），冲突呈周期性锁相。
- **对策**：并发 trace 回放测试进签核清单（16 章模拟器）；bank 数选质数（错相位）；引擎间地址偏移策略（启动时错开几 KB）；最狠的招——两个热点引擎的缓冲区物理布局分 bank 池。

**误区六：一致性盲区（软件看见的不等于硬件知道的）**

- **症状**：NPU 偶发读回旧数据，只在压力测试的特定时序下出现（低概率复现，最恶性的 bug 形态）。
- **根因**：数据在协议覆盖之外传递——典型链路：CPU 写数据（在 CPU cache）→ 拷贝到共享缓冲（部分仍滞留 CPU cache 行）→ NPU 经**非一致路径**读 DRAM（读到旧值）；或反向：NPU 写 SPM → 直接从 SPM 被 CPU 读（NPU 内部写回 DRAM 的时机未到/未触发）——第 11/13 章的"SPM 对协议不可见"在这时咬人。
- **对策**：建立"一致性审计清单"——凡跨引擎传递的缓冲区，逐条确认：走一致互连？或已 clean/invalidate？或经显式 fence+回写 API？驱动代码 review 按此清单过；压力测试用"随机延迟注入"放大时序窗口。

## 17.2 工程经验十条（贴墙版）

1. **先测量后设计**：没有画像数据的设计会议一律改成测量任务布置会。
2. **诊断先于处方**：改任何参数前，先拿到 3C/带宽/时间线的归因证据。
3. **最坏情况是常态**：预算按峰值联立（第 15 章），验收按压力时序（误区六）。
4. **参数改动要归因**：一轮只动一两个变量，且必须能解释"为什么好了/坏了"。
5. **留 20% 余量**：带宽、面积、功耗、时序——后端与未来负载都会变差。
6. **可观测性前置**：流片前把 PMU 计数器、bank 冲突计数、预取精度计数器做进 RTL——没有仪表的系统不能调试。
7. **协议边界画图**：一致域的每一条边界（DSU/CMN/ACE-Lite/非一致）挂图开发，跨界数据流逐一过清单。
8. **警惕周期性锁相**：多引擎同步启动的负载会把偶发冲突变成确定性冲突——测试要打散节拍（随机延迟注入）。
9. **编译器不是黑盒**：NPU 栈的性能问题一半在调度（tiling/融合），拿流量公式（第 10 章）与编译器输出对账。
10. **复盘制度化**：每个误区事故写成"症状-根因-对策"卡进团队知识库——本书第 17 章就该这么长出来。

## 本章小结

- 六大误区共同模式：局部合理 × 系统级交互 = 爆炸；解药全是"系统级视野 + 定量归因"。
- 诊断工具箱：3C 分析（误区一）、多核写带宽预算（二）、预取精度三指标（三）、驻留需求直方图（四）、并发 trace 回放（五）、一致性审计清单（六）。
- 经验十条浓缩为一句话：**测量、归因、最坏情况、可观测性**——这也是全书精神。

## 思考题与练习

**1.（诊断题）** 某系统升级预取器后：预取覆盖率 92%、精度 45%、IPC 下降 7%。给出完整归因链与三个可选处置方案（含回滚之外的）。

**2.（诊断题）** NPU 单任务吞吐 90% 峰值、双任务并发 52%。列出你会依次检查的四个嫌疑（按 17 章内容），每项给出验证方法。

**3.（审计题）** 为下述数据流做一致性审计："CPU 准备输入（写普通内存）→ 拷入 dma-buf → NPU（ACE-Lite）读取 → 结果写入 NPU SPM → CPU 经一致性互连读取结果"。逐段标注：该段的一致性由谁保证、需要什么显式操作。

**4.（设计题）** 你接手一个 SPM 512 KB 的 NPU，目标网络族逐层驻留需求 P50=300 KB、P90=800 KB、P99=2 MB。给出你的容量决策与配套 tiling 策略，并说明为什么不按 P99 配。

**5.（综合题）** 把 17.2 十条经验中的任意三条，映射回本书具体章节的原理（例如"最坏情况是常态"→ 第 15 章 underflow），做成一张对照表——这张表本身就是你的复习大纲。

## 拓展阅读

1. 第 17 章是经验教训汇编，最合适的"拓展阅读"其实是前十六章——按练习 5 做一次总复习。
2. Kroft/SPIRE 类工业故障复盘集与各 CPU 厂商 errata 公告——真实世界的一致性与时序事故长什么样。
3. 《The Practice of System and Network Administration》或任何 SRE 事故复盘方法论文献——"症状-根因-对策"卡的工程学出处。

---
---

# 第 18 章 综合案例研究与实践练习

## 本章导读

毕业考。两个从零开始的综合设计案例（嵌入式 CPU 的 L1D、面向 CNN 的 NPU 存储层次），一个动手实践（Verilog 组相联 cache 或 Python cache 模拟器，二选一或都做），以及设计报告的撰写规范。两个案例都会完整走一遍第 16 章的闭环：需求 → 参数选择 → 定量验证 → 权衡报告。做完本章，这本书的知识才真正属于你。

## 学习目标

1. 独立完成一次 L1 数据 cache 的参数设计（含 VIPT 约束、PPA 验证、迭代修正）；
2. 独立完成一次 NPU 存储层次设计（SPM 容量、tiling、DMA 调度时间线）；
3. 实现并测试一个组相联 cache（Verilog/SystemC）或参数化 cache 模拟器（Python）；
4. 按 18.4 规范撰写设计文档，把每个决策写成"选项-理由-代价"三段式。

## 核心概念

| 术语（中文） | 英文 | 一句话定义 |
|---|---|---|
| 设计约束 | design constraints | 不可违背的硬边界（面积/延迟/功耗） |
| 目标函数 | objective function | 用于比较候选的量化标量 |
| 设计权衡表 | trade-off table | 每个决策一行的"选项-理由-代价"表 |
| 时间线分析 | timeline analysis | 搬运与计算重叠的甘特式推演 |
| 直接存储访问时间表 | DMA schedule | 双缓冲下每 tile 的搬运编排 |
| 覆盖率（验证） | coverage | 测试激励触达设计状态的比例 |
| 随机化验证 | randomized testing | 随机激励+参考模型比对的验证方法 |
| 黄金模型 | golden model | 行为级参考实现（如 Python 模拟器） |

## 18.1 案例 1：嵌入式 CPU 的 L1 数据 cache 设计

**任务书**：28 nm 工艺、目标主频 1 GHz（1 ns 周期）、**单周期命中**、面积 ≤0.15 mm²（L1D 阵列+外围）、功耗预算 ≤15 mW（动态典型）、目标负载为 MCU 常见混合负载（工作集足迹拐点 20 KB、读:写 = 4:1、写突发小）。

**设计推演**：

1. **容量初选**：足迹拐点 20 KB → 容量应落在 16～32 KB。先取 32 KB 候选；
2. **VIPT 合规检查**（第 7 章约束）：4 KB 页、单周期命中要求 index+offset ≤ 12 位。32 KB/64 B/8 路 = 6+6=12 ✓ 刚好压线；16 KB/4 路 = 6+6=12 ✓ 同样压线（注意：容量减半且相联度减半时组数不变，index 位数不变——此方案与 32 KB/8 路的 VIPT 约束相同，余量来自更小的阵列与比较器而非 VIPT）；
3. **块大小**：负载以栈/全局变量访问为主、空间局部性中等 → 32 B 块（offset 5 位）可放宽 index 空间；但 DRAM/闪存突发按 64 B 更经济 → 折中选 64 B；
4. **写策略**：写少读多 + MCU 下级是慢速 SRAM → **写回 + 写分配**（第 4 章主流组合），把写流量沉淀在 L1；
5. **替换**：4 路用真 LRU（仅 5 位/组）或树形 PLRU（3 位/组）——选 PLRU（面积小、更新局部）；
6. **banking**：2 bank（地址低位切），单周期内可服务一次读+一次写的并发（对齐 4:1 读写比）；
7. **物理验证**（CACTI 量级）：32 KB/8 路/64 B ≈ 0.1～0.15 mm²（含 tag）——**贴着面积红线**；且 8 路比较器在 1 ns 内紧张。
8. **迭代修正**：回退到 **16 KB/4 路/64 B**（VIPT 同为 12 位压线，但 4 路比较器与更小阵列带来时序余量、面积 ≈0.06 mm²）+ 加 next-line 预取器补偿容量减半——预期缺失率从 5% 升到 6.5%，但预取再压回 ~5%，而面积功耗双降。
9. **最终决策表**（节选）：

| 决策 | 选项 | 理由 | 代价 |
|---|---|---|---|
| 容量/相联 | 16 KB/4 路 | 单周期+面积红线+时序余量 | 缺失率 +1.5pp（预取补偿） |
| 写策略 | WB+WA | 写局部性好、下级慢 | 脏位管理、两段式缺失 |
| 预取 | next-line | 顺序代码/数据为主 | 少量污染（8B 粒度可关） |

**验证计划**：trace 回放（第 16 章自研模拟器）对 5 类代表负载跑缺失率；RTL 用 Python 模拟器做黄金模型做随机指令序列比对（18.3）。

## 18.2 案例 2：面向 CNN 的 NPU 存储层次设计

**任务书**：目标 ResNet18 级网络、输入 224×224 int8、吞吐 ≥200 fps、PE 阵列 4 TOPS @500 MHz、DRAM 可用带宽 12 GB/s、SPM 面积预算 ≤1 MB 等效、DMA 2 通道。

**设计推演**：

1. **总账**：ResNet18 ≈ 1.8 G MAC/图（约 3.6 GFLOP，按第 8 章乘加 ×2 口径）。200 fps → 360 GMAC/s（0.72 TOPS）≤ 4 TOPS，计算余量充足 → **关键是别让带宽勒死**（第 8 章结论预演）；
2. **带宽下限**：理想流量（每层数据过境一次）≈ 输入 0.15 MB + 权重 11 MB + 输出 0.5 MB ≈ 12 MB/图 × 200 fps = 2.4 GB/s —— 理论安全，但 12 GB/s 是"可用"值且 tiling 会放大 → 必须控制放大系数；
3. **SPM 分配**（统一池，9.4 路线）：输入池 384 KB + 输出/PSUM 池 384 KB + 权重池 256 KB ≈ 1 MB 预算内；PSUM 按 int32 记账（累加深度 Cin×K² 可达数千，int16 会溢出——int32 是业界标准，呼应第 10 章 TPU v1 的 4 MB int32 累加器）；
4. **tiling 推导**（第 10 章公式实战）：ResNet 深层典型层 `C=256, H=W=14, K=3`：输入 50 KB、输出 50 KB、权重 590 KB——权重池 256 KB 装不下 → 权重按 Cout 分 3 块流播（每块输入输出重读，放大系数 3）：该层流量 ≈ (50+50)×3+590 ≈ 890 KB。逐层做此推演，得全网络流量 ≈ 18 MB/图 → 3.6 GB/s @200fps ✓（余量 3 倍）；
5. **DMA 时间线**（第 9 章双缓冲 × 第 15 章流水线）：权重流播用 ping-pong（256 KB ×2？超预算！）→ 改为"权重单缓冲流播 + 输入输出双缓冲"（权重按 32 KB 微块细粒度补位，计算与搬运重叠率 >95%）；
6. **能耗侧写**（第 12 章）：DRAM 流量 3.6 GB/s × ~1 nJ/B(含控制器) ≈ 3.6 W——占总功耗主导 → 下版芯片的优化项：权重压缩（int8→4bit 结构化，流量减半）；
7. **决策表**（节选）：

| 决策 | 选项 | 理由 | 代价 |
|---|---|---|---|
| SPM 划分 | 统一池+软件分配 | 层间需求差异大 | 分配器复杂度 |
| 权重驻留 | 分块流播（×3 重读） | 权重池装不下全层 | 流量放大（预算内） |
| 缓冲结构 | 权重细粒度补位 | ping-pong 超面积 | 调度器复杂 |

**验证计划**：Python 流量模型逐层对账（tiling 公式 vs 模拟）；RTL 之前先用周期级 C++/SystemC 模型估 fps。

## 18.3 动手实践：组相联 Cache 的实现

**方案 A：Python cache 模拟器（必做，半天）**——第 16 章工具链的微型复刻：

```python
class Cache:
    def __init__(self, size_kb, ways, block_b):
        self.ways, self.block_b = ways, block_b
        self.blocks = size_kb*1024 // block_b
        self.sets = self.blocks // ways
        self.line = {}            # (set, tag) -> FIFO序/时间戳
        self.clock = 0
    def access(self, addr, is_write=False):
        self.clock += 1
        blk = addr // self.block_b
        s, tag = blk % self.sets, blk // self.sets
        if (s, tag) in self.line:      # 命中：更新时间戳
            self.line[(s, tag)] = self.clock; return True
        # 缺失：组满则按策略（LRU/FIFO/Random）逐出
        if len([k for k in self.line if k[0]==s]) >= self.ways:
            victim = min(...)          # 留作练习：实现三种策略
            del self.line[victim]
        self.line[(s, tag)] = self.clock; return False
```

**实验矩阵**：对同一条 trace（自造：顺序、步长 16、随机、循环周期 9）扫 容量 {16,32,64} KB × 相联度 {1,2,4,8} × 块 {32,64} B，画缺失率曲面；解释每个曲面的形状与本书曲线（7.1 节）的对应——尤其是**循环 trace 在直接映射上的 100% 缺失与 8 路下的骤降**（第 1/3 章抖动现象亲手复现）。

**方案 B：Verilog/SystemC 组相联 cache（进阶，一至两周）**——模块骨架：

```verilog
module cache #(
    parameter IDX=7, TAG=19, OFF=6, WAYS=4
)(
    input  clk, rstn,
    input  [IDX+TAG+OFF-1:0] addr,
    input  rw,                     // 0=read 1=write
    input  [63:0] wdata,
    output [63:0] rdata,
    output        hit, miss,
    // 缺失接口：向下级发请求/收数据（简化为阻塞式）
    output [TAG-1:0] req_tag,
    output [IDX-1:0] req_idx,
    input  [WAYS*64*8-1:0] fill_line,
    input        fill_valid
);
    reg [TAG+1:0] tag_arr [0:(1<<IDX)-1][0:WAYS-1]; // {tag,valid,dirty}
    reg [63:0]  dat_arr [0:(1<<IDX)-1][0:WAYS-1][0:7];
    reg [WAYS-2:0] plru  [0:(1<<IDX)-1];          // 树形PLRU
    // 练习：1) 组内并行比对 2) 树形PLRU更新/牺牲者选择
    //       3) 写回路径（脏行逐出）4) 与软件黄金模型比对
endmodule
```

**测试方法（黄金模型比对）**：Verilog 与 Python 模拟器吃同一随机地址流（`$readmemh` 灌入），逐笔比对 hit/miss 与读回数据——**行为等价验证**是硬件功能验证的基本范式（随机化+参考模型，17 章经验第六条的可观测性变体）。

## 18.4 综合设计报告规范

**结构（六段）**：

1. **目标与约束**：任务书逐条复述 + 目标函数（性能/功耗/面积的加权或约束式）；
2. **负载分析**：六问画像（16.1）+ 足迹/驻留直方图；
3. **微架构方案**：结构图（文字版）+ 每个部件引用本书原理章节；
4. **权衡表**：每个决策一行：`决策 | 备选项 | 选择理由（定量）| 付出的代价`——**宁可短，不可没有数字**；
5. **验证与结果**：模拟/CACTI 输出表、迭代轮次记录（含失败轮）、最坏情况分析；
6. **风险与后续**：陡坡参数、遗留问题、下版优化路线。

**评分自查三问**：①每个数字都有出处（公式/工具/文献）吗？②每个被否决的备选都写清为什么吗？③把报告拿掉名字，别人能否复现你的设计？（能复现 = 及格线以上。）

## 本章小结

- 案例 1 演示"约束驱动回退"：32 KB 被面积/时序/VIPT 三面夹击 → 16 KB + 预取的补偿路线；决策表让取舍透明。
- 案例 2 演示"流量驱动设计"：先算理想流量与放大系数，SPM/tiling/DMA 时间线都从带宽预算倒推；PSUM 按宽字记账是新手最易漏的一笔。
- 实践双方案：Python 模拟器（必做）建立直觉与验证资产；Verilog cache（进阶）体验黄金模型比对范式。
- 报告规范的核心是"可复现 + 定量 + 记录失败"——这是工程师与爱好者的分界线。

## 思考题与练习（毕业题）

**1.（毕业设计题 A）** 把案例 1 的负载改为"信号处理 DSP 负载（工作集 64 KB、读:写=1:1、强顺序性）"，重走全部推演，给出新决策表。

**2.（毕业设计题 B）** 把案例 2 的网络换成 MobileNetV2（DW+PW 为主），重算流量与 SPM 分配；指出哪一项设计决策必须翻转（提示：融合策略与容量/带宽优先级）。

**3.（实现题）** 完成 18.3 方案 A 并提交：缺失率曲面两张（容量×相联、块大小×容量）+ 一页结论（对照 7.1 节趋势图逐条解释异同）。

**4.（报告题）** 任选你做过的本书练习，按 18.4 规范补写成完整设计报告——这是你入行前的第一份作品集。

## 拓展阅读

1. 各开源项目：gem5 自带的 cache 模型源码（对照你的 Python 版）、BOOM/Rocket（RISC-V 开源核的 L1 实现实物）、NVDLA RTL——三个"能读的真硬件"。
2. Christian Jacobi 等 Teach Yourself 时序与验证入门材料 + 慕课 "Hardware Verification"（黄金模型/覆盖率方法论）。
3. 本书附录 B/C——写报告时放在手边。

---
---

# 附录 A　术语表（中英文对照）

（按主题分组；标注首次详细定义的章节）

**基础（第 1～2 章）**

| 中文 | 英文 | 章节 |
|---|---|---|
| 存储墙 | memory wall | 1.1 |
| 时间/空间局部性 | temporal / spatial locality | 1.2 |
| 存储层次 | memory hierarchy | 1.3 |
| 命中/缺失 | hit / miss | 1.4 |
| 平均访存时间 | AMAT (Average Memory Access Time) | 1.4 |
| 强制/容量/冲突缺失 | compulsory / capacity / conflict miss (3C) | 1.5 |
| 静态/动态随机存储器 | SRAM / DRAM | 2.1 |
| 6T 单元 / 1T1C 单元 | 6T cell / 1T1C cell | 2.1 |
| 刷新 / 行缓冲 | refresh / row buffer | 2.1 |
| 暂存存储器 | Scratchpad Memory (SPM) | 2.3 |
| 嵌入式 DRAM | eDRAM | 2.2 |
| 高带宽内存 | HBM (High Bandwidth Memory) | 2.2 |
| 重排序缓冲 | ROB (Reorder Buffer) | 2.6 |
| 存储缓冲 | Store Buffer | 2.6, 4.6 |
| 地址翻译后备缓冲 | TLB (Translation Lookaside Buffer) | 2.6 |

**CPU Cache（第 3～7 章）**

| 中文 | 英文 | 章节 |
|---|---|---|
| 直接映射/全相联/组相联 | direct-mapped / fully associative / set-associative | 3.2–3.4 |
| 组 / 路 | set / way | 3.4 |
| 标记 / 索引 / 块内偏移 | tag / index / block offset | 3.5 |
| 虚拟索引物理标记 | VIPT | 7.3 |
| LRU / 伪 LRU / 随机替换 | LRU / PLRU / random replacement | 4.2 |
| 写直达 / 写回 | write-through / write-back | 4.3–4.4 |
| 写分配 / 非写分配 | write-allocate / no-write-allocate | 4.5 |
| 写缓冲区 / 写合并 | write buffer / write merging | 4.6 |
| 脏位 / 有效位 | dirty bit / valid bit | 2.4, 4.4 |
| 包含/互斥/非包含 | inclusive / exclusive / NINE | 5.2 |
| 反向无效化 | back-invalidate | 5.2 |
| 受害者缓存 | victim cache | 5.3 |
| 路预测 | way prediction | 5.3 |
| 硬件/软件预取 | hardware / software prefetch | 5.4–5.5 |
| 缓存污染 | cache pollution | 5.4 |
| 写无效 / 写更新 | write-invalidate / write-update | 6.2 |
| 总线侦听 / 目录协议 | bus snooping / directory protocol | 6.3, 6.5 |
| MSI/MESI/MOESI/MESIF | 协议名 | 6.3–6.6 |
| 侦听过滤器 | snoop filter | 6.6 |
| bank 划分 / 路停机 | banking / way gating | 7.2 |
| 帕累托前沿 | Pareto frontier | 7.4, 16.2 |

**NPU（第 8～12 章）**

| 中文 | 英文 | 章节 |
|---|---|---|
| 特征图（输入/输出） | IFM / OFM (feature map) | 8.1 |
| 算术强度 | arithmetic intensity (AI) | 8.2 |
| 脉动阵列 | systolic array | 8.4 |
| 处理单元 / 乘加器 | PE (Processing Element) / MAC (Multiply-Accumulate) | 8.4 |
| DMA 描述符 | DMA descriptor | 9.2 |
| 双缓冲 / 多缓冲 | double / multi buffering | 9.2 |
| bank 冲突 / 填充 | bank conflict / padding | 9.3 |
| 数据流（权重/输出/行固定） | WS / OS / RS dataflow | 10.1 |
| 循环分块/交换/展开 | tiling / interchange / unrolling | 10.3 |
| im2col | im2col | 10.3 |
| 统一缓冲 / 权重 FIFO | Unified Buffer / Weight FIFO | 10.4 |
| CSR / CSC | compressed sparse row/column | 11.3 |
| 结构化稀疏（N:M） | structured sparsity (N:M) | 11.3 |
| 片上网络 | NoC (network-on-chip) | 11.4 |
| 屋顶线模型 | Roofline model | 12.3 |
| 算子融合 | operator fusion | 12.1 |
| 自动调优 | auto-tuning (AutoTVM/Ansor) | 12.2 |

**异构系统（第 13～15 章）**

| 中文 | 英文 | 章节 |
|---|---|---|
| 统一内存 | unified memory | 13.1 |
| IOMMU/SMMU | IOMMU / SMMU | 13.3 |
| 描述符环 / 门铃 | descriptor ring / doorbell | 13.2 |
| ACE / ACE-Lite | AMBA ACE / ACE-Lite | 13.4 |
| 非一致内存访问 | NUMA | 14.1 |
| AMBA CHI / CXL.cache / CXL.mem | 互连协议 | 14.2 |
| 内存模型 / 弱内存模型 | memory model / weakly-ordered model | 14.3 |
| 屏障 / 发布-获取 | barrier/fence / release-acquire | 14.3 |
| 零拷贝 | zero-copy | 15.1 |
| 软件流水线 | software pipelining | 15.2 |
| 仲裁 / 服务质量 | arbitration / QoS | 15.3 |

---

# 附录 B　推荐阅读与参考资料

**教材与专著**

1. John L. Hennessy & David A. Patterson, *Computer Architecture: A Quantitative Approach*, 6th ed., 2019——第 2 章（存储层次）与第 5 章（多处理器/一致性）是本书第二、四部分的母本。
2. Daniel Sorin, Mark Hill, David Wood, *A Primer on Memory Consistency and Cache Coherence*, 2nd ed., 2020（Morgan & Claypool，免费电子版）——一致性与内存模型的最佳系统教材。
3. Randal Bryant & David O'Hallaron, *深入理解计算机系统*（CSAPP），第 6 章——程序员视角的局部性与缓存优化，本书第 1 章的姊妹篇。
4. Vivienne Sze, Yu-Hsin Chen, Tien-Ju Yang, Joel Emer, *Efficient Processing of Deep Neural Networks*, 2020——NPU 数据流、SPM、能耗分析的系统教材（第三部分母本）。
5. David Money Harris & Sarah Harris, *Digital Design and Computer Architecture*——第 18 章动手实践前的数字设计补课。

**经典论文（按主题）**

6. Alan J. Smith, "Cache Memories," *ACM Computing Surveys*, 1982。
7. Norman Jouppi, "Improving Direct-Mapped Cache Performance by the Addition of a Small Fully-Associative Cache and Prefetch Buffers," ISCA 1990。
8. Andre Seznec, "A Case for Two-Way Skewed-Associative Caches," ISCA 1993。
9. Censier & Feautrier, "A New Solution to Coherence Problems in Multicache Systems," *IEEE Trans. Computers*, 1978。
10. Y.-H. Chen, J. Emer, V. Sze, "Eyeriss: A Spatial Architecture for Energy-Efficient Dataflow for CNNs," ISCA 2016。
11. Norman Jouppi et al., "In-Datacenter Performance Analysis of a TPU v1," ISCA 2017。
12. Mark Horowitz, "Computing's Energy Problem," ISSCC 2014。
13. Tianqi Chen et al., "TVM," OSDI 2018；Lianmin Zheng et al., "Ansor," OSDI 2020。
14. Song Han et al., "EIE," ISCA 2016；Aamer Jaleel et al., "RRIP," ISCA 2010。

**工具与手册**

15. gem5（gem5.org）、Sniper、CACTI、McPAT——第 16 章工具链的官方仓库与文档。
16. Arm AMBA AXI/ACE/CHI 规范（developer.arm.com）。
17. Intel® 64 and IA-32 Architectures Optimization Reference Manual。
18. NVDLA 开源项目（nvdla.org）——可读的真实 NPU RTL 与文档。
19. Linux 内核文档：DMA-API、dma-buf 子系统。

---

# 附录 C　常用设计参数速查表

**C.1 核心公式**

| 公式 | 表达 | 出处 |
|---|---|---|
| AMAT | 命中时间 + 缺失率 × 缺失代价 | 1.4 |
| 多级 AMAT | 递归套用（注意局部/全局缺失率） | 1.4 |
| offset/index/tag 位数 | log₂(块)/log₂(组数)/地址−其余 | 3.5 |
| VIPT 约束 | index + offset ≤ 页内偏移位数（4KB 页→12 位） | 7.3 |
| tag 开销 | 行数 × (tag+valid+dirty) bit | 3.5 |
| 真实 LRU 状态位 | log₂(N!) bit/组（2 路=1 bit） | 4.2 |
| Roofline | 吞吐 = min(峰值算力, 带宽×AI)；拐点=算力/带宽 | 12.3 |
| 算术强度 | AI = 运算次数 ÷ 搬运字节数 | 8.2 |
| 双缓冲稳态周期 | max(搬运, 计算) | 9.2 |
| 流水线稳态周期 | max(各段) | 15.2 |

**C.2 延迟与能耗数量级（背诵级）**

| 项目 | 数量级 |
|---|---|
| L1/L2/L3/DRAM 延迟 | ~4 / ~12 / ~40 / 200–300 周期 |
| int8 加/乘能耗 | ~0.03 / ~0.2 pJ（45nm） |
| 片上 SRAM 读 / DRAM 读 | ~5–20 pJ / ~640–2000 pJ |
| DRAM 刷新周期 | 64 ms 全片（tREFI≈7.8 µs） |

**C.3 真实芯片参数（公开资料口径，量级参考）**

| 芯片/部件 | 层级 | 容量 | 相联度 | 块 |
|---|---|---|---|---|
| Intel Skylake | L1D | 32 KB | 8 | 64 B |
| AMD Zen 4 | L1D | 32 KB | 8 | 64 B |
| Apple Firestorm | L1D | 128 KB | 未披露（推测 8） | 128 B |
| Arm Cortex-A55 | L1D | 8–64 KB | 4 | 64 B |
| Google TPU v1 | UB | 24 MB | —（软件管理） | — |
| NVIDIA NVDLA | SPM | 可选 512 KB | bank 化 | — |

> 注：发布年份参考——Skylake 2015 / Cortex-A55 2017 / TPU v1 2015 / NVDLA 2017 / Firestorm 2020 / Zen 4 2022。引用时请区分发布年数据与当代前沿，避免把历史参数当最新水平。

**C.4 快速决策口诀**

- 缺失分类 → 对策：强制→预取/大块；容量→扩容/tiling；冲突→相联度。
- 地址编译期可知 → SPM；运行时才知 → cache。
- 每层看 AI 对拐点：坡上省钱（流量），棚上榨利用率（计算）。
- 跨界数据流四问：走一致互连吗？clean 了吗？invalidate 了吗？fence 了吗？
- 预算按峰值联立，验收按最坏时序，参数选在平坦区。

---

# 结语

十八章的旅程走完：我们从一道裂缝（存储墙）出发，看着 CPU 用 cache"赌局部性"、NPU 用 SPM"算局部性"，再看两种文明在同一颗 SoC 上学会共存（一致域、零拷贝、QoS），最后亲手把这一切装进设计流程与两个毕业案例。

三个建议作为临别赠言：**一，数字是锤子**——本书每个结论都配了计算，遇到争论先抄公式；**二，最坏情况是常态**——平均值只会出现在汇报 PPT 里；**三，去动手**——18.3 的模拟器今晚就能写完第一个版本。

存储层次的故事没有终点：CXL 正在把一致性拉出机箱，存算一体想把计算埋进 DRAM 阵列，大模型把"带宽即一切"写进每份招股书。等你遇到它们时，希望这本书给的坐标系仍然够用。

祝设计顺利，缺失率常低。

*（全书完 · 2026-08-29）*
